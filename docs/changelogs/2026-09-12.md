## 2026-09-12 — #138 Options rewrite: semantic alignment check, card redesign, refine-flow UI fixes — committed, live-verified

### Changed

- **§5 — semantic alignment check** (new, this session): `lib/prompts/path-options-semantic-check.ts` — an independent LLM judge, separate from the generation prompt so it has no visibility into how or why a draft was produced. Real captured failure that motivated it: 2/4 options for an Amara-like profile were team-leadership-framed in substance ("Strategic Team Builder," "Outcome-Focused Mentor") against explicit delegation/team-management must-avoids, with zero hits from the existing phrase-match hard check. No negative-word-list expansion (explicitly rejected as the same brittle mechanism at larger scale) — judges substance holistically instead. Wired into `generate-path-options-session.ts` as hard check 4: one batched call per generation attempt (not per option), semantic-fail treated exactly like a must-avoid hit in the accept/reject/retry control flow, and — per this session's confirmed decision — the judge's own real reasoning always goes into the retry steer verbatim, never a generic template, for semantic-only rejections.
- **`app/api/path-options/route.ts`** — fixed the one remaining pending gap from §2's context-shape rewrite: `buildGenerationContext` was still constructing `OptionsGenerationContext` with the old `primary_constellation` field. Added `selectPrimaryDriverSignatures` — every signature with `confidence: High` (case-insensitive, per `PrimaryConstellationConfidence`'s own documented real-data casing inconsistency), falling back to the single highest-scoring signature when none are High. Closed out the last known project-wide `tsc` error.
- **§6 — option card redesign** (new, this session): `PathOptionsCandidate` gained `core_statement`, `tension`, `signatures_engaged`, `fit_score`, `fit_confidence` (new `FitConfidence` type — same literal casing as `PrimaryConstellationConfidence` but deliberately not aliased to it, since they measure genuinely different things). Generation prompt rewritten to produce the first three; the semantic-check judge extended to also emit `fit_score`/`fit_confidence` per draft, additive to its existing pass/fail verdict. `OptionsFlow.tsx` switched from hand-rolled card markup to the real `ConstellationCard` component (fixing a pre-existing, silently-missing number badge), rendering `select_if`, the `core_statement` callout, a `tension` block, a 2-box "Overall fit"/"Confidence" stat row, and `signatures_engaged` chip tags via the previously-unused `ChipRow`/`.option-card-sigs` infrastructure recovered from the pre-#129 flow.
- **`fit_score`/`fit_confidence` parsing** (`generate-path-options-session.ts`): validated *leniently and independently* of `pass`/`reasoning` — a malformed or missing value in either new field nulls out only that field, never the surrounding verdict, and never triggers the existing fail-closed fallback. This is what keeps the §6 extension genuinely additive: gating behavior is provably unchanged from before the extension, confirmed by re-running the real entrepreneur/must-avoid scenario and comparing rejection shapes before/after.
- **§3 — three UI fixes**, all in `components/OptionsFlow.tsx`:
  - Renamed the refine trigger "Not quite right?" → "Want a different option?" (matches what the button underneath actually does).
  - Refine card now hides once a candidate is tentatively selected (`!tentativeId` added to its guard) — previously stayed visible alongside "Confirm selection," which read as confusing.
  - Comments field "reads as inert" complaint: traced the actual data flow first — comments already save correctly and are already threaded into `#139`'s report generation (`lib/generate-path-report.ts`). Added a single inline acknowledgment ("Got it — this will be factored into your final report.").
- **Real bug found and fixed during live verification of the comments acknowledgment**: it was first implemented inside `OptionsFlow.tsx`'s own `'complete'` branch — which `app/path/page.tsx` has documented since #134 Slice 3 as unreachable dead code (`page.tsx` never renders `<OptionsFlow>` again once `options.status === 'complete'`; it goes straight to the report-loading interstitial / `<PathReportFlow>`). Confirmed via a real browser pass before assuming otherwise. Moved the acknowledgment to the one genuinely reachable spot — `page.tsx`'s `"Loading your Path report."` interstitial, conditional on `optionsContent.comments` — and hoisted the existing `optionsContent` narrowing earlier in that function so it's in scope there. `COMMENTS_ACKNOWLEDGMENT` exported from `OptionsFlow.tsx` for reuse rather than duplicated as a literal string; left rendering (harmlessly, still unreachable) in `OptionsFlow.tsx`'s own dead branch too, matching this project's existing "flag, don't necessarily delete yet" posture for that exact pre-existing dead code.
- **CSS dedup, caught during commit-prep review**: `.fit-stat-row` (added for §6's 2-box stat row) turned out to be a byte-for-byte duplicate of `/identity`'s existing `.scoring-chips` class except for column count. Extracted a shared `.stat-row` base (`display`/`gap`/`padding`/`border-top`); `.scoring-chips`/`.fit-stat-row` reduced to column-count-only modifiers, combined with `.stat-row` in markup. Touches `app/identity/page.tsx` (one added class token, zero visual change) alongside the Options-card files.

### Decisions

- Semantic check stays scoped to alignment only, not also distinctness between options — the existing material-duplicate check's own theoretical brittleness (phrase-overlap could miss a semantic duplicate) hasn't been observed as a real captured failure, so it's out of scope per this project's "fix real captured failures, not speculative ones" discipline. The batched-per-round architecture would allow that extension later without a redesign, if ever needed.
- `tension`/`core_statement` are deliberately **not** scanned by the existing must-avoid/duplicate hard checks. Reason: `tension` is designed to sometimes name must-avoid-adjacent territory as an honest, non-negated partial-overlap caveat — scanning it with the same phrase-match check would false-positive on exactly the content the field exists to contain.
- `fit_score`/`fit_confidence` payload to the judge stays `must_haves`/`must_avoids`/`ideal_life` + drafts only — deliberately not `signatures`/`how_you_operate`. The judge checks alignment with what the person stated, not whether an option correctly extrapolates their identity pattern (the generation prompt's job, not this independent check's).
- Comments acknowledgment is a single static line for the whole `report.loading` duration, no timer/fade/transient mechanism. Reason: report generation is comparable in length to Options generation (a real wait, not instant), and a timed show-then-hide would be new UI mechanism the session explicitly ruled out; `GeneratingState`'s `description` prop already works this way everywhere else in the codebase.
- The deeper "make the comment feel read" design (e.g., surfacing it inside the report itself) is deferred to `#139`, once that report's actual prompt is rebuilt under the life-path framing and can be verified for real — not guessed at here.

### Live-verification results

- Real-LLM pipeline run (`generateInitialOptions`, the entrepreneur/must-avoid repro scenario, both blank and non-blank `ideal_life`) — both scenarios accepted 4/4; `signatures_engaged` subset check passed in both (every name traced to the actual input signature set); semantic-only rejections showed real judge reasoning, not boilerplate; rejection *shapes* (job-title-only, job-title+semantic combined, semantic-only) matched the pre-§6 baseline, confirming the fit-field extension didn't shift gating.
- Real browser pass (fresh synthetic user, seeded `path_options_session` directly) confirming every §6 card element renders correctly: number badge, `core_statement` callout, `select_if` line, `tension` block, 2-box stat row (including the `—` fallback for a candidate with null `fit_score`/`fit_confidence`), `signatures_engaged` chip tags.
- Real browser pass driving the full selection flow confirming all three §3 fixes: renamed trigger present before selection, old copy gone; refine card genuinely absent after tentative selection; acknowledgment text present and exactly matching on the report-loading interstitial after confirming with a comment.
- Real browser regression check on `/identity` after the `.stat-row` CSS extraction: signature card's 4-chip row unchanged (4 equal 129px columns, correct labels/values) — confirmed via computed-style check, not just visual inspection.
- Project-wide `tsc` clean (`EXIT CODE: 0`, empty output) at every step, verified through throwaway copies before each real-file write.

### Files

- `app/api/path-options/route.ts`
- `app/globals.css`
- `app/identity/page.tsx`
- `app/path/page.tsx`
- `components/ConstellationCard.tsx`
- `components/OptionsFlow.tsx`
- `lib/artifact-schemas.ts`
- `lib/generate-path-options-session.ts`
- `lib/path-options-session.ts`
- `lib/prompts/path-options-semantic-check.ts`
- `lib/prompts/path-options-session.ts`
- `scripts/test-134-slice2-options-checks.mts`
- `scripts/test-134-slice3-report-checks.mts`

### Next

- **Industry-leak risk (§2's Verify requirement) — accepted, not open**: the literal-industry/context abstraction leak (e.g. "logistic" from the entrepreneur persona's evidence) appeared once across this session's real generation runs (two independent runs, deliberately using a leak-prone persona, each scanning explicitly for it) — no systemic pattern, one incidental mention. Accepted as sufficient verification to close #138 without a further dedicated check or a strengthened abstraction instruction — instructed directly in this session, 2026-09-12, per this project's Definition of Done (explicit, logged risk acceptance rather than an agent's own judgment call).
- `#139` ("Your Path" report rebuild) can now start — `#138` was its blocker per the brief's own Notes.
- `docs/briefs/138-options-life-path-rewrite.md` — deleted in its own commit this session (see session-end message): `#138`'s full scope is committed, live-verified, and its one flagged risk has real, logged sign-off.

---

## 2026-09-12 — Cross-account localStorage leak on /start — fixed, live-verified

### Changed

- **Root cause confirmed**: `app/start/page.tsx` wrote two flat, non-namespaced localStorage keys (`zyrro_discovery_answers`, `zyrro_user_name`) with no user-id scoping at all. A separate, unconditional `useEffect` read `zyrro_discovery_answers` into `answers` state on every mount, racing ahead of the async auth-resolution effect — so whichever browser occupant wrote last had their answers silently prefilled for the next visitor, authenticated or anonymous. Because `saveToStorage` always wrote the full 13-question array, an untouched slot for the new visitor retained the prior visitor's text and could be submitted to Supabase as that new visitor's own answer via `/api/stage-discovery-answers`/`/api/complete-discovery`. Ruled out the already-fixed cookie leak (`3979b8c`) as the cause — confirmed `/account` correctly redirected to `/login` with no session.
- **Fix**: every write to both keys is now stamped with an `ownerId` (the real Supabase `user.id`, or `null` for an anonymous writer). A single identity gate, `clearStorageUnlessOwnedBy`, runs inside the existing `resolveMode()` effect (the old separate prefill effect was folded in, closing the race) and wipes both keys outright unless `resolvedOwnerId` is a real id that exactly matches the stamp. Anonymous visits (`resolvedOwnerId === null`) never trust any pre-existing blob, full stop — including one they wrote themselves moments earlier — because localStorage can't prove continuity between two anonymous visitors either way.
- Added defense-in-depth clearing of both keys on logout (`app/app/logout-button.tsx`, `app/account/page.tsx`) and on successful flow completion (`handleContactSubmit`, `handleFinishState3`), so a completed flow doesn't linger in storage for the next browser occupant.

### Decisions

- Anonymous visitors now lose in-progress questionnaire answers on any reload — a real behavior change from the prior always-persist design. Explicit tradeoff directed this session: since localStorage can't distinguish one anonymous visitor from the next, every fresh anonymous visit is treated as unproven/untrusted by default, identically to an authenticated-identity mismatch, rather than building a session-token system to special-case same-tab continuity.

### Live-verification results

- Real two-synthetic-user Playwright pass, one persistent browser context, cookies swapped between users to simulate a shared computer (ad hoc script, written and deleted this session — not part of the repo): confirmed none of the 13 question slots leak user A's answers to user B, including the compounding case (slots A never touched); confirmed both localStorage keys are actually wiped (not just hidden in the UI) the moment an identity mismatch is detected; confirmed a same-owner reload still legitimately resumes in-progress answers; confirmed an anonymous visit after B refuses to trust even B's own honestly-stamped data.
- Project-wide `tsc` clean, verified via a throwaway copy before applying the real edit and again against the real files afterward.

### Files

- `app/start/page.tsx`
- `app/app/logout-button.tsx`
- `app/account/page.tsx`

### Next

- Nothing outstanding — full scope live-verified this session, no leftover piece to hand off.
