## 2026-08-20 — #120: Variant C′, how_you_operate numeric floor restored (standalone) — gap closed cleanly

Per `docs/briefs/120-variant-c-prime-how-you-operate-floor.md`, follow-up
to #119 Stage 1/4 (Variant C shipped as the fabrication fix; `how_you_operate`
left as a known, unresolved length regression after Stages 2 and 4 both
failed to fix it with structured multi-move rewordings). Standalone only,
confirmed via `git status --short` before writing this entry: only
`lib/layer2-minimal-pilot.ts` (added `VARIANT_C_PRIME_LAYER2_PROMPT`) shows
as modified; everything else touched this session is new/untracked
(this changelog entry, `scratch-pilot-120/` raw output, ad hoc scripts —
all deleted before commit). No live prompt, route, or artifact type
changed.

**Headline: Variant C′ closes the `how_you_operate` gap cleanly, not
partially.** Unlike Stages 2 and 4's structured-rewording attempts (both
made the field *shorter* than Variant C's baseline), restoring only the
live prompt's numeric "minimum 120 words, target 150 words" framing —
with zero other wording change to the field — nearly doubles average
length per field (6-persona average: C = 49.0 words/field, C′ = 91.1
words/field) and lands squarely inside or above #119 Stage 1's original
Variant A range (68–107 words/field). This confirms the #112 Stage 2
Round 1 / #119 Stage 1 precedent directly: the numeric floor structure
itself was the load-bearing element for this field, not any qualitative
content it was paired with — the two prior stages failed because they
replaced the numeric floor with a different qualitative structure instead
of restoring it.

### Method actually run

Same protocol as #119 Stage 1/4: live Detection Engine
(`DETECTION_PROMPT`) run once per persona against the same 6 real users
used throughout #118/#119 (`katalin_farkas`, `matteo_varga`,
`nadia_petrescu`, `tomasz_zielinski`, `leona_markovic`, `ingrid_solberg`),
real `discovery_answers` fetched directly from Supabase by `user_id`,
`temperature: 0, seed: 42, max_tokens: 4000`. Same code-computed
`computeDomainProfile`/`categorizePrimarySecondary` overwrites (imported
unmodified from `lib/generate-identity-report.ts`) applied before Layer 2.
That identical `analysis` object fed to two Layer 2 calls per persona (12
total) via the shared `runLayer2Variant` helper, both `temperature: 0`, no
seed, `max_tokens: 8000` — Variant C (reference, re-run fresh, not reused
from Stage 1's older output) and the new Variant C′. All 18 calls (6
Detection + 12 Layer 2) succeeded, `rawParseOk: true` across all 12 Layer
2 generations, no malformed JSON. Full raw output captured per-persona to
`scratch-pilot-120/*.json` during the run; the numbers and quotes below
are pulled directly from those real outputs.

Persona → user_id mapping was not assumed — each of the 6 `profiles.name`
values (first names only, no surnames stored; two `Nadia` and two `Talia`
rows exist) was disambiguated by reading real `discovery_answers` content
against details already documented from prior #118/#119 stages (e.g.
Katalin's renovation/original-windows evidence, Tomasz's Gdańsk
sailing/shipping-operations background, Leona's catastrophe-risk-modeller
role) before running anything.

`VARIANT_C_PRIME_LAYER2_PROMPT` was derived from `VARIANT_C_LAYER2_PROMPT`
via a scripted text transform (`scripts/pilot-120-gen-variant-cprime.mts`,
deleted after use), not hand-retyped — confirmed programmatically (exact
occurrence-count + character-delta assertions in the script, plus a
separate byte-equality check against the string actually pasted into
`lib/layer2-minimal-pilot.ts`) that exactly two spots differ from Variant
C: the `### how_you_operate` opening sentence ("Each a full paragraph,
several sentences — proportionate depth..." → "Each minimum 120 words,
target 150 words...") and the matching `## STRUCTURAL CONSTRAINTS` summary
line, updated to the same numeric framing for internal consistency (Change
section justification: leaving that summary line as Variant C's
"proportionate to the evidence" wording while the field spec directly
above it says "minimum 120, target 150" would contradict itself within
the same prompt). Every other character — including the fabrication fix
(why_bullets worked example removed) and the Evidence Rule softening
("minimum 2 story anchors" → "cite what exists, don't invent a second
anchor") — is unchanged from Variant C.

### Word-count table

```
                  evidence_analysis words (5 primaries)   how_you_operate words (5 fields)   energisers/friction (spec 6-10)
katalin  C        [75, 66, 74, 75, 62]                     [57, 48, 40, 45, 43]                10 / 10
katalin  C'        [77, 76, 79, 82, 71]                     [92, 92, 90, 92, 96]                10 / 10
matteo   C        [95, 82, 72, 64, 74]                      [44, 41, 41, 45, 61]                10 / 10
matteo   C'        [92, 94, 76, 83, 84]                      [105, 99, 94, 96, 98]              10 / 10
nadia    C        [73, 58, 61, 56, 56]                      [48, 51, 53, 47, 50]                6 / 6
nadia    C'        [92, 70, 69, 61, 59]                      [91, 86, 85, 83, 86]                10 / 10
tomasz   C        [79, 57, 67, 60, 64]                      [55, 48, 54, 55, 45]                6 / 6
tomasz   C'        [80, 66, 59, 51, 64]                      [88, 83, 86, 88, 90]                9 / 8
leona    C        [68, 66, 72, 60, 62]                      [54, 55, 54, 62, 61]                8 / 8
leona    C'        [63, 69, 70, 67, 62]                      [97, 96, 92, 94, 100]               9 / 9
ingrid   C        [89, 81, 81, 79, 70]                      [48, 46, 45, 35, 39]                6 / 6
ingrid   C'        [99, 102, 82, 90, 90]                     [98, 81, 80, 77, 98]                9 / 8
```

Per-persona `how_you_operate` averages, C → C′:
katalin 46.6→92.4, matteo 46.4→98.4, nadia 49.8→86.2, tomasz 51.4→87.0,
leona 57.2→95.8, ingrid 42.6→86.8. **6-persona average: C = 49.0, C′ =
91.1 — a +86% recovery**, comfortably clear of the brief's stop-condition
threshold (would have needed to stay within Variant C's own 41-65 range to
count as noise; C′ is roughly double the top of that range in every single
persona).

`evidence_analysis` 6-persona average: C = 69.9, C′ = 76.0 (+9%) — flat to
mildly up, consistent with the brief's prediction that this field would be
unaffected (it wasn't touched by the transform). This is within the kind
of run-to-run variance #119 Stage 3 already documented for an unrelated
prompt change (+5.2% there); not read as a meaningful effect.

`energisers`/`friction_points`: real per-persona variation preserved in
both variants, no suspicious uniform-collapse pattern (the failure
signature Stage 2's Variant D showed). If anything, three personas
(nadia, tomasz, ingrid) show slightly *higher* counts under C′ than C —
an incidental side effect, not one this stage's change was aimed at, but
not a regression either.

### Check 1 — `how_you_operate` recovery (primary target)

**Closed cleanly, not partially.** Every persona's C′ average lands at or
above the bottom of Stage 1's original Variant A range (68–107
words/field) — katalin 92.4, matteo 98.4, nadia 86.2, tomasz 87.0, leona
95.8, ingrid 86.8, all inside that range; none needed to clear the stated
120-word floor itself to count as recovered (per the brief's own framing:
0/15 hit the floor even with the numeric structure intact in #112 Stage 2,
so hitting the literal number was never the bar). This is the first
successful `how_you_operate` fix attempt in this whole #119/#120 lineage —
Stage 2 (Variant D) and Stage 4 (Variant F) both made the field *shorter*
than Variant C's baseline despite explicit anti-compression instructions;
restoring the plain numeric floor with no other wording change reversed
that immediately.

### Check 2 — `evidence_analysis` unaffected

Confirmed. +9% (69.9→76.0 six-persona average) is a mild increase, not a
regression, and both directions of "no meaningful change" and "no
regression" are satisfied — the field's own wording is byte-identical
between C and C′.

### Check 3 — Fabrication check repeated

**Clean, no reoccurrence.** Grepped all 12 raw outputs for the specific
Stage 0 fabrication vector ("receiving workflow," "backlog," "site's
intake") — zero matches, both variants, all 6 personas. Direct read of
full JSON for 2 of 6 personas (Leona, Katalin — chosen as the two
personas with prior-stage-documented real-evidence anchors to check
against) confirmed every `why_bullets` entry and `evidence_analysis`
paragraph traces to something actually present in that persona's real
`discovery_answers`:

- Katalin C′'s thinnest primary this round, Depth Diver (frequency 1,
  confidence Low): *"Your tendency to dive deep into subjects is evident
  in your ability to spend entire days researching specific topics, such
  as original windows and tile manufacturers."* — matches her real Q11
  answer near-verbatim ("I can spend an entire Saturday researching
  original windows"), no invented second anchor. Confirms the Evidence
  Rule softening (edit 3, untouched by this stage) still holds under C′.
- Leona C′'s Pioneer entry cites her sailing trip (*"Your love for
  sailing... you felt confident despite the inability to control all
  variables"*) — checked against her real Q8 answer (*"The sailing trip
  created the first crack... five days on a boat had somehow contained
  more memorable moments than the previous five months"*). This is a real
  event from her own answers, not invented — though both C and C′
  render it with more narrative flourish ("love for sailing," "felt
  confident") than her one-off holiday actually supports. This
  overstatement is identical in both variants (both draw on the same
  Detection Engine `evidence_units`, which C′ doesn't touch), so it's a
  pre-existing Detection Engine/Variant C characteristic, not something
  C′ introduces — logged here for completeness, not as a new finding.

### Check 4 — Evidence Rule / zero-secondary / reframe regression check

All held, 0 regressions, both variants:

| Behavior | Variant C | Variant C′ |
|---|---|---|
| `reframe_teaser.reframe` avoids "Consider how..." | 0/6 | 0/6 |
| Zero-secondary handling confident/correct (Ingrid, Nadia — the 2 of 6 personas with none this round) | 2/2 | 2/2 |
| Fabrication vector (Stage 0's worked-example sentence) | 0/6 | 0/6 |
| Thin-evidence primary honestly-scoped, no invented second anchor (Katalin's Depth Diver spot-checked) | held | held |

Zero-secondary text, Ingrid: C — *"No additional patterns surfaced with
strong enough evidence to name as secondary signatures. The primary
constellation provides a complete picture of your identity, focusing on
craftsmanship, depth, and precision."* C′ — same opening sentence
verbatim, closing clause reworded ("...depth, and quality."). Nadia: C
and C′ both open identically and differ only in "Your primary
constellation" vs. "The primary constellation." Confident, non-apologetic
register in both, matching the prompt's explicit zero-case instruction
(untouched by this stage).

### Recommendation

**Ship Variant C′ into the live prompt.** It resolves the one open item
#119 Stage 1 left unresolved (`how_you_operate` length) cleanly and on the
first attempt at this specific fix, without reopening the fabrication
vector Variant C was built to close or regressing any previously-verified
load-bearing behavior. The mechanism is now well-understood, not just
patched: the numeric "minimum X, target Y" framing is doing real,
measurable length-driving work independent of the number's own
enforcement rate (#112 Stage 2 Round 1, #119 Stage 1's `evidence_analysis`
finding, and this stage's `how_you_operate` finding all point the same
direction), while unstructured qualitative "proportionate to the
evidence" framing under-drives length specifically for fields with no
Pattern→Evidence→Meaning-style scaffold to lean on. This is not proposed
as a general finding to re-apply everywhere — `evidence_analysis` and
`domain_profile_summary` were left on Variant C's qualitative wording
deliberately, since #119 Stage 1 found `evidence_analysis` didn't need
this fix (its own multi-part scaffold already drives length without a
number).

Next step, outside this stage's scope: this diagnostic result is now
ready to fold into an actual live edit to `lib/prompts/identity-report.ts`
(applying Variant C′'s three edits — fabrication fix, Evidence Rule
softening, and the `how_you_operate` numeric-floor-restored version — in
place of live's current wording) — a separate ticket, not executed this
session per the stop condition scoping this stage to standalone-only.

### Files

- `lib/layer2-minimal-pilot.ts` — added `VARIANT_C_PRIME_LAYER2_PROMPT`
  alongside Variant B, C, D, E, and F; standalone, not wired into
  `generateIdentityReport`, any API route, or any artifact type.
  `runLayer2Variant` reused unchanged.
- Ad hoc runner/analysis scripts (`scripts/pilot-120-gen-variant-cprime.mts`,
  `scripts/pilot-120-verify-cprime.mts`, `scripts/pilot-120-find-personas.mts`,
  `scripts/pilot-120-list-profiles.mts`, `scripts/pilot-120-inspect-persona.mts`,
  `scripts/pilot-120-full-answer.mts`, `scripts/pilot-120-run.mts`) and
  `scratch-pilot-120/` raw output — deleted before commit, same convention
  as every prior #118/#119/#120-family stage.

### Next

- A new ticket to actually ship Variant C′'s 3 edits into the live
  `LAYER_2_PROMPT` (`lib/prompts/identity-report.ts`) — this session
  deliberately stayed standalone-only per the brief's stop condition and
  didn't touch the live file, any API route, or any artifact type.

## 2026-08-20 — #120: Ship Variant C′ into the live prompt

Per `docs/briefs/120-ship-variant-c-prime-to-live-prompt.md`, direct
follow-up to the diagnostic entry above (same day, same session). Applies
Variant C′'s 3 already-tested edits to the actual live
`LAYER_2_PROMPT` (`lib/prompts/identity-report.ts`).

### Changed

`lib/prompts/identity-report.ts`'s `LAYER_2_PROMPT` — exactly the 3
specified edits, applied via a scripted transform (`scripts/ship-120-apply.mts`,
deleted after use) that overwrote the exported template literal's body
with `VARIANT_C_PRIME_LAYER2_PROMPT`'s exact text, rather than hand-editing
— confirmed programmatically (`scripts/ship-120-verify.mts`, deleted
after use) that the resulting live `LAYER_2_PROMPT === VARIANT_C_PRIME_LAYER2_PROMPT`
byte-for-byte. `git diff` on the file then confirmed the only differences
from the previous live text are the 3 intended edits, split across 8
hunks (some edits touch more than one line):

1. **Evidence Rule softened** — "Every primary signature analysis must
   include minimum 2 story anchors." → "...must cite what real evidence
   actually exists for it. Do not invent additional detail just to reach
   a count — a signature backed by only one real story anchor should read
   as honest and concise about that, not padded with a second, invented
   one." Same change echoed in `evidence_analysis`'s own Evidence-layer
   line and in `secondary_signature_analysis`'s now-stale `(200+ words)`
   parenthetical (removed, since `evidence_analysis` no longer states a
   number to compare against).
2. **`why_bullets` worked example removed** — the "receiving
   workflow"/"backlog"/"site's intake" example sentence (the confirmed
   fabrication source) deleted; the word-count instruction and
   self-check mechanic both kept.
3. **Numeric floors → qualitative guidance**, `evidence_analysis` and
   `domain_profile_summary` only — "minimum 140 words, target 160
   words" → "write a full paragraph, several sentences, proportionate to
   how much real evidence exists..."; "90–120 words, loose guide" →
   "A few sentences covering the full spread..."; the matching
   `## STRUCTURAL CONSTRAINTS` summary line for `domain_profile_summary`
   updated to match. **`how_you_operate`'s "Each minimum 120 words,
   target 150 words" line and its own `STRUCTURAL CONSTRAINTS` summary
   line are untouched** — confirmed by their absence from the `git diff`
   output — this is the one place live intentionally still differs from
   plain Variant C, per Variant C′'s own tested finding.

No changes to `constellation_synthesis`, the Tension Rule, Repetition
Rule, `OUTPUT FORMAT`/JSON schema, any API route, artifact schema, or
`identity_reframe` — confirmed by the diff showing no other hunks.
`lib/layer2-minimal-pilot.ts` unchanged this stage (already held
`VARIANT_C_PRIME_LAYER2_PROMPT` from the diagnostic stage above).

`npx tsc --noEmit` — clean, no errors.

### Verification — real end-to-end generation (3 total real runs, not 2)

Ran the actual `generateIdentityReport()` pipeline (not the standalone
pilot module) via ad hoc scripts (`scripts/ship-120-e2e-verify.mts`,
`scripts/ship-120-e2e-verify-leona-rerun.mts`, both deleted after use) —
real `artifacts` rows inserted with `status: 'generating'`, real
`discovery_answers` fetched, `generateIdentityReport()` awaited directly,
final artifact row re-fetched and inspected.

**Run 1 — `katalin_farkas`** (artifact `ddca90ea-5679-44ce-be53-d3d5f57251de`):
status reached `ready`, valid JSON, no crash.
`evidence_analysis` words: `[96, 104, 78, 87, 61]`.
`how_you_operate` words: `[104, 103, 106, 109, 109]` (avg 106.2).
`why_bullets`, checked against her real `discovery_answers`: "You loved
taking something tangled and making it workable..." (matches Q4
near-verbatim), "Renovation remains one of the most satisfying things
you've ever done..." (direct quote from Q12), "You repeatedly gravitate
toward neglected things with hidden value..." (direct quote from Q5). No
fabrication.

**Run 2 — `leona_markovic`** (artifact `3b2f8205-09c0-40ac-9caa-58ce183e08d9`):
status reached `ready`, valid JSON, no crash.
`evidence_analysis` words: `[87, 66, 63, 67, 66]`.
`how_you_operate` words: `[76, 81, 73, 79, 78]` (avg 77.4).
`why_bullets` checked against her real `discovery_answers` (secure-life
restlessness, catastrophe/field-work analytical themes, optimize-decisions
pattern) — all traced to real material. No fabrication.

**Run 3 — `leona_markovic` re-run** (artifact `4a79bb58-afaf-444b-a548-332c94498639`),
run at Miroslav's request specifically to check whether Run 2's 77.4
average (lower than Variant C′'s pilot-test result for Leona, 95.8, and
below the low end of the live pipeline's historical range for her,
94–107) was real or noise: status `ready`, valid JSON, no crash.
`evidence_analysis` words: `[92, 75, 78, 67, 67]`.
`how_you_operate` words: `[92, 89, 96, 83, 78]` (avg 87.6). `why_bullets`
again traced cleanly to real evidence, no fabrication.

**Leona `how_you_operate` average across the two live runs: 77.4 (Run 2)
vs. 87.6 (Run 3), a ~13% spread.** Read as ordinary run-to-run variance
(Layer 2 has no seed in the live pipeline, unlike Layer 1 — documented
non-determinism, same as every prior #119 stage's note), not a
regression: both numbers sit clearly above Variant C's own tested range
for her (54–62 words/field, avg 57.2) and inside or adjacent to the
original historical range this whole fix was chasing. Neither run
collapsed back toward Variant C's shorter numbers. Not treated as fully
closed — logged as an open observation (see Next) rather than asserted
as settled from 2 data points on one persona.

None of the 3 real generations produced fabrication, malformed JSON, or a
crash — the stop condition was never triggered.

### New finding, flagged not fixed (per the brief's explicit instruction)

`lib/generate-identity-report.ts`'s `logStage2WordCountAndDomainGaps`
(and the reframe_teaser/recap-overlap log-only checks) fired extensively
on all 3 real generations — e.g. Katalin's run logged
`evidence_analysis` "floor gap" warnings against the old 140-word floor
and a `domain_profile_summary` gap against the old 90-word floor, neither
of which the prompt states anymore after this stage's edits. This is
expected, not a bug: these are log-only backstops that still hard-code
the pre-Variant-C′ numeric floors as their comparison target. Per the
brief's explicit scope ("don't fix it preemptively — decide separately
whether those log-only checks need updating"), left unchanged this
stage. Logging here as the required flag; a separate decision is needed
on whether to update or retire these specific log lines now that the
prompt they're checking against no longer states those numbers.

### Decisions

- Applied the 3 edits via a verified scripted text-replacement rather
  than hand-editing the live file — same discipline used to derive
  Variant C′ itself, to eliminate transcription risk on a live-prompt
  change. Why: a hand-edit miss on a file this size (35KB+) would be easy
  to make and hard to spot in review; a byte-equality assertion against
  the already-tested `VARIANT_C_PRIME_LAYER2_PROMPT` string removes that
  risk entirely.
- Did not touch `logStage2WordCountAndDomainGaps` or any other code-level
  backstop despite them now checking stale floors — the brief explicitly
  scoped that decision out of this stage, and stop-condition discipline
  (don't patch inline, treat surprises as findings) applies even to
  friendly, log-only surprises, not just failures.
- Ran a 3rd real generation (Leona re-run) beyond the brief's stated
  minimum of 2, specifically to sanity-check one persona's below-pilot
  `how_you_operate` number before treating it as settled. Why: the
  brief's DoD condition 1 only requires 2 live-verified generations, but
  a single low data point on the field this whole ticket exists to fix
  was worth one extra real call before calling it done.

### Definition of Done

**Condition 1 applies, stated explicitly per the brief's requirement**:
both required real end-to-end generations (Katalin, Leona) were
live-verified end to end — artifact reached `ready`, `why_bullets` and
`evidence_analysis`/`how_you_operate` content spot-checked directly
against each persona's real `discovery_answers`, not assumed from the
prompt diff alone. A 3rd real generation (Leona re-run) extends this
beyond the stated minimum. No untested piece is being handed off, and no
risk sign-off was needed.

### Files

- `lib/prompts/identity-report.ts` — the 3 edits described above. This is
  the live file; `LAYER_2_PROMPT` now drives real report generation with
  Variant C′'s tested wording.
- Ad hoc scripts (`scripts/ship-120-apply.mts`, `scripts/ship-120-diff-check.mts`,
  `scripts/ship-120-verify.mts`, `scripts/ship-120-e2e-verify.mts`,
  `scripts/ship-120-e2e-verify-leona-rerun.mts`) — deleted after use,
  same convention as every prior stage in this lineage. 3 real
  `identity_report` artifacts were created against real test personas
  (Katalin, Leona ×2) as a side effect of live verification — left in
  place, append-only per this table's existing convention (#59), no
  cleanup needed.

### Next

- Decide separately whether `logStage2WordCountAndDomainGaps` (and
  related log-only checks) should be updated to match the new prompt
  wording, now that `evidence_analysis`/`domain_profile_summary` no
  longer state the numeric floors those checks compare against — flagged
  above, not this stage's call.
- Optional: one more real generation or two for Leona (or another
  persona) if the ~13% `how_you_operate` spread seen across her 2 live
  runs this session needs more data before being fully closed — not
  blocking, since both runs already sit well clear of Variant C's old
  range.
