## 2026-08-05 — #100 Stage 2: Cover section reorder

Per `docs/briefs/100-cover-reorder-brief.md`. Pure JSX reorder/removal
inside `app/identity/page.tsx`'s Cover block — no data model changes, no
prompt changes, no new components, no CSS file edits.

### Changed
- **`app/identity/page.tsx`** —
  - Context line (`cover.prepared_for · cover.identity_context`) moved from
    below `identity-thesis` to directly under the `<h1>` named-identity
    heading, now rendering *before* the thesis rather than after.
  - Date line (`prepared-for-line` / `reportDate` / `formatDate`) deleted
    outright, not relocated.
  - Constellation Synthesis moved into `.report-cover`, positioned
    immediately after `identity-thesis`. Its standalone `.report-section`
    wrapper (previously the first child of `.report-sections`) was removed
    entirely, not left as an empty shell. The `"CONSTELLATION SYNTHESIS"`
    eyebrow and the `<h3 className="named-identity mb-12">` heading were
    dropped — not rendered anywhere. The `.card` wrapper div was removed;
    `constellation_synthesis.synthesis` now renders as a bare `<p>` with no
    wrapping div and no `className`.
  - `constellation_synthesis.named_identity` is now unused in this file as
    a direct result of dropping the heading — expected per the brief, not a
    bug.
  - Removed now-dead code that only existed to support the deleted date
    line: `reportDate` (derived from `genPhase.createdAt`), the
    `formatDate()` helper, and `MONTHS` (formatDate's only consumer).
    Confirmed via grep that none of the three had any other reference in
    the file before removing them.

### Decisions
- Left `MONTHS`/`formatDate`/`reportDate` removal in scope even though the
  brief only named `reportDate`/`formatDate` explicitly — `MONTHS` became
  transitively dead the moment `formatDate` was removed, so leaving it
  behind would have been an inconsistent partial cleanup of the same dead
  code path.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean.
- **Live** via `withVerificationSession`, seeding a real ready
  `identity_report` (no new generation needed — this ticket makes no
  content changes). Confirmed via a real Playwright page load against
  `/identity`:
  - DOM order inside `.report-cover`: eyebrow → badge → `<h1>` → context
    line → identity thesis → constellation synthesis paragraph, in that
    exact order (checked via `cover.children` enumeration, not just
    presence).
  - No date text renders anywhere on the page (checked for month-name
    strings in `document.body.textContent`).
  - Constellation synthesis text renders as a plain `<p>` (empty
    `className`), parented directly by `.report-cover`, not inside a
    `.card` div, with no eyebrow and no heading above it.
  - `.report-sections`' first child section's eyebrow is now `SIGNATURE
    PROFILE`; `"CONSTELLATION SYNTHESIS"` does not appear anywhere in the
    page's eyebrow list — confirms the old `.report-section` is gone, not
    just emptied.
  - Zero console/page errors.
  - Screenshot saved to `.verification-runs/screenshots/` (gitignored),
    not committed.
- Ad hoc verification script run from `scripts/verify-100-cover-reorder.mts`,
  deleted after, not committed — confirmed via `git status`.

### Stage 3 flag (not actioned here — CSS edits explicitly out of scope)
- `.prepared-for-line` is now fully orphaned repo-wide (its only consumer
  was the deleted date line). `.card` is not orphaned — still in active use
  elsewhere in this file (Signature Profile, How You Operate), so only
  `.prepared-for-line` is confirmed dead.

### Next
- **Stage 3** — CSS cleanup, including removing the now-orphaned
  `.prepared-for-line` rule.

## 2026-08-05 — #100 Stage 2: merge Primary+Secondary signature bars into one card, remove numbering

Per `docs/briefs/100-signature-profile-merge-bars-brief.md`. Scaled-down
first step toward the eventual unified all-tier chart — Emerging/Suppressed
signatures are explicitly deferred, not part of this ticket.

### Changed
- **`components/PrimarySignatureBars.tsx`** — added an optional `numbered`
  prop, default `true`. When `false`, `.sig-num-circle` is omitted entirely
  from each row; name, domain, bar fill, and score are unchanged. Default
  behavior is unchanged for any caller that doesn't pass the prop.
- **`app/identity/page.tsx`**, Signature Profile section —
  - Now calls `<PrimarySignatureBars signatures={primary_constellation} numbered={false} />`.
  - Secondary block's `.sig-num-circle-muted` div removed from each row —
    same "drop the circle, keep everything else" treatment.
  - Primary's and Secondary's previously-separate `.card` divs merged into
    one `.card`: Primary's rows first (under its own `card-sub-label`,
    "Primary Signatures"), then Secondary's rows (under "Secondary
    Signatures"), each still under its own group label. The existing
    `secondary_signature_analysis.length > 0` conditional still gates only
    the Secondary group within the merged card — Primary (always exactly 5)
    renders unconditionally. Domain Radar's own `.card` untouched, stays
    separate.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean.
- **Live** via `withVerificationSession`, seeding real ready `identity_report`
  content (no new generation needed). Confirmed via real Playwright page
  loads:
  - `/identity` with a non-empty `secondary_signature_analysis`: Signature
    Profile's Primary+Secondary render inside a single `.card` (not two),
    with both `card-sub-label`s present, 7 `.sig-row`s total, zero
    `.sig-num-circle`/`.sig-num-circle-muted` anywhere in the section.
  - `/identity` with an **empty** `secondary_signature_analysis` (added
    case, beyond the brief's own list): same merged card renders with only
    the "Primary Signatures" label, 5 `.sig-row`s, no "Secondary Signatures"
    text anywhere on the page, no circles — confirms Primary always renders
    and Secondary cleanly disappears rather than leaving an empty group.
  - `/dashboard`'s Identity Card, expanded: `PrimarySignatureBars` still
    renders **with** numbered circles 1-5 — confirmed directly (not assumed)
    that the `numbered` prop's default wasn't broken by the new optional
    prop, since `IdentityCard.tsx` calls it with no `numbered` argument.
  - Zero console/page errors across all three cases.
  - Screenshots saved to `.verification-runs/screenshots/` (gitignored),
    not committed.
- Ad hoc verification script run from
  `scripts/verify-100-signature-bars.mts`, deleted after, not committed —
  confirmed via `git status`.

### Next
- **Emerging/Suppressed signatures** remain deferred — not touched by this
  ticket, still pending for a future Stage 2 addendum toward the unified
  all-tier chart.
- **Stage 3** — CSS cleanup (see prior entry's `.prepared-for-line` flag).

## 2026-08-05 — #100 Stage 2: revert Signature Profile bar chart merge

At explicit request, reverts the prior entry's merge — Primary and
Secondary Signatures on `/identity` go back to two separate `.card` divs,
each numbered, matching pre-`dc1cc16` behavior. Done via `git revert dc1cc16`
(commit `7b50b78`), not a manual re-edit, so the change is exactly the
inverse of what dc1cc16 applied. The changelog entry for dc1cc16 itself is
untouched — changelogs are append-only.

### Changed
- **`app/identity/page.tsx`** — Signature Profile's Primary and Secondary
  groups are back in separate `.card` divs (Primary's card, then Secondary's
  own card when `secondary_signature_analysis` is non-empty). Domain Radar's
  `.card` was never touched by either change.
- **`components/PrimarySignatureBars.tsx`** — the `numbered` prop introduced
  by dc1cc16 is gone, not left behind as an unused default. A clean revert
  of dc1cc16 undoes its full diff, including the prop's introduction — it
  didn't exist before dc1cc16, so there's nothing to leave in place after
  reverting it. `.sig-num-circle` is unconditional again, matching pre-dc1cc16
  behavior exactly. `IdentityCard.tsx` never passed `numbered`, so its
  dashboard rendering is unaffected either way.

### Decisions
- **Leftover prop note corrected against the original ticket's assumption.**
  The ticket that requested this revert assumed `numbered` would remain in
  `PrimarySignatureBars.tsx` as a harmless unused prop (defaulting to `true`,
  still exercised by `IdentityCard`) even after `app/identity/page.tsx`
  stopped passing `numbered={false}`. That assumption doesn't hold under a
  git revert: reverting dc1cc16 undoes the prop's introduction along with
  everything else in that commit, so it's fully gone from the component, not
  left dangling. Confirmed via grep — zero references to `numbered` anywhere
  in `components/` after the revert. Functionally there is no difference
  (`IdentityCard` never passed the prop either way), so nothing is lost; this
  is purely a correction of what the leftover would look like, surfaced to
  and confirmed by Miroslav before writing this entry.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean.
- **Live** via `withVerificationSession`, seeding a real ready
  `identity_report` with 2 primary + 2 secondary signatures. Confirmed via
  real Playwright page loads:
  - `/identity`: `.card` containing "Primary Signatures" and `.card`
    containing "Secondary Signatures" are two distinct DOM nodes (not the
    same card), 4 total `.sig-row`s across them.
  - Primary rows carry `.sig-num-circle` (2 of them, text `"1"`, `"2"`);
    Secondary rows carry `.sig-num-circle-muted` (2 of them, text `"6"`,
    `"7"`, continuing the primary numbering) — both groups numbered again,
    matching pre-merge behavior.
  - `/dashboard`'s `IdentityCard`, expanded: still renders 2 numbered
    `.sig-num-circle`s (text `"1"`, `"2"`) — confirmed unaffected by the
    revert, as expected since it never used the removed prop.
  - Zero console/page errors on either route.
  - Screenshots saved to `.verification-runs/screenshots/` (gitignored), not
    committed.
- Ad hoc verification script run from a temp file in `scripts/`, deleted
  after, not committed — confirmed via `git status`.

### Next
- Nothing new opened by this revert. Stage 3 CSS cleanup and the deferred
  Emerging/Suppressed signatures work (see prior two entries) are unaffected
  and still outstanding.
