## 2026-08-25 — #123: consolidate section dividers into one .section class

Per `docs/briefs/123-section-divider-consolidation-brief.md`. Replaces
the old three-mechanism divider system (`.report-sections`' unconditional
top border, `.report-section + .report-section` adjacency,
`.report-footer`'s own separate border) with one class.

### Scope check (done first, before any code — brief flagged this unconfirmed)

Grepped `.report-section(s)`/`.report-footer`/`.report-scroll`/`.report-cover`
as full-word matches across the whole repo before touching anything:
- `.report-scroll`/`.report-cover` — exactly 4 sites (`/identity`,
  `/path`, `/plan`, `/start`), matching the brief's claim.
- `.report-sections`/`.report-section` — `/identity` (7×), `/plan` (3×),
  `/path` (1×), plus `components/ReframeCtaBlock.tsx` (1×, from the CTA
  headline restructure ticket). **Not in the brief's scope list**:
  `components/QuestionAnswerList.tsx` has its own internal
  `.report-sections` wrapper, shared by `/identity`'s "Your Answers"
  (nested inside `.report-footer` — the literal double-divider
  mechanism) and `/start`'s State 2 (no wrapper at all today).
- `.report-footer` — `/identity` only, nowhere else.
- `/dashboard`, `/account`, `/mentor`, and `/` (home) — confirmed zero
  matches for any of these five classes. Not in scope.
- Collision check: no pre-existing bare `.section`/`.scroll`/`.cover`
  class anywhere. Near-misses confirmed genuinely distinct and left
  untouched: `.hero-section`, `.page-section(-alt)`,
  `.section-title/-body/-cta` (all on `/`, the marketing home page),
  `.scroll-area(--*)`, `.cover-context-line`.

Reported both findings (QuestionAnswerList's own wrapper, and a
Cover-divider design question) back before writing any CSS/JSX, per
Miroslav's explicit instruction.

### Design (confirmed with Miroslav, revised mid-session from the brief's original draft)

Original brief draft used border-top (owned by the *following* section)
with a `.report-cover + .section` adjacency override and an explicit
"no divider above Domain Profile" rule. Miroslav's revision, after
reviewing that draft: flip ownership to border-bottom (owned by the
*preceding* section), Cover becomes `.section.cover` (composed, same
pattern as `.section.documentation`) instead of getting its own
adjacency override — removes the override entirely, and (as a natural
side effect of uniform treatment) keeps Cover's existing divider rather
than removing it.

```css
.section {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding-left: 18px;
  padding-right: 18px;
  padding-bottom: 28px;
  border-bottom: 0.5px solid rgba(0,0,0,0.07);
}
.section:not(:has(~ .section)) {
  border-bottom: none;
  padding-bottom: 0;
}
.cover {
  padding-top: 36px;
  padding-left: 24px;
  padding-right: 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  text-align: center;
}
```

`:not(:has(~ .section))` replaces an initial `:last-child` attempt —
see Verification below for why.

### Changed

- `.report-section` → `.section`, every call site (`/identity` ×7,
  `/plan` ×3, `/path` ×1, `ReframeCtaBlock.tsx` ×1).
- `.report-sections` wrapper removed entirely (CSS rule + JSX `<div>`),
  including its use inside `QuestionAnswerList.tsx` — that component no
  longer wraps itself, each call site supplies its own single
  `.section` instead (`/identity`'s existing "Your Answers" wrapper;
  a new one added on `/start`, which had none before).
- `.report-footer` retired → `.section` (`/identity`'s "About This
  Report" keeps its `documentation` combo as `.section documentation`;
  "Your Answers" is now one mechanism instead of two).
- `.report-scroll` → `.scroll`, `.report-cover` → `.section.cover`,
  across all 4 call sites.
- Section horizontal padding (18px) placed on `.section` itself, not
  `.scroll` — confirmed via live computed-style checks to exactly
  match the old values with zero doubling onto Cover (24px, unaffected)
  or `LimitsBlock` (its own unrelated 20px, unaffected).

`npx tsc --noEmit` — clean throughout (CSS/JSX-only change).

### Verification — live, twice, all 4 pages, computed style + screenshots

First pass (`:last-child`): `/identity`, `/path`, `/start` all correct
— both motivating bugs confirmed fixed (no double divider on "Your
Answers," a real divider now above the CTA section). **`/plan` failed**:
`LimitsBlock` ("Now Let's Implement It") renders as a trailing
non-`.section` sibling after "Start Here," so `:last-child` never
matched any real `.section` — "Start Here" incorrectly kept its
`border-bottom`, visible in a full-page screenshot as a stray divider
line above `LimitsBlock`. Reported back with 3 options rather than
silently patching; Miroslav chose swapping to
`.section:not(:has(~ .section))`.

Re-verify after the swap initially looked like it hadn't worked —
`/plan`'s computed style was byte-identical to the broken first pass.
Before concluding the selector was wrong, checked the compiled CSS
chunk's mtime against the source edit's mtime
(`feedback_stale_dev_server_stale_build`, a documented lesson from this
project's history): the chunk was compiled 9 minutes *before* the
`:has()` edit — stale Turbopack cache, not a real bug. Killed the
process, cleared `.next`, restarted clean, reran. Confirmed: `/plan`'s
"Start Here" now correctly reads `border-bottom-width: 0px`, the stray
divider is gone in the full-page screenshot, and `/identity`/`/path`/`/start`
are byte-for-byte identical to the pre-swap run (selector change didn't
alter their behavior, as required).

### Decisions

- Design revised from border-top+adjacency-override to
  border-bottom+`:not(:has(~ .section))` mid-session, per Miroslav —
  see Design above.
- `QuestionAnswerList.tsx`'s own wrapper stripped rather than kept as
  a nested `.section`, since nesting `.section` inside `.section` would
  have reintroduced bug #1's exact double-divider mechanism in new
  clothes.
- Didn't pre-emptively deviate from the literal `:last-child` spec when
  the `/plan` risk was first reasoned through structurally — implemented
  it exactly as given, then let the live screenshot/computed-style check
  surface the real regression before proposing a fix, per this session's
  established practice of confirming predictions with evidence rather
  than skipping straight to a workaround.

### Files

- `app/globals.css`
- `app/identity/page.tsx`, `app/path/page.tsx`, `app/plan/page.tsx`, `app/start/page.tsx`
- `components/QuestionAnswerList.tsx`, `components/ReframeCtaBlock.tsx`
- Ad hoc verification scripts and a locally-run dev server (`.next`
  cleared once, per the stale-cache finding above) — cleaned up /
  stopped after use.

### Next

Nothing outstanding — all 4 affected pages live-verified against the
final selector, both motivating bugs confirmed fixed, no regressions
found on re-check.

## 2026-08-25 — CTA headline restructure: Miroslav's live confirmation

Same-day follow-up to the CTA headline restructure entry below
(`a050f77`), which explicitly shipped unverified pending Miroslav's own
check on the deployed dev URL. He has now confirmed it directly
("all good verified") — recorded here as the actual verification step
for that change, satisfying it in full. No code changed in this entry
— reference only.

Both Definition of Done conditions for
`docs/briefs/cta-headline-restructure-brief.md` now hold: its
implementation was committed this session (`a050f77`) and live-verified
this session (Miroslav's confirmation just now) — deleting it in this
same session-end pass, per AGENTS.md.

## 2026-08-25 — CTA headline restructure: moved outside the card, centered

Per `docs/briefs/cta-headline-restructure-brief.md`. Restructures
`components/ReframeCtaBlock.tsx` only, plus the one-line unwrap this
forces on `app/identity/page.tsx` — no content/copy changes beyond the
headline text itself.

### Changed

`components/ReframeCtaBlock.tsx`:
- The component's return is now `<div className="report-section">`
  (previously callers supplied this wrapper) containing a new
  `<div className="cta-header">` (eyebrow + `<h2>`, both bare — `eyebrow`
  and no class on `h2`, not `limits-eyebrow`/`limits-heading`, since
  they now sit on the page's light background, not the dark card) above
  the existing `<div className="cta-block">` (recap, pullquote,
  forward_frame, bullets, price, button — content unchanged, headline
  removed since it now lives above).
- Headline copy changed: "Find out who you should become next." → "Find
  Out Where It's Pointing You" (brief-specified, not a copy decision
  made here).

`app/globals.css` — new `.cta-header` rule: `display: flex;
flex-direction: column; align-items: center; text-align: center; gap:
12px`. Gap matched to `.report-cover`'s own value (grepped first, real
pattern: `padding: 36px 24px 28px; display: flex; flex-direction:
column; align-items: center; gap: 12px; text-align: center`) rather
than picked arbitrarily — both are a short eyebrow-led centered block
on this same page.

`app/identity/page.tsx` — removed the outer `<div
className="report-section">` that used to wrap `<ReframeCtaBlock>`,
since the component now supplies its own. `app/path/page.tsx`
unchanged — it never had a wrapper around `<ReframeCtaBlock>` in the
unpaid branch, confirmed by grep before touching anything, matching the
brief's claim exactly.

`npx tsc --noEmit` — clean.

### Verification

**Not live-verified against a running dev server this pass.** The
verification-script write was interrupted and Miroslav instructed
skipping it — he is checking directly on the deployed dev URL himself
afterward, same pattern as the Secondary Signatures fix earlier this
session. His confirmation, once reported back, is the actual
verification step for this change, not anything checked locally here.
Confirmed only: `npx tsc --noEmit` clean, and the diff matches the
brief's specified JSX/CSS exactly (see Changed above) — code review and
type-checking, not a substitute for a live check, per this repo's own
Definition of Done.

### Decisions

- `NEXT_PUBLIC_OPEN_ACCESS` in `.env.local` is now **`false`**, staying
  that way going forward — a deliberate departure from this session's
  earlier flip-to-false-then-restore-to-true pattern (used for the
  reframe-teaser-redesign ticket's live verification). Per Miroslav:
  Vercel's Preview environment now also has this set to `false`, so
  leaving local dev at `true` would make local and deployed dev
  disagree on which code path renders. Local dev now matches deployed
  dev; anyone testing `/path`'s unpaid state locally no longer needs to
  flip this themselves first.
- Bare `.eyebrow`/`h2` (page-default styling) used for the new
  `.cta-header`, not `.limits-eyebrow`/`.limits-heading` (the dark-card
  variants the headline used before) — the brief's own JSX example
  specified this, and it's the correct call now that the headline sits
  on the light page background instead of inside `.cta-block`.

### Files

- `components/ReframeCtaBlock.tsx`
- `app/globals.css`
- `app/identity/page.tsx`

### Next

Awaiting Miroslav's direct confirmation on the deployed dev URL
(`/identity` and `/path` unpaid, cold) before this is treated as fully
verified — not yet reported back as of this entry.

## 2026-08-25 — /identity: Secondary Signatures muted styling + /25 meta fix

Two small, same-file fixes on `app/identity/page.tsx`, requested
directly (no brief file) after a git-history investigation into how
Secondary Signatures was styled before #121's merged Signature Profile
chart.

### Changed

`app/identity/page.tsx`:
- Signature Profile's merged chart (`SIGNATURE PROFILE` section):
  secondary bars previously rendered via `PrimarySignatureBars` reused
  as-is, unmuted — identical to primary. Replaced with inline `sig-row`
  markup using `sig-num-circle-muted` / `sig-bar-fill-muted` /
  `sig-score-label-muted` — CSS that was already live in
  `app/globals.css`, confirmed unused since #121 via `git show
  dc1cc16`/`b2bf5f8` history checks earlier this session. Badge
  numbering kept as `i + 6`, continuing from Primary — matches #121's
  current behavior, not `dc1cc16`'s own numberless version (that commit
  removed numbering entirely; not restored here). Primary bars
  unchanged (`PrimarySignatureBars`, unmuted).
- Secondary Signatures deep-dive cards: `meta` string was
  `` `${sig.domain} · ${sig.score}` ``, missing the `/25` Primary's
  equivalent card already has. Now `` `${sig.domain} · ${sig.score}/25` ``,
  exact match.

`npx tsc --noEmit` — clean.

### Verification

Confirmed via direct git history inspection before implementing — real
diffs from `dc1cc16` (2026-08-05 merge attempt) and its parent-for-this-file
`b2bf5f8`, plus `git diff b2bf5f8 dc1cc16 -- app/globals.css` (empty,
confirming the muted CSS values are unchanged from back then) and a
grep confirming those same classes are still present, byte-identical,
in current `app/globals.css`. Reported to Miroslav before writing any
code, per this session's established practice of not trusting cited
line numbers/history without checking them directly (the citing brief-
style instruction used line numbers from `context-bundle.md`, not the
real file — caught and corrected before editing).

**Not live-verified against a running dev server this pass** — the
verification-script write was interrupted and Miroslav instructed
committing directly instead ("commit and push to dev"), skipping the
DOM/screenshot check every other change this session went through.
Flagged explicitly in the commit message (`a142b09`) rather than
implied as tested. Miroslav confirmed separately ("all good verified")
after checking it himself, post-commit — logged here as his
verification, not mine.

### Decisions

- Restored #121-predecessor muted CSS/markup rather than writing a new
  muted variant from scratch, since the exact classes already existed
  live and unused — confirmed identical via git diff before reusing.
- Kept #121's `i + 6` numbered-badge behavior over `dc1cc16`'s
  numberless version, per Miroslav's explicit instruction — the two
  historical versions disagreed on this and the newer (#121) behavior
  was the one to keep.
- Skipped live-verification on Miroslav's direct instruction to commit
  immediately; per this ticket's own precedent (#125's Stripe gap),
  gaps like this get logged explicitly rather than silently assumed
  covered — done here via the commit message and this entry, with
  Miroslav's own later confirmation recorded as the actual verification
  step.

### Files

- `app/identity/page.tsx`

### Next

Nothing outstanding — Miroslav confirmed the result directly.

## 2026-08-25 — #125 assigned: checkout flow verification follow-up

Same-day follow-up to the reframe teaser redesign entry below. The
deferred checkout verification it names (confirm the CTA on both
`/identity` and `/path` gets back a real Stripe session URL and
redirects, once `STRIPE_SECRET_KEY` is available locally) has a real
ticket number: **#125**. No code changed in this entry — reference
only, added for the same reason and in the same format as #126's
entry above. Referencing #125 wherever this verification gap is
mentioned going forward, per Miroslav's instruction.

## 2026-08-25 — #126 assigned: identity_reframe physical cleanup follow-up

Same-day follow-up to the reframe teaser redesign entry below. The
deferred physical cleanup it names (delete
`app/api/generate-identity-reframe/route.ts`,
`lib/prompts/identity-reframe.ts`, and the `identity_reframe`
artifact-type migration/constraint, once #125's checkout verification
lands) now has a real ticket number: **#126**. No code changed in this
entry — reference only, so the earlier entry's "not yet created" note
doesn't go stale. Referencing #126 wherever this cleanup is mentioned
going forward, per Miroslav's instruction.

## 2026-08-25 — Reframe teaser redesign: /identity CTA section + retire identity_reframe

Per `docs/briefs/reframe-teaser-redesign-brief.md` (supersedes #116,
folds in #114, retires #98/#113's `identity_reframe` architecture).
Schema + prompt change (`ReframeTeaser`: drop `why_bullets`, add
`forward_frame`), a new shared component, and both `/identity` and
`/path` rewired to render it identically. `lib/prompts/path-options.ts`
and physical deletion of `identity_reframe` code/migration left alone,
exactly as scoped out.

### Changed

`lib/artifact-schemas.ts` — `ReframeTeaser`: dropped `why_bullets:
string[]`, added `forward_frame: string`. No nesting change, no backfill
(same precedent as `domain_profile_summary`).

`lib/prompts/identity-report.ts` (`LAYER_2_PROMPT`) — removed the
`why_bullets` instruction block and its mentions in the pre-return
checklist and structural constraints; added a `forward_frame` block:
question-framed register, sourced primarily from Q10–13 (avoidance,
what still energises despite frustration, last-felt-confident moment,
wanted change), hard rules against quoting/restating raw answers and
against naming a destination/role/outcome, a required stakes beat +
Path Options value-bridge, minimum 55/target 70 words. `lib/layer2-minimal-pilot.ts`
and `lib/call2-pilot.ts` both carry their own copies of this instruction
block but are confirmed standalone pilot files (their own doc comments
say so — not wired into `generateIdentityReport` or any route) — left
untouched.

`lib/generate-identity-report.ts` — `logReframeTeaserWordCountGaps`:
dropped the `why_bullets` loop and `REFRAME_TEASER_BULLET_MIN_WORDS`,
added a `forward_frame` check against new
`REFRAME_TEASER_FORWARD_FRAME_MIN_WORDS = 55`. Same log-only pattern,
no behavior change otherwise.

New `components/ReframeCtaBlock.tsx` — the shared 6-part composition
(headline, recap, pullquote, forward_frame, static "what you get"
bullets, price + CTA), used identically by both pages instead of
copy-pasted JSX, so the two pages' output is identical by construction.
Bullet 1 interpolates a deterministic signature-name callback (e.g.
"your Pioneer/Activator constellation") from `primary_constellation[0]`/`[1]`
rather than relying on the LLM to get it right.

`app/identity/page.tsx` — local `reframe_teaser` type now imports
`ReframeTeaser` from `lib/artifact-schemas` instead of a hand-inlined
shape; added `checkoutLoading` state + `handleCheckout` (same shape as
`/path`'s existing one); `.cta-block` JSX replaced with
`<ReframeCtaBlock>`, guard updated to check `forward_frame` instead of
`Array.isArray(why_bullets)`.

`app/path/page.tsx` — removed entirely: `reframeArtifactId`,
`reframeStartFailed`, `reframeGenPhase`, `reframeContent`,
`loadOrStartReframe`, `handleRetryReframe`, the
`IdentityReframeArtifactContent` import, and all four
idle/spinner/come-back-later/failed polling render branches. The
existing `identity_report` fetch in `init()` now also captures
`reframe_teaser` and `primary_constellation` into state (no second
fetch). Unpaid branch renders the same `<ReframeCtaBlock>` as
`/identity`. Also removed the dead `namedIdentity` state (confirmed via
grep it was read nowhere once the old headline JSX was gone) and the
`NEXT_PUBLIC_PRICE_DISPLAY` conditional price pill — confirmed via git
history (`git log -S`) it was untouched, undocumented scaffolding from
the original Jun 1 Phase-1 commit, never set in any env this repo has
visibility into, before dropping it in favor of the brief's static "$49,
one-time." text.

`app/globals.css` — removed `.reframe-bullets` (list/li/::before rules),
now-orphaned since the `why_bullets` render it supported is gone;
confirmed via full-repo grep it had no other call site (same check
#115 used before deleting `.chip-friction`/`.chip-energiser`).
`.reframe-pullquote` kept as-is, still used.

`npx tsc --noEmit` — clean throughout.

### Verification

**Prompt calibration** — real, non-persisted `generateIdentityReport()`
runs against Leona's and Matteo's actual `discovery_answers` (read-only
profile/answers lookup, throwaway `identity_report` artifact row
deleted in a `finally` after reading `reframe_teaser.forward_frame`,
never their live artifact). First pass caught a real bug: including the
Amara worked example (even labeled "calibration reference only — do not
copy") caused the model to reproduce it byte-for-byte for Leona — the
exact failure mode this same prompt file already documents for
`reframe`'s and `why_bullets`'s predecessor drafts. Fixed by stripping
the literal example entirely, matching the file's own established "no
example given on purpose" convention. Reran: both personas produced
fresh, evidence-grounded content (73/78 words), no quoting/restating of
raw answers, no named destination/role/outcome. Added a light
anti-formula instruction after noticing both outputs converged on an
identical 4-sentence shape (temperature 0) — reran once more (65/67
words) to confirm no regression; the macro shape stayed similar (an
expected temp-0 effect, not one of the brief's two hard gates) but
content stayed fresh and compliant. Floor 55 / target 70 shipped,
sitting with margin below the observed 65–78-word real range (plus
Amara's ~70-word reference).

**Live UI verification** — found and worked around an environment
blocker first: `.env.local`'s `NEXT_PUBLIC_OPEN_ACCESS=true` makes
`/path` skip its entitlement-check branch entirely, so the new unpaid
render was unreachable as configured. Stopped the dev server, flipped
it to `false`, restarted (twice — a first restart hit a port conflict
from a Turbopack child process `TaskStop` had orphaned; confirmed via
`Get-Process`/start-time before killing it, not assumed), ran
verification via `scripts/run-verification.mts` against a fresh dev
server, then restored `.env.local` to `true` and stopped the server
again (same orphaned-child issue recurred and was resolved the same
way) — environment back to its original state.

Confirmed via direct DOM/text checks (not diff-reasoning alone):
- `/path` unpaid, cold (no prior `/identity` visit): `ReframeCtaBlock`
  renders directly — headline, recap (contrast fix visually confirmed
  via screenshot), pullquote, forward_frame, "Pioneer/Activator"
  constellation callback, "$49, one-time.", and the CTA button all
  present; zero `/api/generate-identity-reframe` requests fired; no
  entitlement row exists for the test user (confirms this really is the
  unpaid path, not open-access bypass).
- `/identity`: identical content via the same component.
- Paid regression (real `entitlements` row, `source: 'manual'`): real
  Path Options render present, `.cta-block` absent, zero
  `/api/generate-identity-reframe` requests.
- Checkout CTA on both pages: confirmed it correctly POSTs
  `/api/checkout` with the real `user_id` and attempts the redirect —
  but the response was a 500 ("Neither apiKey nor config.authenticator
  provided", `lib/stripe.ts:7`), because `STRIPE_SECRET_KEY` isn't set
  anywhere in `.env.local`. Confirmed this predates this ticket (`app/api/checkout/route.ts`
  and `lib/stripe.ts` are untouched by this diff) — not a regression,
  but the "real Stripe session created" assertion could not actually
  run. Per Miroslav's explicit call, **not** silently accepted: handed
  to a new, named follow-up — **#125, "Verify checkout flow end-to-end
  once Stripe test keys are available locally."**

Confirmed via `git status`/`git diff` before commit, at Miroslav's
explicit request: `lib/prompts/path-options.ts` untouched (empty diff);
`app/api/generate-identity-reframe/route.ts`,
`lib/prompts/identity-reframe.ts`, and
`supabase/migrations/20260803000000_identity_reframe_artifact.sql` all
untouched and still present on disk; a full `git diff | grep
identity_reframe` swept the whole diff and found only the intended
removed references inside `app/path/page.tsx`.

### Decisions

- Used the brief's proposed headline copy verbatim ("NOW THAT YOU KNOW
  WHO YOU ARE" / "Find out who you should become next.") — confirmed
  with Miroslav before implementation, per the brief's explicit
  "confirm before implementation" flag.
- Sourced the 2 calibration personas beyond Amara from Leona and
  Matteo — confirmed with Miroslav first. Two profiles matched
  `Matteo%` (plain "Matteo" vs. "Matteo_Rinaldi"); pinned to the exact
  `user_id` for plain "Matteo" (created 2026-08-11, matching the
  "Matteo reproducibility reruns" changelog date) rather than guessing.
- Built `ReframeCtaBlock` as a shared component rather than duplicating
  JSX across both pages — guarantees the brief's "byte-for-byte
  identical" requirement by construction, not by convention.
- Interpolated the signature-name callback deterministically into the
  static bullet text (code-side) rather than asking the LLM to name it
  correctly inside `forward_frame`.
- Physical cleanup of `identity_reframe` code/migration and the
  `path-options.ts` prompt's stale `identity_reframe` context input:
  both explicitly left alone per the brief's out-of-scope section,
  confirmed untouched via git before commit (see Verification above).

### Files

- `lib/artifact-schemas.ts`
- `lib/prompts/identity-report.ts`
- `lib/generate-identity-report.ts`
- `components/ReframeCtaBlock.tsx` (new)
- `app/identity/page.tsx`
- `app/path/page.tsx`
- `app/globals.css`
- Ad hoc calibration/verification/debug scripts and a locally-run dev
  server (with a temporarily-flipped-then-restored `.env.local`) — all
  cleaned up / stopped after use.

### Next

- **#125** (new, named this session): verify the checkout flow
  end-to-end (real Stripe session + redirect, both pages) once Stripe
  test keys are available locally.
- Separate future cleanup ticket (not yet created): physical deletion
  of `app/api/generate-identity-reframe/route.ts`,
  `lib/prompts/identity-reframe.ts`, and the `identity_reframe`
  artifact-type migration/constraint, per the brief's explicit
  deferral — do not do this until #125 lands and this ticket's
  checkout path has been fully live-verified.
- `lib/prompts/path-options.ts` still takes `identity_reframe` as
  context input, now stale — deferred to #25 (Path Options quality,
  Paused) per the brief, unchanged this session.
