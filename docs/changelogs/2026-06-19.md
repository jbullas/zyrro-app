# 2026-06-19

## Changed

- **Fixed generation stuck on "waiting" (serverless fire-and-forget bug).**
  - `app/api/generate-report/route.ts`: added `import { after }` from `next/server`, added `export const maxDuration = 60`, replaced `void runGeneration(...)` with `after(() => runGeneration(...))`.
  - `app/api/generate-path-options/route.ts`: same three changes applied identically.
  - `lib/generate-path-plan.ts`: added `import { after }` from `next/server`, replaced `void runPlanGeneration(...)` with `after(() => runPlanGeneration(...))`.
  - `app/api/select-path/route.ts`: added `export const maxDuration = 60` at module scope (fire-and-forget lives in the shared helper, not the route itself).
- Build confirmed clean (`next build`, no TS errors) after all changes.

## Decisions

- `after` + `maxDuration = 60`: verified against `node_modules/next/dist/docs/` before writing any code — import source and export name both confirmed correct for Next 16.2.1.
- `maxDuration = 60` placed in route files only, not in `lib/generate-path-plan.ts` — route-segment config is a no-op in non-route modules; it must live at the route level to be picked up by Vercel.
- `select-path` split: `after()` fix went into `generate-path-plan.ts` (where the un-awaited call lives); `maxDuration` went into the route (the only place it takes effect). Kept shared helper untouched beyond the one-line `after()` swap.

## Next

- Deploy to `dev` and run end-to-end acceptance: questionnaire → `identity_report` flips to `status='ready'` with non-empty content, waiting screen resolves. Same check for path options on `/path` and path plan on `/plan`.
- If any run still hangs / 504s near the 60s mark: stop and assess — generation may exceed the Hobby-plan ceiling and need a separate timing/plan decision (out of scope for this brief).
- Brief B (billing) and conversation-resume work are queued after acceptance confirmed.
