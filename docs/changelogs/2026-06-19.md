# 2026-06-19

## Changed

- **Fixed generation stuck on "waiting" (serverless fire-and-forget bug).**
  - `app/api/generate-report/route.ts`: added `import { after }` from `next/server`, added `export const maxDuration = 60`, replaced `void runGeneration(...)` with `after(() => runGeneration(...))`.
  - `app/api/generate-path-options/route.ts`: same three changes applied identically.
  - `lib/generate-path-plan.ts`: added `import { after }` from `next/server`, replaced `void runPlanGeneration(...)` with `after(() => runPlanGeneration(...))`.
  - `app/api/select-path/route.ts`: added `export const maxDuration = 60` at module scope (fire-and-forget lives in the shared helper, not the route itself).
- Build confirmed clean (`next build`, no TS errors) after all changes.

## Decisions

- `after` + `maxDuration = 60`: verified against `node_modules/next/dist/docs/` before writing any code — import source and export name both confirmed correct for Next 16.2.1.
- `maxDuration = 60` placed in route files only, not in `lib/generate-path-plan.ts` — route-segment config is a no-op in non-route modules; it must live at the route level to be picked up by Vercel.
- `select-path` split: `after()` fix went into `generate-path-plan.ts` (where the un-awaited call lives); `maxDuration` went into the route (the only place it takes effect). Kept shared helper untouched beyond the one-line `after()` swap.

## Next

- Deploy to `dev` and run end-to-end acceptance: questionnaire → `identity_report` flips to `status='ready'` with non-empty content, waiting screen resolves. Same check for path options on `/path` and path plan on `/plan`.
- If any run still hangs / 504s near the 60s mark: stop and assess — generation may exceed the Hobby-plan ceiling and need a separate timing/plan decision (out of scope for this brief).
- Brief B (billing) and conversation-resume work are queued after acceptance confirmed.

---

## Changed (second block)

- **Test-mode open-access switch (`NEXT_PUBLIC_OPEN_ACCESS`).**
  - `lib/entitlements.ts`: short-circuit added at the top of `hasEntitlement` — if `NEXT_PUBLIC_OPEN_ACCESS === 'true'`, returns `true` immediately before any DB query. Covers all API routes automatically (`generate-path-options`, `select-path`, `mentor` all call through this helper).
  - `app/mentor/page.tsx`: short-circuit at top of `checkSubscription()` — sets `isSubscribed=true` and skips the DB query.
  - `app/plan/page.tsx`: wraps the entitlement DB query + `setPageState('unpaid')` block in `if (NEXT_PUBLIC_OPEN_ACCESS !== 'true')`.
  - `app/path/page.tsx`: same wrap for the larger block (entitlement check + identity artifact fetch for paywall personalisation). Confirmed correct: `namedIdentity` is only used in the `'unpaid'` render branch; it is not displayed in the `'ready'` (path options) view, so not fetching it under open-access is intentional.
- Build confirmed clean after all changes.

## Decisions

- Used `NEXT_PUBLIC_OPEN_ACCESS` (not `OPEN_ACCESS` as written in the brief) — the three page files are `'use client'` components and cannot read private server-only env vars. A single `NEXT_PUBLIC_` flag works on both server and client. **Jeff: set `NEXT_PUBLIC_OPEN_ACCESS=true` in Vercel Preview environment and in `.env.local`. Leave unset on Production.**
- Identity artifact fetch on `/path` stays inside the entitlement block — it was already only fetched for unpaid users (to personalise the paywall headline). Open-access users skip the unpaid state entirely, so not fetching it is correct.

## Next

- Jeff to set `NEXT_PUBLIC_OPEN_ACCESS=true` in Vercel Preview and `.env.local`, then test full funnel (questionnaire → report → `/path` → select → `/plan` → `/mentor`) with a fresh account that has no entitlement rows.
- After open-access acceptance confirmed: Brief B (billing / subscription checkout for Mentor) and conversation-resume work.
