## 2026-07-27 — #81 render `scoring_explanation` in Signature Profile

### Changed
- **`app/identity/page.tsx`**: added `signature_profile_summary?: {
  scoring_explanation: string }` to the `IdentityReport` interface,
  destructured it from `report`, and render it as a plain `<p>` directly
  under the "SIGNATURE PROFILE" eyebrow in Section 3, above the
  `PrimarySignatureBars` card — matching Section 2's "What This Report Is"
  typographic convention (plain paragraph, no new component). Field is
  optional and only rendered when present, so an artifact missing it
  degrades to no paragraph rather than a crash. No changes to
  `PrimarySignatureBars`, `DomainRadarChart`, the secondary-signature list
  markup, D5, the scoring model, or any prompt file — per the brief's stop
  conditions.

### Decisions
- **Made `signature_profile_summary` optional in the `IdentityReport`
  interface rather than required.** The brief's own verification step asked
  for a safe-fallback check against an artifact missing the field; every
  live `ready` row currently has it, but nothing guarantees that forever, so
  the type reflects the real contract rather than assuming presence.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean.
- Queried all 13 live `ready` `identity_report` artifacts directly —
  every one already has `scoring_explanation` populated (43–53 words each).
  No backfill needed; the brief's stop condition (missing field → report
  back instead of writing a backfill) never triggered.
- Live-verified with an ad hoc script built on `scripts/run-verification.mts`
  (run and deleted, not committed), dev server running locally:
  - **Real-artifact render**: bootstrapped a synthetic test user, seeded an
    `identity_report` artifact using the actual content of the most recent
    real `ready` row (copied via the service-role key, not fabricated),
    loaded `/identity`. Confirmed the exact `scoring_explanation` text
    appears inside `#section-3`, confirmed via DOM child-order check that
    the paragraph precedes the bars card, and confirmed the bars/secondary
    list/radar cards still render unchanged (3 cards present). Screenshot of
    the rendered section taken for visual confirmation.
  - **Missing-field regression**: same flow with `signature_profile_summary`
    deleted from the seeded content entirely. No page error thrown, no
    stray explanation text rendered, existing cards unaffected.
  - Both test users torn down via `teardownTestUser`; confirmed no orphaned
    `zyrro-verify-*` auth users or rows afterward.

### Next
- Nothing outstanding from this ticket.

## 2026-07-27 — session 2: #81 correction — make scoring explanation static

Follow-up/correction to this same day's earlier #81 entry above (commit
`ced0fdb`). Scoped per `docs/briefs/58-static-scoring-explanation-brief.md`,
whose own header states this explicitly: **"Ticket: #81 ... Not part of
#58."** #58's board description is unrelated and untouched by this work.
The brief's filename carries the "58" prefix despite being #81's ticket —
not renamed here since it wasn't part of this session's instructions, but
flagging the mismatch for anyone who goes looking for it later.

### Changed
- **`app/identity/page.tsx`**: added a static `SCORING_EXPLANATION` constant
  next to the existing `WHAT_THIS_REPORT_IS` constant (same pattern — a
  fixed, generic explanation of the Frequency × Intensity model and the
  Primary/Secondary distinction, rewritten from the substance of what the
  LLM used to generate). Section 3 now renders this constant unconditionally
  in the same position #81 established, instead of the per-artifact
  generated field. Removed the now-unused `signature_profile_summary`
  optional field and its destructure, added by #81 solely to plumb
  `scoring_explanation` through.
- **`lib/prompts/identity-report.ts`**: removed `scoring_explanation` from
  the Layer 2 output JSON template and deleted its
  `### signature_profile_summary.scoring_explanation` field-requirement
  block. Nothing else in the prompt touched — `what_this_report_is` and
  `domain_profile` remain #58's separate, untouched territory.
- **`lib/artifact-schemas.ts`**: removed `scoring_explanation` from
  `SignatureProfileSummary` entirely (not left optional) — Postgres `jsonb`
  doesn't enforce the TypeScript shape, so old stored rows keep whatever
  text they were generated with, inert but harmless, with no `schema_version`
  bump needed.
- No backfill script, no schema_version bump, no touching `domain_profile`
  or `what_this_report_is` — per the brief's explicit stop conditions.

### Decisions
- **Removed `scoring_explanation` outright from the schema rather than
  making it optional.** Confirmed via grep it's a plain TypeScript interface
  with no runtime (Zod-style) validator anywhere in the codebase, and the
  only two consumers (the schema file itself and `app/identity/page.tsx`)
  were both being edited in this same pass — no reason to carry dead
  optionality forward.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean.
- **Controlled A/B on the prompt edit** (stop condition: "if removing
  scoring_explanation's instructions changes any other field's output,
  stop and report back"): ran Layer 2 generation 6 times (3 original prompt,
  3 edited prompt) against the *same* fixed Detection-Engine analysis output,
  via an ad hoc script built directly on the real `lib/llm.ts` /
  `lib/prompts/identity-report.ts` / `lib/generate-identity-report.ts` code
  (run and deleted, not committed). All 3 original trials: `scoring_explanation`
  present, `primary_signatures` = 5, `secondary_signatures` = 3. All 3 edited
  trials: `scoring_explanation` absent, `primary_signatures` = 5,
  `secondary_signatures` = 3. No other field's output changed — stop
  condition did not trigger.
- **Two real end-to-end generations** through the actual production path
  (`/auth/callback` → `kickoffIdentityGeneration` → `completeDiscovery` →
  `after(() => generateIdentityReport(...))`), not just direct prompt calls:
  bootstrapped synthetic test users with real discovery-answer metadata via
  `scripts/run-verification.mts`, polled the live `artifacts` row to
  `ready`. Both confirmed `scoring_explanation` absent from the real stored
  LLM output, `what_this_report_is` and `domain_profile` still generated
  and untouched, and the static text rendering correctly in `#section-3` in
  a real authenticated browser session (same position #81 established,
  bars/secondary/radar cards unaffected).
- **Investigated an anomaly before treating this as verified**: the first
  two real end-to-end attempts (on a freshly started dev server, edited
  prompt) got stuck at `status: 'generating'` past the route's 240s budget
  and never resolved. Rather than wave this off, isolated it: reverted to
  the original prompt and reran through the identical real route — resolved
  cleanly in 48s. Restored the edited prompt and reran — also resolved
  cleanly (54s). Killed the server, cleared `.next`, and reran the edited
  prompt as the literal first request on a truly fresh process — resolved
  cleanly in 49s, disproving a cold-start-specific theory too. Conclusion:
  the two hangs were a one-off flake specific to that first dev-server
  process of the session, uncorrelated with prompt content — not caused by
  this change. Separately noticed `signature_profile_summary.secondary_signatures`
  came back with 2 entries instead of 3 in one uncontrolled full-pipeline
  run; confirmed via a second full-pipeline trial that the **unedited**
  original prompt shows the identical deviation, so this is pre-existing
  Detection-Engine variance upstream of anything touched here, not a
  regression. (This sub-array isn't rendered anywhere in the UI regardless
  — the page's "Secondary Signatures" list reads `secondary_signature_analysis`,
  a different, unaffected array, which had all 3 entries in every run.)
- No orphaned `zyrro-verify-*` auth users or rows after any run (checked
  directly against the live `auth.users` table, not assumed from
  `teardownTestUser` not throwing).

### Next
- Nothing outstanding from this ticket. The one-off `generating`-hang
  anomaly investigated above did not reproduce under matched conditions and
  isn't attributable to this change, so it isn't being opened as a new
  ticket — flagging here only so it's on record if it's ever seen again.
- **`signature_profile_summary.secondary_signatures` occasionally returns 2
  entries instead of 3** — confirmed pre-existing Detection-Engine variance
  (reproduced on the unedited original prompt too, see Verification above),
  not caused by this change, and has no UI impact today since the page
  renders `secondary_signature_analysis` instead, which was consistently
  correct in every run. Not opening a ticket for it now, but worth one if
  it's ever seen to affect a field that actually renders, or if the
  deviation rate turns out to be more than occasional.
