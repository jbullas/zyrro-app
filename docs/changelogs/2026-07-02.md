## 2026-07-02 — Framework Reference list entry, conversation bundles (core mechanism)

### Changed

- Added `zyrro_continuity_memory_blueprint_v_1.md` to the Framework Reference list in `docs/standards/product-decisions.md`, placed in true alphabetical order (after `zyrro_detection_engine_spec_v_1.md`, before `zyrro_identity_report_blueprint_v_1.md`). Commit `d554210`.
- Executed `docs/briefs/conversation-bundles.md`. Commit `8f4556d`:
  - `supabase/migrations/20260702000000_messages_baseline.sql` — baseline migration for `public.messages`, which was created directly in the Supabase dashboard and had no tracked migration until now.
  - `supabase/migrations/20260702000001_conversation_bundles.sql` — adds `conversations.last_message_at` / `summary` / `summary_generated_at`, plus an `AFTER INSERT` trigger on `messages` that keeps `last_message_at` current.
  - `lib/prompts/conversation-bundle.ts` — summarization prompt (plain text output).
  - `lib/conversation-bundle.ts` — `resolveConversationBundle(conversationId, userId)`: staleness check, synchronous summarization (no polling), error handling on both the messages fetch and the summary write (returns `null` rather than a silent partial success on either failure).
  - `app/api/end-conversation/route.ts` — session-derived user only; verifies conversation ownership, sets `status = 'completed'`, returns the resolved bundle.

### Decisions

- Before writing the `messages` baseline migration, verified the live schema against Supabase directly rather than inferring constraints from the `conversations_baseline.sql` pattern — the user asked for it and rejected the first "mirror the pattern" proposal. Confirmed via PostgREST OpenAPI introspection (columns/types/PK/FK) plus read-only `pg_catalog` queries run by the user in the Supabase SQL Editor (RLS policies, FK `ON DELETE CASCADE`, indexes, `role` CHECK constraint). One query came back looking contradictory (two `messages_pkey` definitions) because it wasn't schema-qualified and matched Supabase's built-in `realtime.messages` table too — resolved by cross-checking against the index list, which was schema-qualified and unambiguous.
- Did not touch `app/mentor/page.tsx` or the `/mentor` UI (ticket #46) and did not build the meta-bundle (ticket #45) — both explicitly out of scope per the brief's stop conditions.

### Next

- Ticket #46 — wire `/mentor` UI to call `end-conversation` and surface the bundle.
- Ticket #45 — meta-bundle (aggregating bundles across conversations).
- Everything else carried over from `2026-07-01.md` remains untouched (path-options/select-path hardening, verify-checkout hardening, Supabase Pro upgrade, Resend email, memory table ownership, Stripe subscription billing, `dev` → `main` deferred until launch).

## 2026-07-02 — session 2: blueprint commit, LLM call-site rule, planning-surface bundle script

### Changed

- Committed `zyrro_continuity_memory_blueprint_v_1.md`, which existed untracked on disk from an earlier session — content untouched, just added and committed. Commit `81b99e0`.
- Per `docs/briefs/document-llm-call-rule.md`, added an "LLM calls" section to `docs/standards/coding-standards.md`: all provider calls must go through `lib/llm.ts` (doesn't exist yet — takes effect once ticket #47 lands it), model name from env (`OPENAI_MODEL`, default `gpt-4o`), never hardcoded. Commit `cfcea20`.
- Added `scripts/make-bundle.mjs` (new, no prior history) — assembles `context-bundle.md` from every tracked text file (`git ls-files`, filtered by extension, lockfile excluded) for the claude.ai planning surface, which has no repo access otherwise. Added `/context-bundle.md` to `.gitignore` — the script's own header comment called for this but it had never actually been added until now.
- Ticket #47 opened: consolidate the six direct-OpenAI-client call sites into `lib/llm.ts`.
- Ticket #46 brief written (`docs/briefs/46-mentor-conversation-list.md`) but not executed this session.

### Decisions

- The bundle script takes a no-curated-list approach (everything `git ls-files` tracks, minus lockfile/binaries) rather than a maintained include list, so the planning surface always sees what the repo actually tracks.

### Next

- Ticket #47 — consolidate the six direct OpenAI call sites into `lib/llm.ts`.
- Ticket #46 — execute the mentor-conversation-list brief.
- Ticket #45 and the rest of the carryover list are unchanged from session 1 above.

## 2026-07-02 — session 3: ticket #47 landed, ticket #46 landed, live-migration gap found and fixed

### Changed

- Ticket #47 executed per `docs/briefs/47-centralize-llm-calls.md`: added `lib/llm.ts` (`getChatCompletion()`) and migrated all six direct-OpenAI-client call sites (`app/api/generate-path-options`, `app/api/generate-project-name`, `app/api/mentor`, `lib/conversation-bundle.ts`, `lib/generate-identity-report.ts` [two calls], `lib/generate-path-plan.ts`) to use it. Mechanical only — no prompt/token/temperature/parsing/validation changes. Commit `e0b19c3`.
- Ticket #46 executed per `docs/briefs/46-mentor-conversation-list.md`: added `listConversations()` (`lib/conversations.ts`) and `listMessages()` (`lib/messages.ts`), and switched `/mentor` from auto-starting a single chat on load to a list-first UX (reverse-chronological conversation list as default view, "New conversation", opening a row restores its history, explicit "End conversation" resolves the bundle via `POST /api/end-conversation` and returns to the list). Auth/subscription gating in `app/mentor/page.tsx` untouched. Commit `de40bd7`.
- Brief cleanup commit `8ae0f95`: removed `document-llm-call-rule.md` (done, landed in `cfcea20`), added `47-centralize-llm-calls.md`.

### Decisions

- Verified ticket #46 end-to-end on the live dev Supabase project (per user instruction, not just typecheck/lint/build) using a synthetic magic-link test user and a headless-Playwright driver script, since no project skill for running this app existed yet. All six flows (empty list, new conversation, send message, back to list, reopen existing, end conversation) passed with zero console errors; the three synthetic test users and their data were deleted afterward via the service-role key, cascade-verified.
- That live run surfaced that `supabase/migrations/20260702000001_conversation_bundles.sql` — despite being committed in `8f4556d` and logged as "Executed" in this same file's session-1 entry above — had never actually been applied to the live database. `conversations.last_message_at` / `summary` / `summary_generated_at` and their trigger did not exist; `listConversations()` was silently failing and falling back to an empty list every time. There is no Postgres connection string, no authenticated `supabase` CLI, and no `exec_sql`-style RPC available in this environment, so DDL can't be run programmatically — the user ran the migration's SQL directly in the Supabase SQL Editor. Confirmed applied by re-querying the live schema before re-running the test. This means the migration file and the live schema can silently diverge even after a migration is committed and the changelog says it ran — committing the file is not the same as applying it.

### Next

- Ticket #45 — meta-bundle (aggregating bundles across conversations) — still open, unchanged.
- Consider a `/run` project skill for this app (magic-link test-session bootstrap + Playwright driver) so future verification passes don't have to be rebuilt from scratch.
- Worth a pass to confirm no other committed migration has this same apply-gap — this session only checked `conversations`.
- Everything else carried over from `2026-07-01.md` remains untouched.

## 2026-07-02 — session 4: local memory documented, LLM calls rule finalized, ticket #45 shipped

### Changed

- Added a "Local memory (Claude Code)" section to `AGENTS.md`, naming the persistent file-based memory Claude Code has kept outside the repo (`.claude/projects/C--Users-miros-zyrro-app/memory/`) since an earlier session — it held narrow operational lessons (live-schema verification, migration-applied checks) with nothing in the repo's own docs acknowledging it existed. Per `docs/briefs/document-cc-local-memory.md`. Commit `fbc7d08`.
- Finalized the "LLM calls" section in `docs/standards/coding-standards.md` now that ticket #47 has landed: dropped the stale "doesn't exist yet" caveat, and distinguished swapping models within a provider (one-line env change) from swapping providers entirely (not one-line — `lib/llm.ts` wraps the OpenAI SDK directly, so a provider swap means rewriting that one file). Per `docs/briefs/finalize-llm-calls-rule.md`. Commit `f8704dd`.
- Ticket #45 (meta-bundle mechanism) shipped per `docs/briefs/45-meta-bundle.md` — mechanism only, mirroring how #16 shipped `resolveConversationBundle()` before #46 wired it in; wiring into the Dashboard is ticket #13, separate. Commit `94a6afc`:
  - `supabase/migrations/20260702000002_meta_bundles.sql` — new `meta_bundles` table (`id`, `user_id`, `content`, `created_at`), append-only by design (no status field, no `updated_at`/trigger, no UPDATE policy) — "current" is the latest row by `created_at`. RLS: insert/select own rows only.
  - `lib/prompts/meta-bundle.ts` — `META_BUNDLE_PROMPT`, synthesizes a chronologically-ordered JSON array of conversation summaries into one evolving continuity baseline (recurring themes, standing commitments, shifts over time). Plain prose output.
  - `lib/meta-bundle.ts` — `resolveMetaBundle(userId)`: resolves any stale conversation bundles first (via `resolveConversationBundle()`), then re-synthesizes only if any conversation's `summary_generated_at` is newer than the latest `meta_bundles` row (or none exists yet). Synchronous, no polling — mirrors `resolveConversationBundle()`'s pattern exactly, including returning `null` on any fetch/write failure rather than a partial success. All LLM calls go through `getChatCompletion()`.
- Two brief-cleanup commits following the established pattern (add the just-executed brief, remove the ones whose work already landed): `21050b8`, `30ad9e5`.

### Decisions

- **`20260702000002_meta_bundles.sql` is committed but NOT applied to the live Supabase project.** Nothing this session needed it live, so per the user's explicit instruction it was left unapplied rather than run speculatively. Next session: don't assume it exists on the live DB just because it's committed and in this changelog — verify first (this is exactly the gap session 3 found with `conversation_bundles`).
- Meta-bundle storage is a dedicated table, not a new `artifacts` type, because `artifacts` is scoped to gated, one-current-row-per-type user-facing deliverables (Identity Report, Path Options, Plan); the meta-bundle is internal continuity machinery, never shown to the user directly, and is genuinely multi-row by design.

### Next

- Ticket #45's migration needs to be applied (SQL Editor, same manual step as before) before `resolveMetaBundle()` can be exercised against real data.
- Ticket #13 — wire the meta-bundle into the Dashboard UI (depends on the above).
- Ticket #46's earlier `/run`-project-skill suggestion and the "check other migrations for the same apply-gap" item are both still open.
- Everything else carried over from `2026-07-01.md` remains untouched.
