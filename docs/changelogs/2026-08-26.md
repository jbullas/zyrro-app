## 2026-08-26 — #129 Stage A: checkpoint infrastructure (placeholder content)

Per `docs/briefs/129-stage-a-checkpoint-infrastructure-brief.md` (full
design: `docs/briefs/129-checkpoint-guided-path-selection-design.md`).
Infrastructure only — no real reasoning prompts, no OpenAI calls, no
`identity_reframe`/`path_options`/`path_plan`/user-facing route touched.
Builds the three pieces the staged, checkpoint-guided path-selection
redesign needs before Stage B lands real content on top of them.

### Changed

`supabase/migrations/20260826000000_path_checkpoint_session_stage_a.sql`
(committed and run against the live dev Supabase project):
- Extended `artifacts_type_check` with two new types.
- `path_checkpoint_session` — **mutated in place** (one row per user),
  not append-only, per the brief's explicit instruction not to apply
  the Tier C pattern here. New `current_stage` column (own column,
  same precedent as `path_plan`'s `path_options_artifact_id`/`path_id`)
  plus the existing `content` jsonb column holding
  `{ stage_outputs: { stage1: {...}, stage2: {...}, ... } }`, each key
  overwritten independently on redo.
- `path_checkpoint_result` — a normal Tier C append-only artifact
  (same `getCurrentArtifact` read pattern and generating-guard shape as
  `identity_report`/`identity_reframe`), written once at session
  completion. New `path_checkpoint_session_id` column links it back to
  the session that produced it (self-referential FK, same pattern as
  `path_plan`'s `path_options_artifact_id`).
- Two new partial unique indexes
  (`artifacts_one_path_checkpoint_session_generating_per_user`,
  `artifacts_one_path_checkpoint_result_generating_per_user`), same
  shape as #59/#71/#98.
- New, separate table `path_checkpoint_exchanges` (checkpoint log —
  what was presented, what the user chose/typed) — reuses the
  `conversations`/`messages` turn-taking *shape* (`role` + `content` +
  `created_at` ordering) but is its own table. RLS: insert/select for
  the owning user only, no update/delete policy (append-only by
  omission; service role still bypasses RLS as usual).

`lib/path-checkpoint.ts` (new) — the state-machine module owning every
mutation to a `path_checkpoint_session` row: `startCheckpointSession`
(insert-then-catch-23505-then-reread, #71's create-race pattern),
`claimGeneration` (conditional UPDATE for the *mid-flow* claim race —
see Decisions below), `recordStageOutput` (stage-scoped overwrite),
`advanceToStage`, `logExchange`, `completeCheckpointSession` (writes
the Tier C `path_checkpoint_result` row, links it back, marks the
session `complete`).

`lib/artifacts.ts` — `ArtifactType`/`ArtifactStatus` unions extended
with the two new types and `awaiting_checkpoint`/`complete` (type-only
change, confirmed via `git diff` — no behavior change to any existing
artifact type's handling).

`scripts/verify-129-stage-a.mts` (new, kept per Miroslav's call at
review — Stage B will build real reasoning on top of this exact state
machine, cheap to rerun as a regression check) — standalone script, not
wired into any route, exercising all 5 scenarios from the brief against
the real live dev Supabase instance with real throwaway auth users
(`admin.auth.admin.createUser`, no magic-link/browser/OpenAI call
needed since no route is involved yet).

`npx tsc --noEmit` — clean throughout.

### Verification — full terminal output, direct DB reads throughout

All 5 scenarios passed on a single run
(`npx tsx --env-file=.env.local scripts/verify-129-stage-a.mts`):

1. **Walk through several stages** — session created (stage 1,
   `generating`) → stage 1/2 placeholder outputs recorded → status
   correctly `awaiting_checkpoint` after stage 2.
2. **Checkpoint exchange (presented → redo → proceed)** — a "presented"
   entry logged, then a redo (stage 2 overwritten to a v2 placeholder),
   then a proceed (advanced to stage 3). Repeated once more at stage 4.
   Confirmed via **fresh, independent DB reads** (not reused in-memory
   objects) that stage 2's and stage 3's outputs were byte-identical
   (`JSON.stringify` equality) before and after each sibling stage's
   redo. The full 8-entry exchange log was read back independently by
   `session_id` (not through the session row) and matched the exact
   expected role sequence
   (`presented, redo, proceed, presented, redo, proceed, presented, proceed`).
3. **Abandoned session** — created, advanced to stage 2, then
   deliberately not touched again ("closed tab" simulation); a fully
   separate later read via `getCurrentArtifact` confirmed
   `current_stage`/`status` intact.
4. **Concurrent generation attempts**, tested as two distinct races
   (see Decisions — the brief's guard only covers one of them):
   - **4a, creation race**: two concurrent `startCheckpointSession`
     calls for the same fresh user resolved to the identical row id
     (loser caught `23505`, reread and reused the winner's row); a
     direct count confirmed exactly one `path_checkpoint_session` row
     exists for that user.
   - **4b, mid-flow claim race**: two concurrent `claimGeneration` calls
     against the *same existing* row — exactly one returned the row,
     the other `null` (the loser-reuses-winner contract, enforced by a
     conditional `UPDATE ... WHERE status != 'generating'` rather than
     the partial index, since two `UPDATE`s on one row can't violate a
     uniqueness constraint against each other).
5. **Completion** — `completeCheckpointSession` wrote a placeholder
   `path_checkpoint_result` row; `getCurrentArtifact` resolved the same
   row; `path_checkpoint_session_id` correctly linked back; the session
   row read back as `status: 'complete'`.

**Schema-separation check** (direct DB read, not code inspection alone):
fetched the live PostgREST OpenAPI description before and after running
the migration — confirmed `path_checkpoint_exchanges` exists as a fully
distinct table with its own column set, and that `artifacts` gained
exactly the two new columns expected, nothing else drifted from the
migration file. Repo-wide grep of every `.from('conversations')` /
`.from('messages')` / `last_message_at` reference (`lib/conversations.ts`,
`lib/conversation-bundle.ts`, `lib/meta-bundle.ts`, `lib/messages.ts`,
`lib/handoff.ts`, `app/api/end-conversation/route.ts`) confirmed every
call site uses a literal table-name string — nothing dynamic that could
accidentally pick up the new table, and the `last_message_at` staleness
trigger (`set_messages_conversation_last_message_at`) is declared
directly on `public.messages`, so it cannot fire for a different table
by construction.

**Cleanup verified, not assumed**: after each test user's teardown
(`admin.auth.admin.deleteUser`), a direct count query against
`profiles`/`artifacts`/`path_checkpoint_exchanges` for that `user_id`
confirmed zero orphaned rows for all three test users used across the
run.

### Decisions

- **Renamed `path_selection_session`/`path_selection_result`/
  `path_selection_exchanges` (the brief's literally proposed names) to
  `path_checkpoint_*`** before writing anything live — flagged to
  Miroslav that the brief's proposed name collides badly with the
  pre-existing, unrelated `public.path_selections` table (#10, project
  naming / "which path did the user pick" event log). Confirmed with
  him before implementing; `path_selections` itself is completely
  untouched by this migration.
- **Two separate concurrency guards, not one**, after finding the
  brief's literal instruction ("same partial-unique-index-on-generating
  pattern as #59/#71") only actually protects the initial *creation*
  race — #59/#71's precedent is append-only (a race between two
  INSERTs), but this type is mutated in place, so a *second* race (two
  concurrent claims on the one already-existing row — e.g. a
  double-click on "proceed" once a session already exists) can't be
  caught by any unique index at all, since two UPDATEs on the same row
  never violate a uniqueness constraint against each other. Implemented
  the brief's literal index for the creation race, and added a
  conditional-UPDATE `claimGeneration` helper for the mid-flow race —
  both are exercised as separate scenarios (4a/4b) in the verification
  script. Flagged in the migration file's own comments, not silently
  decided.
- Kept `scripts/verify-129-stage-a.mts` (Miroslav's call at review) —
  Stage B builds real reasoning directly on top of this state machine,
  so rerunning it is a cheap regression check going forward.
- Final artifact (`path_checkpoint_result`) writes `status: 'ready'`
  directly rather than `generating` → `ready`, since Stage A has no
  async generation step to model yet (placeholder content is
  synchronous). Stage C will need to add the real
  `generating`-then-update-to-`ready` transition once actual LLM
  content generation lands, matching `path_options`'s existing shape.
- Discovered in passing, not part of this ticket: a live-only Postgres
  trigger auto-creates a `profiles` row on `auth.users` insert — it's
  not in any tracked migration (`profiles_baseline.sql` shows no such
  trigger). The verification script's test-user bootstrap had to switch
  from `insert` to `upsert` on `profiles` after hitting a duplicate-key
  error from this. Worth tracking down and adding a migration for at
  some point, but out of scope here — noting it so it isn't rediscovered
  from scratch next time.

### Open item carried to Stage A/B review

- Whether `path_checkpoint_session`/`path_checkpoint_exchanges`/
  `path_checkpoint_result` should register with #79/#80's eventual
  lineage/versioning standard, or predate and inform that decision —
  per the design doc's own §6, left undecided here (not blocking).

### Files

- `supabase/migrations/20260826000000_path_checkpoint_session_stage_a.sql`
  (new)
- `lib/path-checkpoint.ts` (new)
- `lib/artifacts.ts`
- `scripts/verify-129-stage-a.mts` (new, kept)

### Next

- Stage B: reasoning pipeline + Checkpoints 1–2 (Stages 1–4 of the
  design doc), including the blocking `identity_reframe` 409 fix in
  `generate-path-options/route.ts` as part of the input rewrite.
- `docs/briefs/129-stage-a-checkpoint-infrastructure-brief.md` — both
  Definition of Done conditions now hold (implementation committed this
  session, live-verified this session against the real dev DB) —
  deleting it in its own commit per AGENTS.md's session-end procedure.

## 2026-08-26 — #129 Stage B: reasoning pipeline + Checkpoints 1-2 (real content)

Per `docs/briefs/129-stage-b-reasoning-pipeline-brief.md` (full design:
`docs/briefs/129-checkpoint-guided-path-selection-design.md`). Builds Stages
1–4 of the checkpoint-guided redesign as real (non-placeholder) reasoning,
on top of Stage A's `lib/path-checkpoint.ts` state machine (extended, not
replaced). Stage 5/Checkpoint 3/Stage 6 stay Stage C's; `app/path/page.tsx`
and all other user-facing UI/routing untouched, per the brief's hard stops.

### Part 1 — `identity_reframe` 409 bug fix (own commit, verified before the rest)

`app/api/generate-path-options/route.ts` — removed the `identity_reframe`
artifact lookup and its 409 branch (that artifact type has generated for
no one since #124 folded it into `identity_report.reframe_teaser`), reading
`reframe_teaser` straight off the already-fetched `identity_report` content
instead. Committed alone (`bdc0d17`) and live-verified against a real,
freshly-entitled persona (Leona, zero `identity_reframe` artifacts) before
any Stage B work began: `POST /api/generate-path-options` returned 200 (was
409), resulting `path_options` artifact reached `ready` with valid 4-option
content. Rerun a second time, fresh, against the exact committed state with
nothing pending on top, per an explicit request to remove any doubt about
sequencing — same result both times, full terminal output both times.

### Part 2 — the reasoning pipeline

New `lib/generate-path-checkpoint.ts` — `ingestStage1Context` (pure
retrieval: raw `discovery_answers`, the full scored `signatures[]` list via
`raw_signature_analysis` per #62 with a documented fallback for reports that
predate it, `energisers`/`friction_points`, `reframe_teaser.forward_frame`),
`runStage2Intersections`, `runStage3Friction`, `runStage4Candidates` — three
real LLM calls via new `lib/prompts/path-checkpoint.ts`, each carrying the
brief's required grounding-citation discipline and "could this be written
for a different user" self-check.

`app/api/generate-path-options/route.ts` rewritten again (on top of the
bug-fix commit): no longer calls `PATH_OPTIONS_PROMPT` directly — kicks off
a `path_checkpoint_session` and runs Stage 1+2 in the background instead.
`PATH_OPTIONS_PROMPT`/`lib/prompts/path-options.ts` left in place, not
deleted, per the brief (Stage C may reuse its prose conventions).

New `app/api/path-checkpoint-response/route.ts` — handles `proceed`/`redo`
at Checkpoint 1 (stage 2) and Checkpoint 2 (stage 4). 2-redo cap per
checkpoint (design doc §6 default, still open on exact escalation
behavior): on the 3rd redo attempt, auto-proceeds with the last-generated
version instead of regenerating again, logging both the capped `redo` and
the `auto_forced` `proceed` exchange explicitly — at Checkpoint 2
specifically, since a redo never carries a genuine candidate choice, this
means auto-selecting the first Stage 4 candidate (array order, deterministic)
and logging it plainly as `was_genuine_user_pick: false`, never presented as
a real pick.

`lib/path-checkpoint.ts` extended (Stage A's state machine unchanged, not
replaced): `startCheckpointSessionWithCreationFlag` (lets a route know
whether *it* won the session-create race before kicking off real background
work — Stage A's own `startCheckpointSession` didn't need this, its callers
only ever cared about the resulting row), `recordChosenCandidate`,
`countRedosForStage`.

**Caught in review, fixed before anything ran**: two real bugs in the first
draft of `path-checkpoint-response/route.ts`, both flagged directly rather
than smoothed over —
1. `recordChosenCandidate`'s first draft never actually wrote the chosen
   candidate id into `content` (`content: supabase.rpc ? undefined :
   undefined`, leftover placeholder junk) — fixed to merge into
   `priorContent`, same pattern `recordStageOutput` already uses.
2. An `as never` cast on a Stage 4 redo call was hiding a real type
   mismatch (the surviving-overlaps shape passed didn't actually match what
   Stage 4 expected) — fixed by narrowing `runStage4Candidates`'s parameter
   type to `Pick<Stage3Output, 'surviving'>` (all it ever reads) instead of
   requiring a full `Stage3Output`, removing the need for any cast.

**A second, broader concurrency gap found on audit** (explicitly requested,
not assumed clean): the redo path claimed generation via `claimGeneration`
before proceeding, but the `proceed` path didn't — two concurrent `proceed`
clicks could both call `advanceToStage` and both fire background work.
Worse, *any* synchronous throw after a successful claim (not just the one
stage-4-invalid-choice branch already patched) would strand a session at a
false `'generating'` status forever, since nothing else would ever reset
it. Restructured rather than spot-patched: every path now claims once,
upfront, and everything after that claim runs inside one `try/catch` in
`POST()` that resets status back to `awaiting_checkpoint` on any exit that
isn't a background handoff or an explicit status-settle.

`npx tsc --noEmit` clean throughout, at every step.

### Verification — full terminal output, 3 real personas, real OpenAI calls

`scripts/verify-129-stage-b.mts` (new, kept — reusable for Stage C, same
convention as `verify-129-stage-a.mts`) drove the real HTTP routes end to
end via a fresh local dev server against Leona (redo at both checkpoints),
Katalin (forced the 2-redo cap), and Matteo_Varga (concurrent-duplicate-
start guard). All mechanical assertions passed; full run logged to a local
file and read back in full (not summarized) for the grounding/genericness
review the brief requires as a judgment call, not an automated assert:

**Grounding** — spot-checked line by line against each persona's real
`discovery_answers`. Every `evidence_citation`/`desire_citation` traced to
real text: e.g. Leona's Pioneer overlap cites "flood investigations" and
"comfort with variables you can't control" (real, from her Q11/Q12
answers and `evidence_analysis` prose); Katalin's Builder overlap cites
"your passion for renovation" (real); Matteo_Varga's Builder overlap
quotes "That wasn't there before" (real, verbatim from his `evidence_analysis`).
No fabricated overlaps found in any of the three personas' Stage 2/3/4
output.

**Genericness — the actual question this stage exists to answer** — the
three personas' Stage 4 candidate sets are genuinely distinct, evidence-driven,
and not padded to a template count (Leona: 2 candidates, Katalin: 3,
Matteo_Varga: 1). Matteo_Varga's run is the clearest evidence the redesign
works as intended: Stage 3 correctly **dropped** his Meaning Maker overlap,
citing a real, specific friction point ("Struggle when projects lack a
clear or meaningful purpose") as structurally disqualifying; Stage 4 then
correctly **consolidated** his two surviving overlaps (Originator + Builder)
into one coherent candidate ("Visionary Creator") rather than forcing them
into two separate options — the exact behavior #25's original genericness
finding (four cultural-default archetypes regardless of user data) lacked.
**Richer input plus the staged filter/derive split does move the needle** —
this is not the same failure mode as before.

**One real weakness, reported plainly rather than smoothed over**: Leona's
Checkpoint 2 redo (steer: "reconsider... maybe a different grouping")
produced candidates that are only cosmetically different from the first
attempt — same 1-signature-per-candidate structure, same two signatures,
renamed ("Trailblazing Explorer"→"Pathfinder Innovator",
"Visionary Strategist"→"Future Architect"). The redo mechanism itself
works correctly (isolation held via direct DB read, the byte content did
change), but didn't meaningfully honor that specific steer's actual ask.
Not a blocker, but worth a note for Stage C/prompt-tuning attention — the
determinism/temperature question design doc §5 flagged is not fully
resolved by richer input alone.

**Redo isolation** — same direct-DB-read discipline as Stage A, at both
checkpoints, for both a real redo and the cap-exceeded auto-forced case:
every stage's already-confirmed output byte-identical before/after a
sibling stage's redo, confirmed via fresh reads, not reused in-memory
objects.

**Redo cap** — Katalin: 2 real redos (2 real Stage 2 regenerations, both
different from the initial output), then a 3rd redo request correctly
auto-forced a proceed with no 3rd regeneration; both the capped `redo`
exchange (`cap_exceeded: true`) and the `auto_forced` `proceed` exchange
confirmed present via direct, independent query of `path_checkpoint_exchanges`.

**Concurrency** — Matteo_Varga: two concurrent duplicate
`POST /api/generate-path-options` calls resolved to the identical session
id (the route's `existingSession` early-return, not the create-race path
Stage A already proved — that race needs a truly simultaneous *first-ever*
insert, which a sequential test can't force; noted as a real distinction,
not conflated with Stage A's finding).

### A significant discovery, unrelated to Stage B, found incidentally

Driving personas through `/auth/callback` (required to get authenticated
test sessions — no route in this project supports password login) is
supposed to be a no-op for a user who already has a `ready` `identity_report`
(`completeDiscovery`'s idempotency guard). It wasn't: real, unwanted
Layer 1 + Layer 2 regenerations fired for **all 3 personas**, and one hit a
real OpenAI rate limit and failed outright.

**Root cause, confirmed by direct reproduction against real data**:
`completeDiscovery`'s existence check used `.maybeSingle()` with no
`.order()`/`.limit(1)` — `identity_report` is append-only (#59), so any
user who has ever regenerated more than once (the normal state, not an
edge case) has multiple `ready` rows, and `.maybeSingle()` throws
`"multiple (or no) rows returned"` in that case. The thrown error was also
silently swallowed (only `data` was destructured), so the guard fell
through as if no report existed at all — for *every* real user with 2+
`ready` rows, on *every* subsequent login. Reproduced directly against
Leona's real data (8 accumulated `ready` rows): confirmed `PGRST116` before
the fix, a real row with no error after.

**Fixed in its own commit** (`lib/complete-discovery.ts`, unrelated file to
everything else in this brief): added `.order('created_at', {ascending:
false}).limit(1)` before `.maybeSingle()`, same narrowing
`lib/artifacts.ts`'s `getCurrentArtifact` already uses for exactly this
reason — and, since the commit's own message named the silent
error-swallow as part of the bug, also captured and checked that error
(`existingCheckError`), logging it and bailing out (`return false`) rather
than falling through, matching this same function's existing
`insertError`-handling precedent. Verified via direct query reproduction
against Leona's real 8-row data both before and after. One inert `failed`
row this incident left on Katalin's account was cleaned up (harmless per
this table's established "failed rows are noise, not history" convention).

### Decisions

- Bug fix landed and was independently verified *before* the reasoning
  pipeline work began, exactly as the brief required — confirmed explicitly,
  twice, after a direct request to remove any doubt about sequencing.
- `completeDiscovery`'s idempotency bug fixed immediately in its own commit
  rather than just logged as a new ticket reference, per explicit
  instruction — small, precisely diagnosed, and directly caused by this
  session's own test activity.
- Kept `scripts/verify-129-stage-b.mts` (same call as Stage A's script) —
  Stage C builds directly on this exact session shape.
- Redo-cap escalation behavior (auto-proceed with the last-generated
  version, deterministic first-candidate auto-select at Checkpoint 2) is
  Stage B's default per the brief, not a final product decision — design
  doc §6 still lists this open.

### Files

- `app/api/generate-path-options/route.ts`
- `app/api/path-checkpoint-response/route.ts` (new)
- `lib/path-checkpoint.ts`
- `lib/generate-path-checkpoint.ts` (new)
- `lib/prompts/path-checkpoint.ts` (new)
- `lib/complete-discovery.ts`
- `scripts/verify-129-stage-b.mts` (new, kept)
- Ad hoc persona-refresh/diagnostic scripts (regenerated 3 personas'
  `identity_report` to pick up post-#124 `reframe_teaser`, granted manual
  entitlements, confirmed/fixed the `completeDiscovery` bug, cleaned up one
  failed row) — all deleted after use, none kept.

### Next

- Stage C: develop the chosen path (Stage 5), Checkpoint 3, final
  report+plan write-up (design doc §4's 6-part structure). Sessions from
  this run (Leona/Katalin/Matteo_Varga, all at `current_stage: 5` with a
  `chosen_candidate_id` recorded) are live, real examples ready for Stage C
  to pick up.
- Leona's Checkpoint 2 redo producing only a cosmetic difference is worth
  Stage C/prompt-tuning attention — not a blocker, flagged above.
- `docs/briefs/129-stage-b-reasoning-pipeline-brief.md` — both Definition
  of Done conditions now hold (implementation committed this session,
  live-verified this session against real personas and real OpenAI calls)
  — deleting it in its own commit per AGENTS.md's session-end procedure,
  same corrected practice as Stage A (tracked in git before this session's
  work began, real `git rm` for the deletion, not a loose untracked file).

## 2026-08-26 — #129 Stage C: develop chosen path, Checkpoint 3, final report+plan

Per `docs/briefs/129-stage-c-develop-path-brief.md` (full design:
`docs/briefs/129-checkpoint-guided-path-selection-design.md`, updated same
day before this stage started — §4's destination/strategy reordered, master
strategy restructured from phases into evidence-grounded sequential
objectives, new §8 formally defers `/plan`'s mutable shape to a separate
ticket). Implements Stage 5 (develop the chosen candidate), Checkpoint 3,
and Stage 6 (final merged report+plan) — the last of the reasoning stages,
built on Stage A/B's state machine (extended, not replaced). Reused the 3
real sessions Stage B left parked at `current_stage: 5` (Leona, Katalin,
Matteo_Varga) rather than starting fresh, per the brief.

### Changed

`lib/prompts/path-checkpoint.ts` — two new prompts: `STAGE5_DEVELOP_PROMPT`
(re-checks the committed direction against `friction_points` alone now that
attention isn't split across candidates, sharpens the thesis, assesses
stretch as Natural/Adjacent/Reinvention from real evidence) and
`STAGE6_REPORT_PROMPT` (the design doc §4 six-section report — see
Decisions below for two rounds of revision to its `master_strategy`
section).

`lib/generate-path-checkpoint.ts` — `runStage5Develop`, `runStage6Report`,
and `resolveChosenCandidateInputs` (the chosen-candidate/overlap-narrowing
step both the initial kickoff and a Checkpoint 3 redo need, kept out of the
prompt-calling functions since it's a plain filter, not reasoning). Also
fixed a real, pre-existing type-modeling bug found while wiring this up:
`Stage3Output.surviving` was typed as an *array-type* intersection
(`Stage2Overlap[] & Array<{...}>`) instead of an *element-type* intersection
— harmless until `.filter()` needed a precise return type for the first
time, at which point TypeScript's inference broke down. Fixed to
`Array<Stage2Overlap & { friction_considered: string }>`.

`app/api/generate-path-options/route.ts` — the existing-session resume
branch now detects a session parked at `current_stage: 5` by Checkpoint 2's
proceed (Stage B's `recordChosenCandidate`) with no `stage5` output yet,
and kicks Stage 5 off — the actual trigger that moment never had, since
Stage 5 didn't exist when Stage B shipped. Guarded by the same
`claimGeneration` discipline as the original Stage 1/2 creation race.

`app/api/path-checkpoint-response/route.ts` — handles `stage === 5`
(Checkpoint 3 proceed/redo) alongside the existing stage 2/4 handling,
same claim-once-upfront + try/catch-status-reset discipline Stage B
established (not relaxed for the new stage, per the brief). Proceeding from
Checkpoint 3 advances to stage 6 and runs the final report in the
background, ending in `completeCheckpointSession` — there's no checkpoint
after Stage 6 (final delivery), so nothing else settles status afterward.

Closed a genuine ambiguity discovered while wiring the resume branch:
`current_stage === 5 && status === 'awaiting_checkpoint'` describes two
different real states — "parked by Checkpoint 2, Stage 5 never run" and
"Stage 5 ran, genuinely awaiting a Checkpoint 3 response" — indistinguishable
by those two fields alone. Resolved via `stage_outputs.stage5`'s presence;
`path-checkpoint-response` now returns an explicit 409 for the parked case
("poll `/api/generate-path-options` first") instead of silently
misbehaving (which would have crashed Stage 6 on a premature `proceed`, or
quietly succeeded as an accidental first-run on a premature `redo`).

`npx tsc --noEmit` clean throughout, at every step.

### Two real bugs found in review/verification, both fixed

1. **`lib/path-checkpoint.ts`'s `recordStageOutput`** was overwriting
   session `content` with only `{ stage_outputs: {...} }`, silently
   dropping `chosen_candidate_id`. Harmless throughout Stage A/B — that
   field didn't exist until Checkpoint 2's `recordChosenCandidate`, and
   nothing called `recordStageOutput` again after that until Stage 5 —
   but Stage 5's own kickoff calls it right after the field is set. Caught
   in code review, before any real run, by tracing exactly what
   `recordChosenCandidate` had already written versus what this function's
   literal would preserve. Fixed by spreading `...priorContent` first;
   confirmed via a rerun of `scripts/verify-129-stage-a.mts` (cheap,
   placeholder-only, no real cost) that the fix didn't regress Stage A's
   own scenarios.
2. **A second instance of the same class of bug, one level up**, only
   surfacing in a genuine Checkpoint 3 redo: `path-checkpoint-response`'s
   `runStage5Redo` constructed its own `{ stage_outputs: contentAtCheckpoint }`
   literal without `chosen_candidate_id` — so even with fix #1 in place,
   there was nothing in that literal for `recordStageOutput`'s
   `...priorContent` spread to preserve. This one was **not** caught in
   review — it only surfaced live, as the sole failure in the full
   3-persona `scripts/verify-129-stage-c.mts` run (Leona's Checkpoint 3
   redo wiped `chosen_candidate_id`; every other assertion across all 3
   personas passed). Fixed by including `chosen_candidate_id` in the
   literal; confirmed via a second, cheap, targeted real test (a
   throwaway user with placeholder Stage 1-4 content — no real cost there,
   same precedent as `scripts/verify-129-stage-a.mts` — but one real Stage
   5 LLM call for the redo itself) rather than re-running the full,
   costlier 3-persona suite a second time just to re-prove one fix.
   Audited the other 4 call sites building the same kind of literal
   (Stage 1's first-ever write, and Checkpoint 1/2's own redos) and
   confirmed all 4 are safe by construction — `chosen_candidate_id` only
   ever exists after Checkpoint 2's proceed, which happens strictly after
   all 4 of those call sites could ever run.

### Verification — full terminal output, 3 real personas, real OpenAI calls

`scripts/verify-129-stage-c.mts` (new, kept) drove the real HTTP routes
end to end via a fresh local dev server, reusing Stage B's 3 real parked
sessions: Leona (redo tested at Checkpoint 3), Katalin and Matteo_Varga
(straight through). All mechanical checks passed for all 3 (Stage 5
kickoff detection, redo isolation — stages 1-4 and `chosen_candidate_id`
confirmed byte-identical via direct DB read before/after the Stage 5 redo
that exposed bug #2 above — Stage 6 completion, final artifact shape) once
bug #2 was fixed.

**Grounding** — spot-checked the final report's citations against each
persona's real `discovery_answers`/Stage 2-5 output. Held up well across
all 3: e.g. Leona's `why_it_fits` separates capability (Pioneer signature,
flood investigations) and desire (energisers, sailing, field visits) as two
genuinely distinct threads with the overlap named explicitly, exactly as
design doc §4.3 requires — not one blended paragraph.

**A real edge case, handled reasonably**: all 3 personas had an *empty*
Stage 4 `discarded` set (Checkpoint 2 never actually rejected anything for
any of them), so `not_this` couldn't literally "reuse the discarded set" as
the brief specified — there was nothing to reuse. All 3 reports instead
contrasted the chosen direction against the person's real current
situation (e.g. Leona's "remaining in a static, predictable career path").
Grounded and honest, not fabricated, but not what the brief's instruction
anticipated — worth noting for whenever a persona's Stage 4 does produce a
real discarded set to test the literal instruction against.

**Master strategy — the central quality question this stage exists to
answer — genuinely failed for 2 of 3 personas on the first pass, reported
plainly rather than smoothed over.** Leona's (an exploratory/role-change
direction) held up well: 3 varied objectives, one explicitly reasoned as "a
blend of priority and dependency." Katalin's and Matteo_Varga's (both
physical-renovation directions) did not: 4 objectives each, structurally
identical between the two of them — *Identify a project/space → Develop a
plan → Engage specialists/a team → Execute* — generic Q2/Q3/Q4-style
timeline labels, and every `sequencing_rationale` a pure hard-dependency
chain ("cannot begin until..."). This is exactly the "phases in disguise"
failure the design doc named: a generic project-execution lifecycle wearing
the project's own nouns instead of the word "Phase," not two people's
actual distinct strategic reasoning.

**Iterated the prompt twice in response** (per explicit instruction to
attempt a fix before closing this stage, rather than only logging it as a
follow-up):
- Round 1 added an explicit "watch for phases wearing the project's own
  nouns" rule and a requirement that at least one objective address the
  person's real `friction_considered`/`honest_cost_note`. Re-verified
  directly against Katalin's and Matteo_Varga's real, already-computed
  Stage 1/4/5 data (a direct `runStage6Report` call reusing real data, not
  a fresh full pipeline run) — result: **insufficient**. Katalin's actually
  got a fifth objective ("Reflect on the project's impact") — literally the
  banned "Evaluate" phase under a new name — and neither persona's strategy
  engaged their real friction at all.
- Round 2 added a concrete worked example (a rejected phases-in-disguise
  version and an approved friction-first version of the same hypothetical
  restoration project, styled after this project's own established
  approved/rejected-example convention from `identity-report.ts`).
  Re-verified against all 3 personas' real data: **substantial, verified
  improvement**. Katalin's and Matteo_Varga's first 1-2 objectives are now
  built directly around their real, verbatim-cited friction ("I would like
  one real project of my own — not another research folder";
  "[avoiding] turning an interesting thought into an actual possibility")
  rather than generic project mechanics; the citation format issue (see
  below) partially self-resolved as a side effect. **Residual imperfection,
  reported honestly rather than claimed as fully solved**: later objectives
  in each sequence (engage a team, implement the plan) still lean toward
  project-execution mechanics rather than person-specific reasoning, and a
  same-persona rerun (temperature 0.3, not deterministic) showed
  `grounded_in`'s citation format still varies run to run between real
  quoted text and symbolic index references (`"discovery_answers[10]"`)
  even after the round-2 tightening. Stopped iterating here, per this
  project's established "iterate 2-3 rounds, then log the residual gap"
  discipline (#85/#96/#112 precedent) — two real, verified improvement
  rounds is the right stopping point, not a chase for a perfect result.

**Not regenerated**: the 3 personas' actual *persisted*
`path_checkpoint_result` artifacts (written during the initial full-pipeline
run) reflect the **original, pre-revision** Stage 6 prompt, including
Katalin's and Matteo_Varga's generic master_strategy. The prompt-revision
verification above was done via direct, non-persisting `runStage6Report`
calls reusing real data, not through the production completion path —
regenerating the real artifacts through the actual route wasn't asked for
and isn't currently possible without new machinery (there's no "redo a
completed report" entry point, and building one is Stage D/future
territory, not this brief's scope). Flagging this explicitly rather than
implying the live artifacts already reflect the improved prompt.

### Decisions

- Attempted and shipped a real prompt fix for the master-strategy
  genericness finding within this same session, per explicit instruction,
  rather than only logging it as a follow-up ticket — two rounds, both
  independently verified against real data, residual imperfection reported
  honestly rather than claimed fully resolved.
- Verified fix #2 (the redo-specific `chosen_candidate_id` clobber) with a
  new, small, targeted real test rather than re-running the full 3-persona
  suite a second time — cheaper, and still a genuine real-code, real-LLM
  verification of the exact fix.
- Left the 3 personas' persisted `path_checkpoint_result` artifacts as-is
  (reflecting the pre-revision prompt) rather than unilaterally
  regenerating them through machinery that doesn't exist yet — flagged
  plainly instead.
- Fixed the `Stage3Output.surviving` array-vs-element intersection type
  bug at the type-definition source rather than casting around it at each
  new call site.

### Open items carried forward

- `grounded_in`'s citation format (quoted text vs. symbolic index
  references) is inconsistent run to run — worth tightening further
  whenever Stage 6's prompt gets revisited again, not urgent enough to
  block this stage.
- Master strategy's later-sequence objectives (team assembly, execution)
  still lean toward generic project mechanics more than the first 1-2
  objectives do — a real, if smaller, residual version of the same
  finding.
- The 3 real personas' persisted final artifacts do not reflect the
  improved Stage 6 prompt (see "Not regenerated" above) — Stage D or a
  future session should decide whether/how to regenerate them once a real
  "redo the final report" mechanism exists, if that matters by then.
- `docs/briefs/129-stage-c-develop-path-brief.md` — both Definition of Done
  conditions hold (implementation committed this session, live-verified
  this session against 3 real personas and real OpenAI calls, findings
  including the genericness failure reported plainly per the brief's own
  requirement) — deleting it in its own commit per AGENTS.md's session-end
  procedure, same tracked-before-delete practice as Stage A/B.

### Files

- `lib/prompts/path-checkpoint.ts`
- `lib/generate-path-checkpoint.ts`
- `lib/path-checkpoint.ts`
- `app/api/generate-path-options/route.ts`
- `app/api/path-checkpoint-response/route.ts`
- `scripts/verify-129-stage-c.mts` (new, kept)

### Next

- Stage D: `/path` UX rebuild (replace the 4-card browse UX with the
  checkpoint flow) and project-naming trigger relocation (#10, to Stage 6
  completion per design doc §2).
- Whether/how to regenerate the 3 real personas' final artifacts with the
  improved Stage 6 prompt — open, see above.
- `master_strategy`'s residual genericness in later-sequence objectives and
  `grounded_in`'s inconsistent citation format — both worth another look
  whenever Stage 6 is revisited, not blocking.

## 2026-08-26 — #129 Stage D: /path UX rebuild, project naming relocated

Per `docs/briefs/129-stage-d-path-ux-brief.md` (full design:
`docs/briefs/129-checkpoint-guided-path-selection-design.md`). Replaces
`/path`'s old 4-card browse UX with the real checkpoint-guided flow built in
Stages A-C (Checkpoints 1-3 + final report), rendering the final report in
`/identity`'s eyebrow+documentation+card pattern, and relocates project
naming (#10) from the old path-selection moment to Stage 6 completion. The
old `path_options`/`PATH_OPTIONS_PROMPT` code path, `path_plan`/
`PATH_PLAN_PROMPT`, `/plan`'s mutable task-management system, and all
reasoning/prompt logic in `lib/generate-path-checkpoint.ts`/
`lib/prompts/path-checkpoint.ts` are untouched, per the brief's hard stops.

### Two live bugs found before any real user hit them

1. **`/path` was already broken in production.** Discovered by reading the
   live code before writing anything, not from the brief's own description:
   `loadPathOptions` called the rewritten `POST /api/generate-path-options`
   (returns `{session_id, current_stage, status}` since Stage B) but still
   expected the old `{artifact_id}` shape and polled for a `path_options`
   artifact type Stage B's rewrite no longer creates. Any real user visiting
   `/path` since Stage B shipped would have seen it hang indefinitely. This
   confirms Stage D was fixing a real live regression, not just adding new
   UI on top of working code.
2. **Naming/`path_selections`/`/plan` coupling risk**, found by reading
   `/plan/page.tsx` in full before deciding where to persist the new flow's
   project name: it polls forever for a `path_plan` artifact matching
   `path_options_artifact_id`+`path_id` whenever a `path_selections` row
   exists with no matching plan. Reusing `path_selections` for the new
   flow's naming write would have silently broken `/plan` for every user who
   completes the new checkpoint flow. Not exercised live in this session
   (out of scope — `/plan`'s mutable system is an explicit hard stop) but
   confirmed by direct code reading, not assumption.

### Changed

New `lib/checkpoint-status.ts` — `useCheckpointSessionStatus(sessionId,
refreshKey)`, mirroring `lib/generation-status.ts`'s `useGenerationStatus`
phase model and timing constants (`GENERATION_BUDGET_MS`,
`SPINNER_TIMEOUT_MS`, `SPINNER_LATE_COPY_MS`, `FLIP_GUARD_MS`) but for
`path_checkpoint_session`'s own status vocabulary (`generating` |
`awaiting_checkpoint` | `complete` | `failed`) instead of every other
artifact type's Tier C `generating`/`ready`/`failed`. Ages off `updated_at`,
not `created_at` — deliberate, since this session row mutates in place and
`created_at` would make a session several stages in look falsely stranded.
`refreshKey` exists because the hook stops polling once it lands on
`awaiting_checkpoint`/`complete`, but a redo/proceed flips status back to
`generating` server-side with nothing else to notice — bumped after every
checkpoint-response submission to force a fresh check.

`app/path/page.tsx` rewritten in full (~840 lines) — the main deliverable.
Entry gating (anonymous/no-report/unpaid/checkout-verification) unchanged
from the old page; lands on a new `'checkpoint-flow'` state. Kicks off
`POST /api/generate-path-options` once per session, renders Checkpoint 1
(stage 2 overlaps as evidence/energy tension-block cards), Checkpoint 2
(stage 4 candidates as selectable `.project-name-card`s), Checkpoint 3
(stage 5 developed thesis/anchoring signatures/stretch pill/honest cost),
and the final report (all 7 sections: what it is, why it fits, what it
isn't, honest cost, where it leads, strategy as a numbered stepper over
`master_strategy`, and plan-seed starting actions) — all using `/identity`'s
existing `.section`/`.eyebrow`/`.documentation`/`.card` classes, confirmed
present via grep before use; no new CSS written. Naming dialog/CTA moved
here from the old path-selection moment, auto-offered once on first landing
at the completed report (distinguishing "never asked" from an explicit
`null` "skipped" choice already made).

`app/api/generate-project-name/route.ts` — now accepts
`{ path_checkpoint_result_id }` instead of `{ path_options_artifact_id,
path_id }`; sources `chosenCandidateName` and `signaturesEngaged` from the
linked `path_checkpoint_session`'s stage 4/5 output.
`PROJECT_NAME_PROMPT` itself unchanged. Confirmed via grep this route had
no other caller besides the old `/path/page.tsx`, so safe to adapt in place.

New `app/api/name-path-result/route.ts` — persists `project_name` (a real
name, or explicit `null` for "skipped") directly onto
`path_checkpoint_result.content` via UPDATE, as a narrow, explicitly
documented exception to this table's Tier C append-only convention (this is
user-chosen metadata attached after the fact, not a regeneration of the
report's substantive LLM content). Chosen over reusing `path_selections`
specifically to avoid the `/plan` coupling risk above. No concurrency guard
on the read-modify-write — deliberate, stated explicitly in the route's own
code comment and confirmed with Miroslav before shipping: this doesn't gate
an LLM call or session state, the UI already disables submit after first
click, and the worst case of a genuine double-submit race is a harmless
last-write-wins overwrite on a cosmetic field, not corruption or a stuck
state.

`app/api/mentor/route.ts` — added a fallback branch in `buildContextBlock`
for when no `path_selections` row exists: reads `path_checkpoint_result`
(via `getCurrentArtifact`, status `ready`) for "## Chosen Path" and
"## Current Plan" context instead, so new-flow users don't silently get
degraded Mentor context. Added after Miroslav explicitly asked whether this
gap needed addressing before naming work continued; confirmed as worth
fixing now rather than logging as a gap, since it was small, additive, and
read-only.

`npx tsc --noEmit` clean throughout, confirmed fresh multiple times
including immediately before live testing began.

### Two bugs self-caught during implementation, before any test ran

1. **Stage 5 never actually got kicked off.** Tracing the flow while
   writing the new page revealed `recordChosenCandidate` (Stage B/C) only
   parks the session at stage 5 — nothing re-triggers `generate-path-options`'s
   resume/kickoff branch after Checkpoint 2's proceed. Fixed with a
   dedicated effect keyed on `stage5NotYetRun` (`current_stage===5 &&
   status==='awaiting_checkpoint' && !stage_outputs.stage5`).
2. **Inline component definitions causing DOM remount.** `RedoField`,
   `CapNotice`, `SubmitError` were first written as functions defined
   inside `PathPage`'s render body — a new function reference every render,
   so React remounts their subtree on every re-render, which would have
   made the redo `<textarea>` lose focus after every keystroke (typing →
   `setRedoText` → re-render → new component reference). Fixed by moving
   all three to top-level, prop-driven components before `export default
   function PathPage()`.

### A third bug, found via a caught test-assertion tautology

While fixing a near-tautological test assertion (`bodyText.includes('CAP')
=== false`, flagged before running) to instead assert the actual
cap-exceeded copy directly, tracing the real UI code surfaced a genuine
brief-violating bug: when the redo cap is exceeded at Checkpoint 3 — the
*last* checkpoint — the session jumps straight through Stage 6 generation
to the completed final report, with no intermediate checkpoint screen left
to show `CapNotice` on. It was never rendered anywhere in the final-report
view, so a user hitting this case would see no explanation for why their
last redo request didn't do what they expected — exactly the "silently
jump stages with no explanation" failure the brief explicitly calls out.
Fixed by rendering `<CapNotice notice={capExceededNotice} />` at the top of
the final-report JSX, right after the cover section; confirmed live in
screenshot (the notice text sits directly above "WHAT THIS PATH IS").

### Verification — full live browser walkthrough, real OpenAI calls, real Supabase reads

`scripts/verify-129-stage-d.mts` (new, kept — matching the
`verify-129-stage-a/b/c.mts` naming convention; cheap to rerun as a
regression check on this flow going forward) drove a real dev server via
Playwright, using Matteo_Rinaldi (a genuinely fresh persona: real
`identity_report`, zero prior `path_checkpoint_session`/entitlement
history) for the full-flow walkthrough, and reused Leona/Katalin/
Matteo_Varga's already-complete Stage B/C sessions to confirm the final
report still renders their real, untouched data.

**Full flow (Matteo_Rinaldi):**
- Checkpoint 1 — real overlap signatures (Optimizer, Depth Diver, Meaning
  Maker) rendered as evidence/energy tension-block cards; a real redo
  submitted; direct DB read confirmed Stage 1 context byte-identical
  before/after (isolation holds).
- Checkpoint 2 — real candidates rendered ("System Efficiency Architect",
  "Olive Oil Connoisseur"); selection and proceed worked; Stage 5
  kickoff-resume fix confirmed working live (session auto-advanced past
  the parked state with no manual intervention needed).
- Checkpoint 3 — forced all 3 redos to exercise the cap. The first two
  produced genuinely different real LLM output each time (thesis/
  honest-cost-note reworded both times); the third correctly auto-forced a
  proceed with no further LLM call, logging `cap_exceeded: true` on the
  redo exchange and `auto_forced: true` on the proceed exchange, confirmed
  via direct query of `path_checkpoint_exchanges`.
- Final report — all 7 sections rendered with content grounded in the
  persona's real data; the cap-exceeded notice correctly surfaced (the bug
  above, fixed); naming dialog auto-opened; "Streamline Nexus" chosen and
  confirmed persisted to `path_checkpoint_result.content` via direct DB
  read matching the UI selection.
- Stages 1-4 confirmed byte-identical across the entire Checkpoint 3
  redo/cap sequence.

**Regression checks:** Leona/Katalin/Matteo_Varga's pre-existing completed
reports render correctly, visibly untouched, via the live UI. `/identity`
checked directly for shared-component regressions — none found.

Two issues in the verification script itself were caught before running
and fixed: dead code at what were then lines 165-169
(`.eq('id', session.content ? undefined : undefined)`, always querying with
`undefined` and never using the result) removed in favor of a real query
asserting the persisted `project_name`; and the near-tautological
`bodyText.includes('CAP') === false` assertion (see the third bug above)
replaced with a direct assertion for the actual rendered notice text.

**UI/UX judgment calls made where the brief left specifics open:**
- Redo copy: "Tell us what's missing or off, and we'll take another pass,"
  consistent across all three checkpoints rather than stage-specific
  wording.
- Cap-exceeded messaging: surfaced as a plain, non-alarming notice ("used
  up your redos... [latest] version") rather than an error-styled banner —
  this is an expected, designed-for outcome, not a failure state.
- Checkpoint 2 card grouping: one flat list of candidate cards (no
  additional visual grouping/sectioning), matching the existing
  `.project-name-card` pattern as-is rather than introducing new grouping
  UI.
- Final report section ordering follows the design doc's own 7-part
  sequence directly, with `master_strategy` rendered as a `ConstellationCard`
  numbered stepper (reusing the same component Checkpoint 1 uses for
  overlaps) rather than a new component.

### Decisions

- Naming persistence: write `project_name` onto `path_checkpoint_result.content`
  rather than reuse `path_selections`, specifically to avoid the `/plan`
  polling-forever coupling found above. Confirmed with Miroslav via direct
  question before implementing.
- Mentor context fallback added now, not deferred — small, additive,
  read-only, and directly closes a real gap for new-flow users. Confirmed
  with Miroslav via direct question before implementing.
- No concurrency guard on `name-path-result`'s read-modify-write — a
  stated, deliberate choice (see above), not an oversight, confirmed with
  Miroslav via direct question before implementing.
- Kept `scripts/verify-129-stage-d.mts` (renamed from a `_tmp-` prefix
  after verification passed, to match the `verify-129-stage-a/b/c.mts`
  naming convention rather than a divergent name) — cheap to rerun as a
  regression check on the full checkpoint flow + naming + final report
  rendering.

### Files

- `app/path/page.tsx`
- `lib/checkpoint-status.ts` (new)
- `app/api/generate-project-name/route.ts`
- `app/api/name-path-result/route.ts` (new)
- `app/api/mentor/route.ts`
- `scripts/verify-129-stage-d.mts` (new, kept)

### Next

- No outstanding known bugs from Stage D as of this session's final
  verification run (full pass, all assertions green).
- Whether/how to eventually regenerate Leona/Katalin/Matteo_Varga's Stage 6
  content with the improved prompt from Stage C remains open — carried
  forward from Stage C, not touched here.
- `docs/briefs/129-stage-d-path-ux-brief.md` — both Definition of Done
  conditions hold (implementation committed this session, live-verified
  this session via a full real-browser walkthrough including a redo and
  the cap-exceeded case) — deleting it in its own commit per AGENTS.md's
  session-end procedure, same tracked-before-delete practice as Stages A-C.
