## 2026-07-01 — Debug-log strip, merge feat/session-gated-generation → dev

### Changed

- Removed the three temporary `console.log` lines added in `ce5c478` (setAll, verifyOtp, getUser) from `app/auth/callback/route.ts`. No other logic touched.
- Deleted six completed brief files from `docs/briefs/` as routine cleanup: docs-reorg, fix-generation-stuck, generate-report-dead-branch-cleanup, mentor-rebuild, single-source-report-generation, test-mode-open-access.
- Merged `feat/session-gated-generation` into `dev` via standard merge commit (fast-forward not possible — `dev` had two changelog commits not on the feature branch). Merge commit `2d0af5e`, pushed `0500b54..2d0af5e`.
- Resolved one conflict in `docs/changelogs/2026-06-24.md` by keeping both sides (append-only changelog — kept the Correction block that was already on `dev`).

### Verified

- Thread **(c) proxy re-test** independently re-confirmed today: JWT expiry set to 300s, session left idle ~10 min past expiry, `/identity` loaded normally on reload — `proxy.ts` refreshed the token server-side without intervention. JWT expiry restored to 3600s after.
- Traced the existing "Correction" already on `dev` (commit `0500b54`, authored 2026-06-24 21:24) documenting an earlier passing proxy re-test that hadn't propagated into later sessions' Next tracking. Today's run independently confirms the same result.

### Decisions

- `main` will not sync with `dev` on a rolling basis — the app is not live. `dev` → `main` happens once, at launch, not after each merge.

### Next

- path-options / select-path hardening (not started)
- verify-checkout hardening (not started)
- Supabase Pro upgrade (deferred)
- Resend transactional email (deferred)
- memory table — kept in place; owning surface undecided
- Stripe subscription billing / Brief B (deferred)
- `dev` → `main` deferred until launch

## 2026-07-01 (session 2) — Route hardening, Path → Project naming, decision docs

### Changed
- `f315d7c` — Route hardening: `generate-path-options` and `select-path`
  now derive `user_id` from the verified session instead of trusting it
  in the request body (401 if unauthenticated). Updated all client call
  sites in `app/path/page.tsx` and `app/plan/page.tsx` to drop `user_id`
  from their request bodies.
- `9e608d6` — Route hardening: `verify-checkout` now grants the
  entitlement solely from `session.metadata.user_id` (Stripe-authored,
  not client-supplied), returning 400 if that metadata is missing rather
  than falling through silently. Client no longer sends `user_id`.
- `27df6f0` — Path → Project naming: added `path_selections.project_name`
  (nullable). New `lib/prompts/project-name.ts` (Rich Barton naming
  framework — rare words, high-scoring letters). New
  `app/api/generate-project-name/route.ts` (session-derived user,
  returns 3 name+rationale options). `select-path` accepts an optional
  `project_name`. `app/path/page.tsx` confirm flow extended with a
  naming step: 3 generated cards + free-text field (mutually exclusive)
  + Skip.
- `2d2a3a8` — Fixed a silent failure in the naming flow: the
  `path_selections.project_name` migration had been written but never
  applied to the live database, so any insert including a name was
  silently rejected (PGRST204) while the route still reported success.
  Migration applied; `select-path`'s insert now checks and surfaces the
  error instead of proceeding blindly to plan generation.
- `51f6e38` — Added a "Project (optional, always offered)" section to
  `docs/framework/zyrro_path_plan_blueprint_v_1.md`, covering position in
  the arc, the always-offered mechanic, naming, explicit branding
  out-of-scope, and the downstream Plan/Dashboard effect.
- `94893be` — Recorded four mentor & continuity philosophy decisions in
  `docs/standards/product-decisions.md`: mentor mode is Zyrro-detected by
  default with manual override; the Reframe should fold in
  `emerging_signatures` when active without exposing it as a separate
  mechanic; continuity surfaces on the Dashboard; the mentor points
  toward real action and real people, not a substitute relationship.
- `7f00235` — Resolved the memory table ownership question (previously
  left "pending" in the decision above): the mentor/continuity system
  owns it, storing named identity, chosen path/Project, and rolling
  session summaries, read by the mentor and displayed on the Dashboard.

### Decisions
- Ticket #11 (naming framework) merged into #10 (Path → Project) —
  naming *is* the Project mechanic now that branding (#12) was dropped,
  so tracking them separately no longer reflected the actual scope.
- Naming offers 3 AI-generated name+rationale options plus a free-text
  alternative, rather than a single suggestion — mirrors how Path
  Options itself presents genuine choice rather than one system answer.

### Next
- Ticket #44 opened: `/path` should render a full elaborated report for
  the chosen path only after selection (not the summary card), with a
  link to review all options again, and a pencil icon to reopen the
  naming dialog for rename/regenerate.
- Continuity/memory work continued in the next session — see
  `2026-07-02.md`.