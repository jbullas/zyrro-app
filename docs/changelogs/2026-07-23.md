## 2026-07-23 — session 1: #59 identity_report Tier C versioning

### Changed
- **Migration** `20260723000000_narrow_identity_report_unique_index.sql`:
  narrowed `artifacts_one_identity_report_per_user` from
  `WHERE type = 'identity_report'` to
  `WHERE type = 'identity_report' AND status = 'generating'`. Before writing
  it, confirmed the live index definition directly (PostgREST doesn't expose
  index definitions, so had the user run a `pg_indexes` query in the
  Supabase SQL Editor rather than trusting the 2026-06-22 migration file or
  any changelog's "Executed" note) — live matched the original file exactly,
  no drift. Applied by the user in the SQL Editor and re-confirmed
  afterward: `WHERE ((type = 'identity_report'::text) AND (status =
  'generating'::text))`. `identity_report` is now append-only like
  `path_options`/`path_plan`: any number of historical `ready`/`failed` rows
  can coexist; the index only still blocks two simultaneous `generating`
  rows for the same user.
- **`lib/artifacts.ts`** (new) — `getCurrentArtifact(supabase, userId, type,
  { status?, select? })`: resolves the most recent artifact row for a
  user+type by `created_at`, optionally narrowed to a status and/or a
  specific column list. One shared implementation for "what counts as
  current" across all three Tier C artifact types. Takes the Supabase client
  as an explicit first argument (works with browser, server, and
  service-role clients) rather than the brief's shorthand two-arg signature,
  since there's no ambient client to import — every call site already
  constructs its own.
- **Migrated 10 read sites across 7 files** onto `getCurrentArtifact` (grepped the live repo
  for every `.eq('type', 'identity_report'|'path_options'|'path_plan')` hit
  first, rather than trusting the brief's ~8-site estimate, which was
  explicitly flagged as a lower bound from a bundle snapshot):
  - `app/identity/page.tsx` — `loadArtifact` and `handleRetry` (identity_report)
  - `components/IdentityCard.tsx` — dashboard card fetch (identity_report)
  - `app/api/generate-path-options/route.ts` — source identity_report
    (status: ready) and existing path_options check
  - `app/path/page.tsx` — `loadPathOptions` (path_options) and the unpaid-state
    named-identity fetch (identity_report, status: ready)
  - `lib/generate-path-plan.ts` — source identity_report (status: ready)
  - `app/api/mentor/route.ts` — context-block identity_report (status: ready)
  - `scripts/rescue-report.mts` — existing-artifact lookup (identity_report)
- **Left unmigrated, each for a different reason** (full raw grep output
  was shown to and confirmed by the user before proceeding):
  - Primary-key lookups (`generate-project-name/route.ts`,
    `mentor/route.ts`'s path_options-by-id) — already unambiguous, no
    "current" to resolve.
  - Selection-scoped lookups keyed on `path_options_artifact_id` + `path_id`
    (`generate-path-plan.ts`'s existing-plan check, `plan/page.tsx`,
    `mentor/route.ts`'s path_plan lookup, `select-path/route.ts`) — see
    Decisions below on why these are singleton by construction, not a
    "which row is current" question.
  - `kickoff-identity-generation.ts`'s own `.in('status', [...])` existence
    check — an idempotency gate ("does *any* live row exist"), not a
    "resolve current" query; deliberately left as-is.
  - `scripts/backfill-signature-fixes.mts` — a deliberate cross-user bulk
    scan for Tier B correction (wants every `ready` row across all users,
    not one user's latest); migrating it would change its behavior, not
    just its implementation.
- **`lib/kickoff-identity-generation.ts`**: kept the existing "delete
  `failed` rows, then insert" step (see Decisions — this was the brief's
  flagged open question). Updated the comments: the delete is no longer
  there to dodge a unique-index collision (a plain insert works regardless
  of prior row status now), and the index itself is documented as scoped to
  `status = 'generating'`.
- **`scripts/verification/session.mts`**: `seedArtifact`'s identity_report
  delete-before-insert went through two revisions this session — see
  Decisions for why it landed back on "delete by type only, any status."

### Decisions
- **Keep deleting `failed` identity_report rows in `kickoffIdentityGeneration`**
  (the brief's open question — explicitly logging the call either way, not
  blocking on it). A `failed` row has no meaningful content (the LLM call
  never completed) — it's noise, not history worth the append-only
  guarantee. Retention is now the *default* for real generations
  (ready/failed-with-content-attempted rows from actual runs stay forever),
  but an empty-content failed stub isn't in that category. Low-risk either
  way; picked the option that keeps the artifacts table free of rows with
  no informational value.
- **`path_options`/`path_plan`'s write paths are not actually append-only**,
  contradicting the brief's stated premise ("path_options and path_plan
  already work this way"). `generate-path-options/route.ts`,
  `generate-path-plan.ts`, and `select-path/route.ts` all UPDATE an
  existing row in place when one's found for the relevant key
  (user+type, or the selection's `path_options_artifact_id`+`path_id`) and
  only INSERT when none exists — singleton-by-construction, not
  history-preserving. This is *why* the selection-scoped read sites listed
  above don't need `getCurrentArtifact`: there's never more than one row to
  disambiguate today. Did not touch this — changing insert-vs-update
  branching is a real scope expansion beyond what this ticket asked for,
  and out of scope per the brief's boundaries (no #60 regen UI, no /start
  changes). Flagging for a future ticket if `path_options`/`path_plan`
  should actually become append-only to match the documented policy.
- **`seedArtifact`'s identity_report delete: revised mid-session after a
  race was flagged.** First pass narrowed the delete to `status =
  'generating'` only, matching the new index. That's unsafe: bootstrapping
  a test user fires `kickoffIdentityGeneration()` in the background via
  `/auth/callback`, and its row's status by the time `seedArtifact` runs
  isn't guaranteed to still be `'generating'` — under a fast/mocked LLM
  backend or slow test-harness startup it could already be `ready` or
  `failed`, leaving a stale duplicate the narrowed delete wouldn't catch.
  Confirmed this isn't fully absorbed by "all reads go through
  `getCurrentArtifact`" either: `kickoffIdentityGeneration`'s own
  idempotency check uses `.maybeSingle()` with no ordering, which throws on
  more than one matching row — a real failure mode for any verification
  flow that re-triggers kickoff-style logic against a seeded user. Reverted
  to deleting by type only (any status). `seedArtifact`'s contract is "one
  deterministic row per call," not exercising history — tests that
  specifically want coexisting rows (like this session's own scenario 2
  verification) insert the second row directly, bypassing this guard.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean after the read-site
  migration.
- Live-verified with an ad hoc script (`scripts/verify-59.mts`, built on
  `scripts/run-verification.mts` — run and deleted, not committed) against
  the applied migration:
  - **Scenario 1 (concurrent-kickoff race guard)**: bootstrapped a test
    user, cleared the background-kickoff's auto-created row to force a
    clean "no artifact" state, then fired two concurrent authenticated
    `POST /api/retry-generation` calls via `Promise.all`. Result: exactly
    one response reported `{ started: true }`, the other `{ started: false
    }`, and exactly one `identity_report` row (`status: 'generating'`)
    existed in the DB afterward — the narrowed index still rejects the
    loser's insert.
  - **Scenario 2 (read-site current resolution)**: seeded a user with an
    older `ready` identity_report row, waited past a 1-second boundary,
    then inserted a second, newer `ready` row directly (bypassing
    `seedArtifact`). Confirmed `getCurrentArtifact` resolves the newer row's
    id; confirmed both rows still exist in the table afterward (older row
    untouched, not deleted). Went further than a direct function call:
    drove two real, independently-migrated read sites through the actual
    running app via Playwright — `/identity` (page-level fetch) and
    `/dashboard` (`IdentityCard`, a separate component with its own
    independent fetch) — both rendered the newer report's named identity,
    not the older one.
  - Dev server started fresh for this pass and stopped afterward; test user
    and all seeded rows torn down via `teardownTestUser` (verified no
    orphaned rows across `profiles`/`conversations`/`artifacts`/`messages`/
    `discovery_answers`).

### Next
- #20 (State 3 of `/start`: logged-in user with zero `discovery_answers`
  inserting a fresh `identity_report` row) can now proceed — it depended on
  this landing first. Not touched this session per the brief's explicit
  stop condition.
- #60 (user-facing regenerate button/opt-in UI) can now be built on top of
  this — the mechanism exists but isn't exposed anywhere yet, also per the
  brief's explicit stop condition.
- Possible future ticket: make `path_options`/`path_plan` actually
  append-only (see Decisions) to match the documented policy their write
  paths currently don't follow.
