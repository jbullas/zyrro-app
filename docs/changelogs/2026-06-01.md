# Changelog — 2026-06-01

### AI Pipeline fixes
- domain_profile in identity-analysis.ts: 
  strengthened instruction to always return 
  all 5 domains (Visioning, Thinking, 
  Connecting, Driving, Sensing), minimum 
  value 10 for domains with indirect evidence, 
  only 0 if absolutely zero evidence
- domain_profile in identity-report.ts: 
  strengthened to require exactly 5 entries, 
  copy from detection JSON, default 10 if missing
- Added pre-return validation check in 
  identity-report.ts: verifies domain_profile 
  has 5 keys, primary_constellation has 5 entries 
  with frequency/intensity/score/confidence, 
  secondary_signature_analysis has 3 entries
- Named Identity format changed to title case 
  in prompt: "The [Modifier] [Core]"
- .named-identity class added to globals.css 
  with text-transform: uppercase — cover heading 
  now uppercase via CSS not prompt
- User name passed to identity-report.ts 
  via API route user message
- prepared_for instruction strengthened: 
  never use "You", always use provided name
- Word count requirements strengthened: 
  evidence_analysis minimum 200 words, 
  constellation_synthesis minimum 300 words, 
  how_you_operate subsections minimum 120 words, 
  total report must exceed 3,000 words
- frequency, intensity, confidence fields 
  explicitly required per primary_constellation 
  entry — must be copied from detection JSON

### /identity page
- Added DEV ONLY regenerate report button 
  above Limits section (remove before go-live)
- handleRegenerate: fetches discovery_answers 
  and profile from Supabase, calls 
  /api/generate-report, switches to generating 
  state and polls every 3 seconds until ready
- userId state added, set after auth check

### generate-report API route
- Before creating artifact, checks for 
  existing identity_report artifact for user — 
  resets to generating if found, inserts new 
  row otherwise
- Profiles insert changed from insert to 
  upsert (onConflict: user_id) — fixes 
  duplicate key error on regeneration
- Added server-side logging for received name, 
  profiles insert result, discovery_answers 
  insert result

### Debugging helpers (temporary)
- Added console.log in handleRegenerate for 
  userId, answers, name, API call params, 
  and response status
- Added console.log in start/page.tsx for 
  name being sent to API
- Added OPENAI_API_KEY prefix log in route startup
- Added full profile row logging in regenerate

### Standards sync
- coding-standards.md: bottom nav section
  updated — "Dashboard" replaces "Home",
  logged-in/logged-out rule replaces
  homepage-only hide rule; gated pages
  updated to single CTA
- product-decisions.md: registration flow
  updated to reflect signUp replacing
  signInWithOtp, server-side migration in
  API route; identity-signature-icons.md
  added to standards files list
- identity-signature-report.md: removed
  "Your Named Identity" eyebrow label from
  cover section spec
- branding-guidelines.md: Dashboard icon
  updated to ti-layout-dashboard; identity
  badge description updated to shield spec

### Environment
- Added SUPABASE_SERVICE_ROLE_KEY to .env.local
  (was missing, causing 500 errors on all 
  generate-report calls)