## 2026-08-04 — #62 persist full Detection Engine output onto identity_report

Per `docs/briefs/62-persist-detection-engine-output-brief.md`. Layer 1's
full output (`signatures[]` + all 4 categorization lists) was being
discarded in-memory once generation completed — only Layer 2's report made
it to `artifacts.content`. Nothing downstream (Reframe/#43, #100's planned
Emerging/Suppressed cards, mentor longitudinal tracking, ad hoc review)
could access it after the fact.

### Changed
- **`lib/artifact-schemas.ts`** — added optional
  `raw_signature_analysis` to `IdentitySignatureReportArtifactContent`:
  `{ signatures, primary_constellation, secondary_signatures,
  emerging_signatures, suppressed_signatures }`, sourced verbatim from
  Layer 1. Optional since existing reports predate the field. Widened
  `schema_version` to `"1.0" | "1.1" | "1.2"` (type-level only — no code
  path currently emits `"1.2"`; Layer 2's prompt still literally emits
  `"1.1"`, unchanged by this ticket).
- **`lib/generate-identity-report.ts`** — in `generateIdentityReport`'s
  Step C, attaches `report.raw_signature_analysis` from the already-in-scope
  `analysis` object (no new API call) right before the `content: report`
  update.

### Decisions
- No migration — additive JSON field on an existing JSONB column, per the
  brief.
- No backfill — Layer 1 is confirmed non-deterministic even at temperature
  0 (#93), so re-running it against stored discovery answers would write
  plausible-looking but non-original data onto historical reports. Existing
  reports simply won't have `raw_signature_analysis`.
- No UI rendering (that's #100 Stage 2, still paused) and no #43 folding —
  out of scope here, both unblocked by this ticket.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean.
- **Live end-to-end**, 2 real generations via `withVerificationSession`
  (`scripts/run-verification.mts`) with real discovery-answer sets, driving
  the actual `/auth/callback` → `kickoffIdentityGeneration` →
  `completeDiscovery` → `generateIdentityReport` path with real OpenAI
  calls — not seeded content. Confirmed via direct Supabase read of the
  resulting `artifacts` row for both users:
  - `raw_signature_analysis` present and populated (`signatures.length: 5`
    in both runs, real `score`/`domain`/`frequency`/`intensity`/
    `score_band`/`confidence`/`supporting_evidence_indexes` per entry).
  - Existing fields untouched: `primary_constellation` (report, length 5),
    `domain_profile`, `derived_from_signature_analysis` all present and
    correctly shaped, `schema_version` still `"1.1"` — a pure addition.
  - Both test users had empty `emerging_signatures` and
    `suppressed_signatures` — no live signal yet on how often those lists
    are non-empty; useful for #100 Stage 2 to know this wasn't exercised
    here.
- Ad hoc verification script run from `scripts/verify-62-tmp.mts`, deleted
  after, not committed — confirmed via `git status`.
- Not committed/pushed yet this session.

### Next
- **#100 Stage 2** — unpaused now that `raw_signature_analysis` exists to
  render Emerging/Suppressed signature cards from.
- **#43** — Reframe folding in `emerging_signatures`, unblocked but not
  built here.
- Consider a follow-up live check once a real user's generation actually
  produces non-empty `emerging_signatures`/`suppressed_signatures`, to
  confirm those persist correctly too (only the empty-array path was
  exercised this session).

## 2026-08-04 — #100 Stage 1: reorganise /identity section order

Per `docs/briefs/100-stage1-reorganise-brief.md`. Pure structure/IA move —
no new content generation, no prompt changes, no new components, no
chart/CSS rework. Reorganised `app/identity/page.tsx`'s existing rendered
blocks into the target 4-section order: Cover (now including Constellation
Synthesis and the Signature Profile score list/radar as an extension of the
Identity Thesis), Signatures (Primary + Secondary deep-dive cards), How You
Operate (with Energisers and Friction Points folded in rather than standing
alone), and What's Next (the #98 reframe CTA, untouched).

### Changed
- **`app/identity/page.tsx`** —
  - Moved the Constellation Synthesis block and the Signature Profile block
    (scoring explanation, Primary bars, Secondary score list, Domain radar)
    up to sit directly after the Cover hero, preserving each block's
    existing internal order verbatim (no sub-element reordering within
    either block — only the two blocks were relocated as units).
    Constellation Synthesis was placed first, Signature Profile second,
    matching the brief's described Cover content order.
  - Moved Primary Signatures and Secondary Signatures deep-dive card
    sections to follow directly after (the "Signatures" section), unchanged
    internally.
  - Folded Energisers and Friction Points into the same `.report-section`
    as How You Operate (previously two standalone top-level sections after
    it) — verified via DOM check that both render inside the How You
    Operate container, not as siblings of it.
  - Removed entirely: the in-page TOC ("IN THIS REPORT" + anchor-jump
    `scrollTo`), "What This Report Is", and "Research Foundation". No old
    pre-#98 CTA remnant was found in the code — the only CTA present is the
    #98 What's Next reframe pitch, which is the protected exception and was
    left untouched (position, gating behavior, and internal rendering all
    unchanged).
  - Dropped the numeric `id="section-N"` anchors along with the TOC that
    was their only consumer (confirmed via repo-wide grep — no CSS
    selector or other code depended on them; `LimitsBlock`'s `id` prop
    comment referencing "section-11 jump-nav" is now stale but the prop
    itself was never actually passed on `/identity`, so left as-is —
    out of scope for a structure-only ticket).
- **`docs/content/identity-static-content-for-91.md`** (new) — preserved
  `WHAT_THIS_REPORT_IS`, `RESEARCH_PILLARS`, and `TOC_ITEMS` verbatim before
  removal from the page — reference only, not rendered, consistent with the
  2026-08-03 no-inline-gap decision (that decision covered live rendering,
  not documentation retention).

### Decisions
- Kept each moved block's internal sub-order exactly as it rendered before
  (e.g. Signature Profile's explanation → Primary bars → Secondary list →
  radar, in that existing order) rather than resequencing to match the
  brief's prose listing order — Stage 1 is scoped to section-level
  reordering/regrouping ("pure move/remove of existing rendered blocks"),
  not fine-grained resequencing within an already-cohesive block.
- Preserved removed static copy in a new `docs/content/` file rather than
  discarding it outright, so #91 has the exact original wording to draw
  from — this is documentation retention, not inline rendering, so it
  doesn't reopen the 2026-08-03 "no inline gap" decision.

### Verification
- `npx tsc --noEmit` and `npm run build` both clean.
- **Live end-to-end** via `withVerificationSession`, seeding an existing
  ready `identity_report` + `identity_reframe` artifact (per the brief: no
  new generation needed since this ticket makes no content changes).
  Confirmed via a real Playwright page load against `/identity`:
  - Top-level section eyebrows render in DOM order: CONSTELLATION SYNTHESIS
    → SIGNATURE PROFILE → PRIMARY SIGNATURES → SECONDARY SIGNATURES → HOW
    YOU OPERATE (with ENERGISERS/FRICTION POINTS confirmed nested inside
    the same `.report-section`, not standalone).
  - "IN THIS REPORT", "WHAT THIS REPORT IS", "RESEARCH FOUNDATION", and the
    McAdams/Csikszentmihalyi/Deci & Ryan/Doidge research-pillar text are
    all absent from the rendered page.
  - #98 What's Next CTA gating unchanged: click on "What does this mean?"
    correctly revealed recap/meaning/reframe/why content plus the "Get My
    Path & Plan" checkout CTA.
  - Zero console/page errors during the full run.
  - Screenshots saved to `.verification-runs/screenshots/` (gitignored),
    not committed.
- Ad hoc verification script run from `scripts/verify-100-tmp.mts`, deleted
  after, not committed — confirmed via `git status`.

### Next
- **#100 Stage 2** — unified all-tier signature bar chart, tier-name pill,
  Emerging/Suppressed signature card rendering (now unblocked by #62's
  `raw_signature_analysis`), shortened Constellation Synthesis rewrite
  (#102), documentation relocation (#91) to consume
  `docs/content/identity-static-content-for-91.md` and then delete it.
- **Stage 3** — any CSS/component/styling changes, explicitly deferred.
