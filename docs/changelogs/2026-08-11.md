## 2026-08-11 — #118, Stage 0 + Stage 1: Evidence data audit + Detection Engine v2 (standalone)

Per `docs/briefs/118-stage0-stage1-evidence-audit-and-detection-engine-v2.md`.
Both stages are findings/standalone-code only — no changes to
`generateIdentityReport`, any API route, `identity_reframe`,
`path_options`, `path_plan`, or any existing backstop
(`enforceSecondaryEvidenceFloor`, `enforceConstellationSynthesisNonOverlap`,
etc). Confirmed via `git status --short` before writing this entry: the
only files touched this session are new/untracked
(`lib/detection-engine-v2.ts`, this changelog entry, and scratch scripts
deleted before commit) — nothing under `lib/generate-identity-report.ts`,
`lib/prompts/*.ts`, or `app/` shows as modified.

### Stage 0 — Evidence data audit

**Premise correction (the actual headline finding of this stage).** The
brief's data source — "`evidence_units` and `signatures[]` already
persisted on `identity_report` artifacts since #62 shipped" — is only
half true. Queried live Supabase directly (23 `identity_report` artifacts,
8 with `raw_signature_analysis` present, all from 2026-08-04 onward):

```
Artifacts with raw_signature_analysis present: 8
Of those, raw_signature_analysis containing an 'evidence_units' key: 0
Of those, raw_signature_analysis.signatures non-empty array: 8
Artifacts with top-level content.evidence_units key: 0

raw_signature_analysis keys: [
  'signatures', 'emerging_signatures', 'secondary_signatures',
  'primary_constellation', 'suppressed_signatures'
]
```

`evidence_units` was never actually persisted — `#62`'s scope, as
actually shipped in `lib/generate-identity-report.ts:590-596` and typed in
`lib/artifact-schemas.ts:176-182`, only ever wrote the full `signatures[]`
array plus the four categorization lists (`primary_constellation`,
`secondary_signatures`, `emerging_signatures`, `suppressed_signatures`).
Layer 1 generates `evidence_units` transiently every run but it's
discarded before the `artifacts.content` write — it never reaches the DB.
Confirmed no separate artifact type carries it either: the only `type`
values present in the live `artifacts` table are `identity_reframe`,
`identity_report`, `path_options`, `path_plan` — no `signature_analysis`
type (the `SignatureAnalysisArtifactContent` type in
`artifact-schemas.ts:56-76` that does declare `evidence_units` describes a
shape nothing in the app actually writes).

**Practical effect:** measures 1, 2, and 4 (all keyed on `evidence_units`
grouped by `source_question`) are not answerable from historical data —
not a weak signal, a hard zero across every real report generated to
date. Per Miroslav's direction, these were folded into Stage 1's
verification pass instead (see below) rather than dropped, since Stage 1
generates fresh `evidence_units` against the same real users' real
`discovery_answers` anyway.

**Measure 3 — evidence density per signature by `score_band`.** This one
*is* answerable — `signatures[].evidence_count` is real, persisted data,
independent of the missing `evidence_units` array. All 8 artifacts, 43
signature entries:

```
weak      : n=8,  avg=1.00, min=1, max=1,  values=[1, 1, 1, 1, 1, 1, 1, 1]
moderate  : n=16, avg=2.50, min=2, max=5,  values=[2, 5, 2, 2, 2, 2, 2, 2, 3, 2, 2, 2, 2, 4, 3, 3]
strong    : n=12, avg=4.00, min=3, max=7,  values=[3, 7, 5, 3, 4, 3, 3, 3, 3, 5, 4, 5]
dominant  : n=7,  avg=5.86, min=4, max=10, values=[5, 5, 10, 6, 6, 5, 4]
```

Clean, monotonic, low-noise relationship (`weak` has *zero* variance — every
single weak-band signature in the real data has exactly 1 evidence unit,
n=8). This is the number the brief asked for: a weak-band signature is
backed by exactly 1 evidence unit, on average less than half of what a
moderate-band signature has (2.5) and roughly a sixth of a dominant-band
signature (5.86). Directly explains why word-count floors requiring
"minimum 2 real story details" (per `evidence_analysis`'s existing prompt
rule) are structurally unachievable-without-padding for weak-band
signatures specifically — there is often only 1 real detail available to
cite, confirmed by real data, not inferred.

### Stage 1 — Detection Engine v2 (standalone)

**Shipped, not wired in:** `lib/detection-engine-v2.ts` — `DETECTION_PROMPT_V2`
(new prompt, evidence-extraction scope only — deliberately narrower than
the live `DETECTION_PROMPT`, since `signatures[]`/`primary_constellation`/
`named_identity`/`domain_profile` are out of scope for this stage; those
become entirely code-computed from `evidence_units` downstream under this
design) and `computeSignatureScoresFromEvidenceUnits()` (the
Frequency/Intensity/Score formula from the brief, implemented exactly as
specified: Frequency = distinct `source_question` count capped at 5,
Intensity = max of Strong=5/Normal=3/Weak=1 across a signature's links,
Score = Frequency × Intensity). Nothing in `lib/generate-identity-report.ts`,
`lib/prompts/identity-analysis.ts`, or any API route imports or calls this
module. `npx tsc --noEmit` clean.

**Test method:** 6 real personas, all pre-existing in the DB with real
`discovery_answers` (13/13 each) and a real prior `identity_report`
generation to compare against — chosen for domain/score-band spread from
the 8 real users found in Stage 0's audit (`domain_profile` from their
existing reports): `katalin_farkas` and `matteo_varga` (Visioning-top, no
dominant-band signature), `nadia_petrescu` (Driving-top + high Sensing,
has dominant-band signatures), `tomasz_zielinski` (Driving specialist,
Thinking=10, has dominant), `leona_markovic` (Driving/Thinking balanced,
no dominant-band signature), `ingrid_solberg` (Driving/Thinking balanced,
has dominant-band signatures). Real OpenAI calls throughout
(`temperature: 0, seed: 42`, same as the live pipeline's Layer 1 call),
no mocking. 6 first-pass generations (one per persona) plus 6 more from
the rerun-stability check below — 12 real generations total this stage.

#### 1. Lens differentiation — PASS, but on a thin sample

Across all 6 personas' first-pass runs, only **2 of 77** evidence units
(2.6%) received 2+ signature links at all:

```
matteo_varga Q2: "I was mostly responsible for finding opportunities,
negotiating difficult acquisitions and getting complicated projects
moving."
  -> Activator [Normal]: Focus on initiating and driving projects forward.
  -> Pioneer [Normal]: Explores new paths by finding opportunities and
     negotiating acquisitions.

tomasz_zielinski Q2: "I led a temporary crisis unit rerouting cargo
across several countries."
  -> Pioneer [Normal]: Demonstrates exploring new paths and handling
     uncertainty.
  -> Activator [Normal]: Indicates moving to action quickly in a crisis
     situation.
```

Both genuinely distinct — one lens reads "initiates/drives," the other
reads "explores under uncertainty"; not a reworded duplicate in either
case. No near-duplicates found. But the real finding here is the
denominator: the schema's "no cap on links, link whatever the evidence
genuinely supports" design is barely exercised in practice — the model
defaults overwhelmingly to single-signature linking (avg 1.00–1.09
links/unit per persona, identical pattern held across all 6 rerun-stability
generations in section 5 below). This is the opposite of the brief's
anticipated failure mode (uniform *high* link counts / hedging) — the
real risk observed is the model barely using multi-linking at all, not
overusing it.

#### 2. Relevance calibration — real, confirmed miscalibration on the Weak band

Aggregate across all 6 personas' first-pass runs (79 total links):

```
Strong: 28/79 (35.4%)
Normal: 51/79 (64.6%)
Weak:    0/79 (0.0%)
```

Strong is not the dominant band (the brief's specifically-named failure
mode — "if most links end up Strong" — did not occur; Normal correctly
dominates, matching "most real evidence should land on Normal"). But
**Weak was never used once**, in any of the 6 personas' first-pass runs.
The rerun-stability check (section 5) adds 6 more independent real
generations across 3 of those personas — still 0 Weak links, out of
those 6 additional runs. This is not a one-off: 12 independent real
generations total, 0 Weak links. The model is not using the full 3-band
scale specified verbatim in the prompt; it collapses to an effective
Strong/Normal binary.

**Per this session's explicit standard, this is reported plainly as a
real, confirmed failure of the relevance-calibration verbatim wording as
specified — not iterated on with a fourth prompt variant this session.**

#### 3. Link count / hedging — no hedging observed, but see item 1

Per-persona average links/evidence-unit, first pass: katalin 1.00, matteo
1.07, nadia 1.00, tomasz 1.09, leona 1.00, ingrid 1.00. No sign of
uniform high link counts anywhere — if anything, the opposite (item 1).
Max links on any single unit, across all 6 personas' first-pass runs and
all 6 rerun-stability runs: 2. No hedging risk observed in this data.

#### 4. Computed score sanity vs. old model-asserted scores — partial, inconsistent agreement, with an important comparison caveat

Top-3-by-score overlap between v2's code-computed ranking and each
persona's real, previously-generated v1 `raw_signature_analysis.signatures`
ranking:

| Persona | v2 top 3 (computed) | v1 top 3 (model-asserted, real) | Overlap |
|---|---|---|---|
| katalin_farkas | Architect(15), Builder(15), Alchemist(10) | Alchemist(16), Optimizer(9), Builder(6) | 2/3 |
| matteo_varga | Builder(25), Visionary(15), Activator(9) | Originator(16), Builder(12), Architect(6) | 1/3 |
| nadia_petrescu | Builder(25), Optimizer(15), Architect(10) | Builder(16), Optimizer(16), Meaning Maker(9) | 2/3 |
| tomasz_zielinski | Activator(15), Pioneer(10), Architect(10) | Builder(20), Pioneer(12), Activator(12) | 2/3 |
| leona_markovic | Pioneer(25), Pattern Seeker(10), Meaning Maker(9) | Contextualiser(12), Pioneer(12), Optimizer(9) | 1/3 |
| ingrid_solberg | Builder(25), Depth Diver(20) *(only 2 signatures detected)* | Depth Diver(16), Builder(16), Pattern Seeker(9) | 2/2 |

Notable wild divergence: katalin_farkas's v2 run scores Architect at 15
(tied for #1, backed by 3 distinct real evidence units across Q3/Q9/Q12)
— but Architect doesn't appear anywhere in her real v1 signature list at
all. matteo_varga's v1 top signature (Originator, 16) never appears in
the v2 list either.

**Important caveat on this comparison, stated plainly rather than glossed
over:** this does not isolate the scoring *formula* from the *evidence
extraction* — v1 and v2 are different prompts run independently, so a
divergence can come from v2 extracting/linking evidence differently, not
just scoring it differently. A cleaner test (code-computed formula
applied to the *same* evidence set the live pipeline used) isn't possible
because Stage 0 confirmed that evidence set was never persisted. Given
that caveat, rankings show partial but inconsistent agreement — not the
"broadly agree" bar the brief was checking for, and not a clean pass
either.

#### 5. Rerun stability — real, substantial instability, consistent with and extending #93's precedent

3 personas (katalin_farkas, matteo_varga, tomasz_zielinski), 2 additional
rounds each (3 total runs per persona counting the first-pass run above,
6 new generations this check specifically — same input, `temperature: 0,
seed: 42`):

```
katalin_farkas:
  R1: 6 signatures, top: Architect(15)/Builder(15) tied, evidence_units=13
  R2: 8 signatures, top: Builder(15)/Optimizer(15) tied, evidence_units=13
  R3: 7 signatures, top: Architect(15)/Builder(15) tied, evidence_units=13
  (Guardian in R1+R3 but not R2; Contextualiser/Pioneer/Meaning Maker only
  in R2; evidence_unit count stable at 13, but which signature holds top
  score and the full signature set both vary)

matteo_varga:
  R1: Builder=25 (top), Visionary=15, Activator=9, evidence_units=14
  R2: Architect=15 (top), Visionary=10, Builder=10, evidence_units=14
  R3: Builder=20 (top), Activator=15, Visionary=15, evidence_units=15
  (Builder swings from top-scored to tied-3rd to top again across 3 runs;
  Architect appears at 15/top in R2 despite being entirely absent from
  R1's signature list)

tomasz_zielinski:
  R1: Activator=15 (top), Pioneer=10, Architect=10, evidence_units=11
  R2: Activator=20 (top), Pioneer=10, Builder=6, evidence_units=13
  R3: Activator=15 (top), Builder=9, Pioneer=6, evidence_units=11
  (most stable of the 3 — Activator holds top across all 3 rounds — but
  evidence_unit count still varies 11/13/11, and Builder/Pioneer/Architect
  swap rank each round)
```

Relevance distribution held the same pattern in every round of every
persona: 0 Weak links, always. Consistent with item 2's finding above,
now backed by 6 more independent generations.

**Not a full resolution of #93** (that would need a dedicated
investigation), but the observed instability here is real and, in
matteo_varga's case, more severe than #93's original finding — a
signature (Architect) going from entirely absent to tied-for-top and back
to absent, and a top signature (Builder) losing and regaining its #1 spot,
across 3 identical-input runs at `temperature: 0`.

#### Q11/Q12 concentration checks (Stage 0 measures 1 & 2, folded in per Miroslav's direction — run against Stage 1's freshly-generated real `evidence_units` instead of historical data, since none exists)

**Measure 1 — volume distribution by `source_question`:** NOT
concentrated in Q11/12. Summed across all 6 personas' first-pass runs (77
evidence units total): Q11 and Q12 combined account for 11/77 (14.3%) —
essentially the even-distribution baseline for 2 of 13 questions (15.4%),
slightly *under* it. Q4 was the single busiest question (10/77, 13.0%),
matching its "energy + frustration" weighting-rule role. Q8 and Q10 were
the least-cited (2/77 each).

**Measure 2 — `emotional_weight` disproportionately high for Q11/12:**
YES, confirmed, cleanly. Every evidence unit sourced from Q11 or Q12
across all 6 personas was tagged `emotional_weight: "high"` — 11/11
(100%), zero exceptions. Baseline `high` rate across all 77 evidence
units, all 13 questions: 31/77 (40.3%). Q11/12 evidence is tagged high
2.5× more often than the all-question baseline, with no noise in the
signal (11/11, not e.g. 9/11). This is a direct, real confirmation of
#112's original root-hypothesis half — and it's not a rogue extraction
bias, it's the prompt working exactly as designed:
`DETECTION_PROMPT_V2`'s (and the live `DETECTION_PROMPT`'s, unchanged)
own explicit instruction is "Q11-12 = strongest intensity signals" /
"Q11-12 must strongly influence intensity scoring." Combined with the new
code-computed Intensity formula (max relevance across a signature's
links), a signature whose only strong link happens to come from Q11/12
will reliably hit Intensity=5 by design, not by accident.

**Measure 4 — does generated prose disproportionately cite Q11/12
material:** still unanswerable this stage. This measure is about Layer
2's *prose* (`evidence_analysis`, `how_you_operate`, reframe bullets) —
Stage 1 only produces `evidence_units`, no narrative generation (Call 2 is
explicitly out of scope for this brief). Deferred to the Call 2 pilot
brief, not foldable into Stage 1's output.

### Recommendation

**Do not proceed to the Call 2 pilot with Stage 1's schema/formula as-is.**
Two real, confirmed findings meet this session's explicit "stop and report
plainly" bar rather than being iterated past:

1. Relevance calibration never uses the Weak band — 0/79 first-pass links,
   0/additional-stability-run links, 12 independent real generations
   total, 0 Weak. The 3-band scale specified verbatim in the prompt
   collapses to a 2-band scale in practice.
2. Run-to-run instability is real and, in at least one case
   (matteo_varga), more severe than #93's original finding — not just
   score magnitude drifting, but which signatures appear in the list at
   all changing between identical-input runs.

Item 4 (score sanity vs. old model-asserted scores) is a secondary,
softer finding given the stated comparison caveat, and would likely
improve somewhat once items 1 and 2 are addressed — not treated as
independently disqualifying, but not evidence of health either. Item 1
(lens differentiation) and item 3 (link count/hedging) are genuine,
clean passes, with the caveat that multi-linking is exercised too rarely
(2.6% of evidence units) to be confident the "no near-duplicate lenses"
result generalizes to a persona whose narrative genuinely produces more
cross-signature evidence.

The code itself (`lib/detection-engine-v2.ts`) is sound and kept on disk
as the standalone deliverable this brief asked for — the formula
implementation matches the brief's spec exactly and the module is fully
isolated from the live pipeline. What's not ready is the *prompt*
driving it (Weak-band calibration) and an open question about whether
Layer 1's underlying non-determinism (item 5) is something a scoring
redesign can route around at all, or whether it's a ceiling on how
reliable any Detection Engine v2 formula can be regardless of prompt
wording.

### Next
- Weak-band calibration and rerun instability are logged as open,
  confirmed findings for Miroslav's call on next steps — not
  re-attempted with different prompt wording this session, per this
  session's explicit instruction not to iterate blindly on a confirmed
  calibration failure.
- Measure 4 (Q11/12 citation bias in generated prose) stays open,
  deferred to the Call 2 pilot brief where Layer 2 prose actually exists
  to check.
- If a Call 2 pilot brief is written before Stage 1's findings are acted
  on, it should account for measure 2's confirmed finding (Q11/12 evidence
  structurally biased toward high Intensity by prompt design, not a bug)
  when interpreting whatever scores that pilot produces.
- `lib/detection-engine-v2.ts` stays unwired and untouched by anything
  else in the app, as required — no follow-up needed on that front unless
  a future stage explicitly picks it up.

## 2026-08-11 — #118, Stage 1b: Signature-first Detection Engine v3 (standalone)

Per `docs/briefs/118-stage1b-signature-first-detection-engine-v3.md`,
same-day follow-up to Stage 1 above. Standalone only — confirmed via
`git status --short` before writing this entry that the only file changed
is the new `lib/detection-engine-v3.ts` (plus this changelog entry and
scratch test scripts deleted before commit); `lib/detection-engine-v2.ts`,
`lib/generate-identity-report.ts`, `lib/prompts/*.ts`, and `app/` are
untouched. No negative/contradicting-evidence detection added, out of
scope per the brief. `npx tsc --noEmit` clean on all shipped files (one
type-strictness warning appeared in a disposable test script's `Set.has()`
call, not in `lib/detection-engine-v3.ts` itself, and didn't affect that
script's runtime output — the script was deleted after use regardless).

**Shipped, not wired in:** `lib/detection-engine-v3.ts` —
`DETECTION_PROMPT_V3` (signature-first: asks about all 25 signatures
explicitly, one call, requires evidence-or-empty-array for each, reuses
Stage 1's Strong/Normal/Weak wording verbatim per the brief) and
`computeSignatureScoresFromV3()` (same Frequency/Intensity/Score formula
as v2, unchanged, applied per-signature to `evidence` instead of
aggregating from cross-signature links). Also includes
`runDetectionEngineV3()`, a self-contained OpenAI call (its own client
instance, not routed through `lib/llm.ts`) so real token usage and timing
could be captured for check 6 without touching the shared helper the live
pipeline uses.

**Test method:** identical to Stage 1's, for direct comparability — same
6 personas, same real `discovery_answers`, `temperature: 0, seed: 42`,
main pass (1 generation per persona) plus rerun-stability on 3 of them
(`katalin_farkas`, `matteo_varga`, `tomasz_zielinski`, 2 more rounds
each). 12 real generations total this stage (6 main + 6 rerun), same
count as Stage 1.

#### 1. Structural stability — mixed, but the metric that matters most improved

Raw evidence-entry-count across 3 rounds, compared directly to Stage 1's
equivalent `evidence_units` counts for the same 3 personas:

```
                  v2 (Stage 1)        v3 (Stage 1b)
katalin_farkas    13 / 13 / 13        10 / 11 / 13
matteo_varga      14 / 14 / 15        13 / 13 / 13
tomasz_zielinski  11 / 13 / 11        10 / 11 / 13
```

Raw count variance is not uniformly better — matteo's got perfectly
stable (range 0, was range 1), but katalin's and tomasz's both got
*more* variable in raw count (range 3, was 0 and 2 respectively). The
brief's literal check-1 question ("does the shape vary less") does not
get a clean yes on this specific measure.

**But the non-empty-signature SET told a different, more encouraging
story.** katalin_farkas's set of signatures-with-any-evidence was
**identical across all 3 rounds** — the same 9 names every time
(Architect, Alchemist, Depth Diver, Contextualiser, Bridge, Pioneer,
Builder, Meaning Maker, Guardian) — versus Stage 1's 6/8/7 differently-
composed sets for the same persona. matteo_varga and tomasz_zielinski
both still had some tail movement (matteo: Visionary present R1 only,
Synthesizer present R1+R3 but not R2; tomasz's set grew 9→10→12 as
additional low-score signatures picked up weak evidence each round) —
but critically, in both cases the *newly appearing/disappearing*
signatures were always low-scoring tail entries (score 3 or 1), never
the top-ranked signature. See item 3 below for why that distinction is
the one the brief's check 3 actually cares about.

#### 2. Relevance calibration — real but marginal improvement, still fails the practical bar

Aggregate relevance counts, this stage's 12 generations (145 total
evidence entries):

```
Strong: 25/145  (17.2%)
Normal: 119/145 (82.1%)
Weak:     1/145  (0.7%)
```

Weak is no longer a hard zero — it appeared exactly once, in
tomasz_zielinski's Round 3 (`Guardian`, intensity=1, the one and only
Weak-rated piece of evidence across all 145). This is a real, measured
difference from Stage 1's flat 0/145 (0 Weak across the same 12-generation
methodology) — the structural hypothesis (removing the implicit
decide-to-extract pre-filter) does appear to have *some* real effect, it
is not purely noise.

**But 1/145 (0.7%) still fails the practical calibration bar by a wide
margin.** A genuinely working 3-band scale over 145 real, diverse
evidence-to-signature links from 6 different people's life stories should
produce more than one plausible-but-stretch judgment. This moved from
"structurally impossible" to "vanishingly rare" — not to "working."
**Per this session's explicit stop condition, this is reported plainly as
a real but insufficient improvement, not iterated on with a further
prompt variant this session.**

#### 3. Top-5/top-1 ranking stability — the matteo_varga-style flip did not recur, a clean and real finding

This is the check the brief specifically asked about by name. Top-ranked
(highest code-computed score) signature, all 3 rounds, all 3 tested
personas:

```
katalin_farkas:   Architect(10) -> Architect(10) -> Architect(15)   [same signature, all 3 rounds]
matteo_varga:     Architect(10) -> Architect(10) -> Architect(10)   [same signature AND same score, all 3 rounds]
tomasz_zielinski: Architect(6)  -> Architect(6)  -> Architect(6)    [same signature AND same score, all 3 rounds]
```

Reported plainly, not smoothed over: in Stage 1 (v2), matteo_varga's top
signature swung Builder(25) → Architect(15) → Builder(20), with Architect
entirely absent from R1's signature list before appearing tied-for-#1 in
R2. **Under v3, across 9 total round-observations (3 personas × 3
rounds), the top-ranked signature never changed identity, and for
matteo_varga and tomasz_zielinski specifically it never changed *score*
either.** No case of a signature going from entirely-absent to
top-ranked (or the reverse) was observed anywhere in this stage's data —
the specific failure mode the brief named did not recur.

One caveat stated plainly: all 3 tested personas' top signature landed on
**Architect** specifically, which could reflect a real, shared narrative
trait across this particular persona set (all 3 involve coordinating
complex physical/logistical situations — renovation, real-estate
development, crisis logistics) rather than confirming the *stability*
finding generalizes to a wider signature spread. The instability metric
being tested (does the top pick change across identical-input reruns) is
unaffected by this caveat — it did not change, for any of the 3 — but
which signature happens to win the pool wasn't independently varied by
this test.

#### 4. Full-25 coverage — clean pass, no exceptions

All 12 generations: `signatures` array length exactly 25, zero missing
names, zero unexpected/unrecognized names, zero duplicates. No
truncation risk either — max `completion_tokens` observed was 1789
(leona_markovic, main pass), well under the 8000 `max_tokens` cap used
for every call. This new design's specifically-flagged possible failure
mode (the model truncating or omitting some of the 25 under the larger
prompt) was not observed once.

#### 5. Q11/Q12 concentration — present, more pronounced than Stage 1

Summed across the 6 main-pass personas (71 evidence entries total): Q11
+ Q12 combined account for 18/71 (25.4%), above the 2-of-13-question
even-distribution baseline (15.4%) — a real, if moderate, concentration.
This is a different result from Stage 1's v2 design, which landed close
to baseline (14.3%). `emotional_weight: "high"` rate for Q11/Q12
evidence specifically: 16/18 (88.9%), versus 47/71 (66.2%) across all 13
questions — still elevated, but the gap is narrower than Stage 1's
result (100% vs. 40.3% baseline there) because v3's overall baseline
high-rate is itself much higher (66.2% vs. 40.3%) — v3 appears to tag
`emotional_weight: "high"` more liberally across the board, not only for
Q11/Q12. Also notable: Q6, Q9, and Q10 received **zero** evidence
entries across all 6 personas combined under v3 — every question had at
least some coverage under v2's design. v3's evidence appears to
concentrate more heavily on a handful of "big story" questions (Q3: 14/71
= 19.7%, Q4: 12/71 = 16.9%) rather than spreading across all 13 answers.

**Aside, not one of the 6 required checks but visible directly in the raw
output and worth a brief note:** 4 of 71 evidence entries in the main
pass (5.6%) used an invented `signal_types` value outside the schema's 4
valid categories (`"Direction Signals"` ×3, `"Turning Points"` ×1),
despite the prompt's explicit "do not invent any other signal categories"
instruction — all 4 instances were tied to Q13 (the "direction signals"
question per the weighting-rules section). This is not new to v3; the
same pattern was visible in Stage 1's raw v2 output too (its
katalin_farkas Q13 entry also used `"Direction Signals"`), so it's a
pre-existing, un-actioned schema-compliance gap, not something this stage
introduced.

#### 6. Real cost/latency numbers

All 12 real generations (main pass + rerun-stability), actually measured
via `response.usage` and wall-clock timing, not estimated:

```
prompt_tokens:      avg 2875.7   (min 2289, max 3751)
completion_tokens:  avg 1522.7   (min 1293, max 1789)
total_tokens:       avg 4398.3   (min 3729, max 5540)
latency:            avg 16920ms  (min 12221ms, max 27189ms)
```

No directly comparable v2 numbers exist — Stage 1's test script didn't
capture `response.usage` or timing, so this is v3's first real
measurement, not a side-by-side comparison. Per-call cost is well within
the same single-call-per-generation shape as the live pipeline's Layer 1
call (same 13 answers sent once); `max_tokens: 8000` was never approached
(max observed completion was 1789), so there's no truncation pressure at
the current 25-signature-explicit-prompt size.

### Recommendation

**Partial resolution of Stage 1's two blocking findings — genuine progress
on one, still a real failure on the other.** Per this session's explicit
stop condition, reported plainly rather than pursued with another wording
variant:

- **Top-ranking stability (the more severe of Stage 1's two findings,
  since it directly affects which signature a real person would see as
  their #1 result): resolved in this test.** The matteo_varga-style flip
  (absent → tied-#1 → absent) did not occur once across 9 round
  observations. This is the strongest single result of this stage and
  directly validates the brief's structural hypothesis for the dimension
  that matters most practically.
- **Weak-calibration: still fails.** 1/145 (0.7%) is a real, measured
  improvement over Stage 1's hard 0/145, confirming the structural
  pre-filter theory has *some* truth to it — but it is nowhere near a
  working 3-band scale. This blocking finding is not resolved, only
  softened.
- **Structural stability (check 1), read narrowly as "does the raw
  evidence count vary less":** no, not confirmed — 2 of 3 personas got
  *more* variable in raw count under v3. Read as "does the top-ranked
  signature stay identical," which is arguably what actually matters
  downstream: yes, cleanly, for all 3 personas tested.

**Given the mixed result, the recommendation is: v3's signature-first
structure is a real improvement over v2 and is the better foundation to
build on if/when Stage 2 proceeds — but Stage 2 (the Call 2 pilot) should
still wait, specifically on Weak-calibration, rather than proceeding on
v3 as-is.** The brief's own stop condition applies here: Weak-calibration
"still fails clearly" (0.7% is not a working calibration), so this is
reported as a stop, not iterated past with a fourth prompt attempt this
session. Unlike Stage 1 (where both blocking findings were essentially
unresolved), v3 has demonstrably fixed the more consequential of the two
— that's a real basis for choosing v3 over v2 whenever this work resumes,
just not yet a green light on its own.

### Next
- Weak-calibration is the single remaining blocker carried forward from
  both Stage 1 and Stage 1b — a different lever than prompt wording
  (e.g. explicitly requiring N Weak-tagged entries per signature evaluated,
  or a structural change to how the relevance question is asked) is
  probably needed; two consecutive wording-only attempts (Stage 1's
  extraction-then-link design and Stage 1b's signature-first redesign)
  have both landed in the same place on this specific dimension.
- `lib/detection-engine-v3.ts` stays unwired, alongside `lib/detection-engine-v2.ts`
  — both standalone, both untouched by the live pipeline, per this stage's
  stop conditions.
- The invented-`signal_types` aside (item 5) is logged for awareness only
  — pre-existing across v1/v2/v3, not actioned, not blocking.
- If Stage 2 planning resumes, this entry's recommendation (build on v3,
  not v2, but resolve Weak-calibration first) should be the starting
  point rather than re-litigating v2 vs. v3 from scratch.

## 2026-08-11 — #118, Stage 1c: Detection Engine v4 — separated inclusion/rating (standalone)

Per `docs/briefs/118-stage1c-detection-engine-v4-separated-rating.md`,
same-day follow-up to Stage 1b above. Standalone only — confirmed via
`git status --short` before writing this entry that the only files
changed are the new `lib/detection-engine-v4.ts` (plus this changelog
entry and scratch test scripts deleted before commit); `lib/detection-engine-v2.ts`,
`lib/detection-engine-v3.ts`, `lib/generate-identity-report.ts`,
`lib/prompts/*.ts`, and `app/` are untouched. No target Weak count/ratio
was added to the prompt at any point, per the explicit stop condition.
`npx tsc --noEmit` clean on all shipped files.

**Shipped, not wired in:** `lib/detection-engine-v4.ts` —
`DETECTION_PROMPT_V4`, identical to v3 except the combined "does the
evidence genuinely support this signature? If yes, cite and rate it"
instruction is replaced with two explicit sequential steps per signature
(Step A — inclusion, same bar as v3, not loosened; Step B — relevance
rating, independent of Step A, with the brief's exact
"do not use inclusion as a proxy for strength" instruction included
verbatim). Same output schema, same all-25-explicit single-call
structure, same Strong/Normal/Weak wording, same
`computeSignatureScoresFromV4()` formula as v2/v3, unchanged.

**Test method:** identical to Stage 1 and Stage 1b — same 6 personas,
same real `discovery_answers`, `temperature: 0, seed: 42`, main pass (6
generations) plus rerun-stability on `katalin_farkas`, `matteo_varga`,
`tomasz_zielinski` (2 more rounds each). 12 real generations total.

#### 1. Relevance calibration — real, monotonic improvement across all three stages, still low in absolute terms

Aggregate across all 12 generations (129 total evidence entries):

```
Strong: 30/129 (23.3%)
Normal: 96/129 (74.4%)
Weak:    3/129  (2.3%)
```

Direct three-stage comparison, same methodology every time:

```
v2 (Stage 1):   0/145 Weak (0.0%)
v3 (Stage 1b):  1/145 Weak (0.7%)
v4 (Stage 1c):  3/129 Weak (2.3%)
```

Weak usage more than tripled relative to v3, and this stage's main pass
alone (before any rerun) already showed 2 real Weak ratings for
`tomasz_zielinski` (14.3% of his 14 evidence entries that run) — both
shown in full:

```
Architect: "I rebuilt an outbuilding that didn't really need rebuilding."
  (Q7) relevance=Weak, lens="Shows a tendency to create structure even
  when unnecessary."
Guardian: "I live between an apartment in Gdańsk and an old farmhouse
  near the coast." (Q1) relevance=Weak, lens="Protects and maintains
  personal spaces and heritage."
```

A third Weak rating appeared in matteo_varga's rerun Round 2:
`Pattern Seeker: "I seem to need something concrete in front of me that
is imperfect but fixable." (Q5) relevance=Weak, lens="Identifies patterns
in problems that are fixable and seeks to address them."`

**Reported plainly: this is real, structurally-caused progress (three
independent design changes, three consistent directional improvements),
but 2.3% is still a low absolute rate for a genuinely working 3-band
scale** over 129 real, diverse evidence-to-signature links. Separating
inclusion from rating clearly helps — it does not, on its own, fully
resolve the underlying calibration issue.

#### 2. Populated-signature count — NOT clean, the named risk materialized at least once

Per-persona non-empty-signature counts, all rounds, compared directly to
Stage 1b's stated 8–12 baseline:

```
                  Main pass   Round 2   Round 3   (v3 baseline, main pass only)
katalin_farkas    10          8         8         9
matteo_varga       9         16         9         12
nadia_petrescu     5          —         —          8
tomasz_zielinski  13         12        10          9
leona_markovic    10          —         —         12
ingrid_solberg     9          —         —          9
```

Two real deviations outside the 8–12 baseline, reported plainly, not
averaged against item 1's improvement:

- **matteo_varga, Round 2: 16 populated signatures.** This falls inside
  the brief's own named danger zone ("toward 15–25") — the specific risk
  this check existed to catch materialized, at least once, in 1 of 12
  generations, despite Step A's inclusion wording being byte-identical to
  v3's. The newly-appearing signatures in that round (Visionary, Futurist,
  and others not present in matteo's main-pass or Round 3 results) were
  all low-scoring (score 3 or 1), not top contenders — but the count
  itself is a real, direct hit inside the range the brief flagged as
  concerning, not a borderline judgment call.
- **nadia_petrescu, main pass: 5 populated signatures** — below the 8–12
  baseline's floor. This isn't the specific loosening risk named in the
  brief (which was about counts rising, not falling), but it's still a
  real number outside the stated comparison range, reported for
  completeness rather than selectively omitted because it points the
  "wrong" direction.

The other 4 data points (katalin's 3 rounds, matteo's Round 3, tomasz's 3
rounds, leona, ingrid) all landed at or near the 8–12 baseline. This is
not a systemic collapse into all-25 — most runs stayed close to v3's
range — but it is a real, demonstrated instance of the named risk, not a
clean pass, and is reported here as its own finding rather than offset
against item 1's Weak-calibration gain.

#### 3. Top-ranking stability — held, no regression from v3's fix

Top-scored signature, all 3 rounds, all 3 tested personas:

```
katalin_farkas:   Alchemist(6) -> Alchemist(6) -> Alchemist(6)
matteo_varga:     Architect(10) -> Architect(5) -> Architect(10)
tomasz_zielinski: Architect(6) -> Architect(6) -> Architect(6)
```

No case of a signature going from entirely-absent to top-ranked (or the
reverse) anywhere in this stage's data. matteo_varga's top signature
(Architect) held its identity in all 3 rounds even though its score
wobbled (10 → 5 → 10, depending on whether a second corroborating
question was found that round) — a real magnitude fluctuation, but not
the absent-then-top flip the check is specifically testing for. v3's fix
for this specific failure mode carried over cleanly into v4; separating
inclusion from rating did not reintroduce it.

#### 4. Full-25 coverage — clean pass, no exceptions

All 12 generations: `signatures` array length exactly 25, zero missing
names, zero duplicates, every run (`missing=[]`, `duplicates=[]` in every
logged round). No truncation risk — `max_tokens: 8000` never approached
(max `completion_tokens` observed: 1965, matteo_varga Round 2).

#### 5. Lens differentiation — mostly clean, one recurring semantic-proximity pattern worth naming

Cross-signature evidence reuse (same quote cited by 2+ signatures),
main pass: 0 for katalin, tomasz, leona, ingrid; 2 for matteo_varga; 1
for nadia_petrescu. All 3 instances reviewed in full:

```
matteo_varga Q4: "I loved the beginning and middle of difficult
projects..."
  -> Architect [Strong]: Structures complexity by envisioning potential
     in dismissed projects and organizing resources to realize them.
  -> Activator [Normal]: Moves to action quickly by initiating and
     driving forward complex projects.
  (genuinely distinct: structuring vs. taking action)

matteo_varga Q5: "I'm at my best when the answer isn't obvious..."
  -> Architect [Strong]: Thrives in complex environments where multiple
     elements must be structured and aligned.
  -> Contextualiser [Normal]: Sees systems and context by understanding
     how various elements interact within a project.
  (close in theme — both about complex-systems — but distinct in focus:
   actively structuring/aligning vs. understanding/seeing context)

nadia_petrescu Q4: "I like making decisions with incomplete
information..."
  -> Architect [Strong]: Enjoys structuring complex systems to function
     effectively.
  -> Contextualiser [Normal]: Understands and organizes complex systems,
     seeing the broader context.
  (same Architect/Contextualiser pattern as matteo's Q5 case above)
```

No exact reworded duplicates found. But the Architect/Contextualiser
pairing recurred with a similar semantic flavor in 2 of the 3 instances
(both "structuring" vs. "understanding" framings of the same
complex-systems evidence) — not a failure, but a real, repeated pattern
worth naming rather than treating item 5 as an unqualified clean pass.

#### 6. Real cost/latency numbers

All 12 real generations:

```
prompt_tokens:      avg 3058.7   (min 2472, max 3934)
completion_tokens:  avg 1441.3   (min 1053, max 1965)
total_tokens:       avg 4500.0   (min 3726, max 5299)
latency:            avg 11917ms  (min 8010ms, max 17076ms)
```

Comparable to v3's baseline (avg ~4398 total tokens, avg ~16920ms) —
prompt size grew modestly (+6.4%, from the added Step A/Step B
instructions) but total tokens stayed essentially flat (+2.3%) and
latency was actually lower on average (~30% faster), likely reflecting
call-to-call variance rather than a systematic effect of the prompt
change.

### Recommendation

**Mixed, real result — genuine further progress on Weak-calibration, but
a real, demonstrated regression on populated-signature-count stability.
Per the explicit stop condition, these are reported as separate findings,
not netted against each other.**

- Weak-calibration: real, monotonic improvement (0.0% → 0.7% → 2.3%
  across v2 → v3 → v4), driven by a plausible mechanism (separating
  inclusion from rating) — but still a low absolute rate, not resolved.
- Top-ranking stability: v3's fix held, no regression.
- Populated-signature count: **not clean** — matteo_varga's Round 2 (16)
  landed inside the brief's own named risk zone (15–25), a real instance
  of the exact failure this stage's Check 2 existed to catch, alongside
  nadia_petrescu's main-pass 5 (below baseline in the other direction).
  Most runs (4 of 6 main-pass personas, all of katalin's and tomasz's
  rerun rounds) stayed close to the 8–12 baseline, so this isn't a
  systemic collapse — but it is real, not hypothetical, and not
  attributable to a wording change in Step A (which was byte-identical to
  v3's).

**Neither v3 nor v4 is a clean, unqualified basis for proceeding to Call
2 as-is.** v3 has the tighter, more reliable populated-count profile but
weaker Weak-calibration (0.7%). v4 has better Weak-calibration (2.3%) but
introduced a real, if intermittent, populated-count instability that v3
never showed across either of its own test rounds. Per this session's
standard (report plainly, don't smooth mixed results into a clean
narrative, don't attempt a fourth prompt variant this session), this
stage does not produce a "proceed" recommendation for either design.
Whether the matteo_varga Round 2 populated-count spike is genuine
run-to-run noise (in the spirit of #93's precedent) or a real, reproducible
consequence of the Step A/Step B split would need more rerun data than
this stage's budget covered (each persona only has 3 rounds total) to
distinguish with confidence — that uncertainty itself is reported here
rather than resolved by assumption.

### Next
- The populated-count spike (matteo_varga Round 2, 16/25) is the single
  most important open question for any future work on this line — is it
  reproducible or a one-off. Would need additional rerun rounds targeted
  specifically at matteo_varga (or a wider persona sample) to tell noise
  from a real, structural side effect of the Step A/Step B separation.
- No fourth prompt variant was attempted this session, per the explicit
  stop condition — Weak-calibration is real-but-incomplete progress, and
  Check 2 is not clean, so the conjunction that would justify continuing
  to iterate ("Weak-calibration still fails clearly AND populated-count
  is clean") does not hold either way; the honest state is genuinely
  mixed, not a clear signal to keep pushing this session.
- `lib/detection-engine-v4.ts` stays unwired, alongside `lib/detection-engine-v2.ts`
  and `lib/detection-engine-v3.ts` — all three standalone, all untouched
  by the live pipeline.
- If Stage 2 planning resumes, it should weigh v3 (cleaner Check 2,
  weaker Weak-calibration) against v4 (better Weak-calibration, one real
  Check 2 outlier) directly, rather than assuming v4 supersedes v3 —
  this stage's data does not support that assumption cleanly.

## 2026-08-11 — #118, Stage 1d + Stage 2: Matteo reproducibility reruns + Call 2 Analysis pilot

Per `docs/briefs/118-stage1d-stage2-matteo-rerun-and-call2-pilot.md`,
same-day follow-up to Stage 1c above. Standalone only — confirmed via
`git status --short` before writing this entry that the only files
changed are `lib/call2-pilot.ts` (new) plus this changelog entry and
scratch scripts deleted before commit; `lib/detection-engine-v2.ts`,
`lib/detection-engine-v3.ts`, `lib/detection-engine-v4.ts`,
`lib/generate-identity-report.ts`, `lib/prompts/*.ts`, and `app/` are
untouched. No prompt sections from `lib/prompts/identity-report.ts` were
rewritten — reused verbatim wherever quoted below. `npx tsc --noEmit`
clean on all shipped files.

### Part A — matteo_varga v4 reproducibility check

`lib/detection-engine-v4.ts`, completely unchanged, no prompt edits. 4
more rounds against matteo_varga (rounds 4–7, on top of Stage 1c's rounds
1–3) plus 3 more rounds against nadia_petrescu (rounds 2–4, on top of
Stage 1c's round 1). Populated-signature count, every round, no
averaging:

```
matteo_varga:   R1=9  R2=16  R3=9  R4=13  R5=17  R6=14  R7=16
nadia_petrescu: R1=5  R2=8   R3=6  R4=6
```

**Confirmed reproducible, not a one-off — and the reframing matters more
than the headline number.** matteo_varga's Round 2 spike (16/25) was not
an isolated event: across all 7 rounds, only 2 (R1, R3 — both 9) fall
within Stage 1b's 8–12 baseline. The other 5 of 7 rounds (71%) landed
*above* that baseline (13, 14, 16, 16, 17) — meaning the original "one
outlier at 16" framing from Stage 1c actually undersold the instability.
The low rounds are the unusual ones for this persona under v4, not the
high ones. nadia_petrescu's pattern is the mirror image and equally
reproducible: 4/4 rounds landed at or below the baseline floor (5, 8, 6,
6), never once reaching the 8–12 middle of the range.

Per the stop condition, this was a measurement pass only — no prompt
changes made regardless of outcome. This finding directly informed Part
B's scope decision below.

### Part B — Call 2 Analysis pilot

**Scope deviation from the brief, decided mid-session, stated plainly
here rather than silently applied:** the brief's Test Method called for
4 pilot generations (katalin_farkas and matteo_varga, each against both
v3 and v4 evidence). This was narrowed to **2 generations, v3 only**,
after Part A's fuller data confirmed v4's populated-signature-count
instability is real and recurring (71% of matteo's rounds landing outside
baseline), not a one-off as it looked after Stage 1c's 3 rounds alone.
Piloting Call 2 against an unstable evidence source would have confounded
this stage's actual question — does evidence-first input reduce
word-count shortfall and repetition — with a second, already-answered
question (is the evidence source itself stable). v3 was the more stable
design across both Stage 1b's and Stage 1c's own checks, so it carried
this pilot alone. **Consequence: Check 5 (v3-vs-v4 comparison) was not
run this stage** — there is no v3-vs-v4 empirical tiebreaker to report,
only a v3 result.

**Shipped, not wired in:** `lib/call2-pilot.ts` — `ANALYSIS_PILOT_PROMPT`
(reuses `lib/prompts/identity-report.ts`'s wording verbatim, unmodified,
for: GLOBAL STANDARDS' Tone/Tense/Voice, WRITING PRINCIPLE, THE
TRANSLATION FORMULA, THE EVIDENCE RULE, THE EVIDENCE REUSE RULE, THE
TENSION RULE, THE SPECIFICITY RULE, THE REPETITION RULE, THE FRUSTRATION
INVERSION RULE, THE NO COACHING RULE, THE NO GENERIC PRAISE RULE,
RECOGNITION TEST, evidence_analysis's exact "minimum 140 words, target
160 words" framing, how_you_operate's exact "Each minimum 120 words,
target 150 words" framing, and #82's full secondary compressed-comparative
format including its source-discipline and evidence-depth-tier rules —
scoped down to only the 3 fields this call is responsible for; cover,
constellation_synthesis, energisers/friction_points, domain_profile(_summary),
and reframe_teaser are out of scope, per the brief). `categorizeTop5Top3()`
(code, #110's existing top-5/next-3 pattern, applied to
`computeSignatureScoresFromV3`'s scores). `buildPilotSignatureInput()`
(per-signature name/score/frequency/intensity plus only Strong/Normal-rated
evidence — quote, source_question, lens; Weak excluded, per the brief:
"Weak evidence counts toward score, isn't written into prose").
**One disclosed schema deviation:** `primary_constellation`'s live
`confidence` field is dropped entirely — v3's computed scoring never
produces a confidence value, so there's nothing to "copy exactly," and
inventing one wasn't part of what this pilot is testing.

**Test method (as run):** katalin_farkas and matteo_varga, both already
measured against their real live reports earlier this session, each run
once through Detection Engine v3 (fresh call, `temperature: 0, seed: 42`)
then once through the Call 2 pilot. 2 pilot generations total.

#### 1. Word count vs. the live pipeline's own floors (140/160, 120/150)

```
                     evidence_analysis (floor 140)          how_you_operate (floor 120)
katalin_farkas       127, 110, 103, 98, 96                  93, 96, 79, 92, 97
matteo_varga         115, 104, 84, 90, 94                   100, 100, 95, 85, 93
```

**0/10 evidence_analysis entries clear 140. 0/10 how_you_operate entries
clear 120.** Same shape of universal shortfall the live pipeline has shown
throughout this project's history (#112 Stage 2: "0/15 samples cleared
either new floor") — evidence-first input did not close this gap.

#### 2. Word count vs. the real existing reports — mixed, not a uniform win

Real report word counts were re-measured directly from the live DB this
session (not assumed from the brief's rounded approximation), via
`primary_constellation`/`how_you_operate` on both personas' actual `ready`
`identity_report` artifacts:

```
Real evidence_analysis:  katalin 83,86,86,90,102 (avg 89.4) | matteo 74,76,81,89,93 (avg 82.6) | combined avg 86.0
Real how_you_operate:    katalin 86,89,93,99,99  (avg 93.2) | matteo 83,92,93,96,100 (avg 92.8) | combined avg 93.0

Pilot evidence_analysis: combined avg 102.1 (10 samples above)
Pilot how_you_operate:   combined avg 93.0 (10 samples above)
```

**evidence_analysis: real, measured improvement** — pilot average 102.1
words vs. the real reports' 86.0 (+18.7%). Still well under the 140
floor, but a genuine upward shift, not noise.
**how_you_operate: flat** — pilot average 93.0 vs. real 93.0. Identical
to one decimal place. Evidence-first input produced no measurable
difference on this field.

#### 3. Cross-section repetition — real regression, not an improvement

Same 4+-word literal-overlap detection as `constellation_synthesis`'s
existing backstop (`hasOverlappingPhrase`/`CONSTELLATION_SYNTHESIS_MIN_OVERLAP_WORDS`),
applied to primary `evidence_analysis` fields vs. each other and
`how_you_operate` fields vs. the primary cards and each other. To make
this a genuine apples-to-apples comparison rather than citing an
unrelated prior baseline, the identical detector was run against Katalin's
and Matteo's real reports' own `primary_constellation`/`how_you_operate`
text, same scope, same method, no new API calls:

```
                  Pilot (v3 evidence input)   Real live report (same scope)
katalin_farkas    135                         28
matteo_varga      59                          40
TOTAL             194                         68
```

**The pilot produced roughly 2.85x more 4+-word overlapping phrase
instances than the live pipeline's actual real output, in the same
field scope.** This is the opposite of the hoped-for result and is
reported plainly, not smoothed over. The single worst instance, quoted in
full: katalin's pilot `how_you_operate.thinking_style` and
`primary_constellation.Depth Diver.evidence_analysis` share a 19-word-long
consecutive run — `"historical and technical details... [to] uncover
insights that others might overlook, contributing to the richness and
accuracy of your work"` — appearing in both fields almost verbatim. Both
the real and pilot prompts contain the identical banned-filler-phrase
list ("is evident in your," etc.) verbatim, and both outputs still use
those exact banned phrases repeatedly — consistent with this project's
established finding (#112) that filler-phrase banning doesn't reliably
hold regardless of prompt wording or evidence input shape.

#### 4. Evidence Reuse Rule compliance / fabrication spot check

Both full pilot reports (16 signature write-ups total: 5 primary + 3
secondary × 2 personas) were manually checked line by line against the
exact Strong/Normal evidence entries each signature was actually given.

**Largely faithful — 14 of 16 checked cleanly**, direct traceable matches
between each `evidence_analysis`/`tension`/secondary `analysis` claim and
its specific given evidence, no invented facts found in either report.

**One real, concrete likely violation found:** katalin_farkas's Architect
`evidence_analysis` — given evidence was only "I loved taking something
tangled and making it workable" (Q3) and the building-renovation-budget
quote (Q12) — but the output states "...as seen in your satisfaction from
turning **neglected spaces** into structured environments." The word
"neglected" appears nowhere in Architect's given evidence; it's a
specific, distinctive word from **Builder's** given evidence ("I bought
and renovated a **badly neglected** 1930s apartment"), and Builder's
evidence sat directly beside Architect's in the same request payload.
This looks like real cross-signature evidence blending inside a single
combined call, despite each signature's evidence being logically scoped
in the input structure — the model still had every other signature's
evidence in the same context window and drew on it anyway.

**One minor paraphrase-drift instance, not evidence-borrowing:**
matteo_varga's Visionary evidence_analysis says "neglected buildings"
where the actual given quote said "ugly building everyone else had
dismissed" — a word substitution, not a fact pulled from elsewhere (no
other signature's evidence for Matteo used "neglected").

#### 5. v3-vs-v4 evidence comparison

**Not run this stage** — see the scope deviation noted above. v4 was
excluded from this pilot before any Call 2 generation, so there is no
empirical tiebreaker to report between the two Detection Engine designs
from this stage's data.

#### Cost/latency, incidental (not a named check, reported for completeness)

```
                  Detection v3 tokens/latency        Call 2 pilot tokens/latency
katalin_farkas    4231 tokens / 13716ms              6055 tokens / 29491ms
matteo_varga      4700 tokens / 13181ms              5962 tokens / 41511ms
```

A 2-call pipeline (Detection + Analysis) currently runs ~43–55 seconds
combined latency per person for just these two calls, real numbers from 2
real end-to-end runs — relevant to any future decision about a multi-call
redesign's total latency budget, not measured or estimated before now.

### Recommendation

**Genuinely mixed — evidence-first input helped on one of #118's two
target metrics and hurt on the other. Reported plainly, per the explicit
stop condition, rather than forced into a clean win or a clean rejection.**

- Word count (evidence_analysis specifically): real, positive movement.
  +18.7% over the live pipeline's actual current output. This is real
  progress on half of what #118 exists to fix.
- Word count (how_you_operate): no effect. Flat to one decimal place
  against the real reports.
- Cross-section repetition: real, negative movement. Roughly 2.85x more
  same-scope 4+-word overlap than the live pipeline's actual current
  output — the opposite of what this redesign was hypothesized to fix,
  and arguably the more important of the two metrics given #112's
  original finding was specifically about repetition, not only length.
- Fabrication/evidence-reuse discipline: mostly holds, but a real,
  concrete cross-signature-evidence-blending instance was found on the
  first full manual spot-check attempted — not a rare, hard-to-trigger
  edge case.

**This does not clear the bar for "carry this redesign further into
`path_options`/`path_plan`'s equivalent generation, or into live
wiring."** One of two target metrics moved backward, not forward, and the
v3-vs-v4 tiebreaker this stage was also supposed to settle wasn't run at
all, per the disclosed scope deviation. The honest state after Stage 2 is
that evidence-first input is not a proven improvement — it is a
real, if partial, data point that trades one problem for a different one.

### Next
- The repetition regression (item 3) is the most important open question
  before any further work on this line — worth understanding *why*
  scoped-down evidence produced more repetition, not just that it did,
  before attempting a fix. One plausible lever: the pilot's trimmed prompt
  removes surrounding sections (cover, constellation_synthesis,
  reframe_teaser) that in the live pipeline may incidentally absorb some
  of the content that would otherwise get restated in how_you_operate —
  untested, a hypothesis for a future session, not chased here per this
  session's no-fourth-variant discipline extended to this new finding too.
- The cross-signature evidence-blending instance (item 4) suggests that
  scoping evidence per-signature in the input JSON isn't sufficient on its
  own to guarantee source discipline when all signatures still share one
  request/context window — a real, mechanical constraint worth remembering
  before assuming input-shaping alone solves attribution.
- v4's confirmed-reproducible populated-count instability (Part A) is now
  a settled finding, not provisional — any future work choosing between
  v3 and v4 should treat this as closed, not worth another reproducibility
  round.
- `lib/call2-pilot.ts` stays unwired, alongside `lib/detection-engine-v2/v3/v4.ts`
  — all standalone, all untouched by the live pipeline.
- This brief is not deleted — #118 remains open, and this stage's mixed
  result is not a stopping point that resolves the ticket either way.

## 2026-08-11 — #118, Stage 1e: Detection Engine v5 — three targeted fixes on v3 (standalone)

Per `docs/briefs/118-stage1e-detection-engine-v5-reused-evidence-discipline.md`.
Standalone only — `lib/detection-engine-v5.ts` new, `lib/detection-engine-v2/v3/v4.ts`
untouched. Extends v3 with three additions: Fix 1 (deduplication before
extraction — merge same-event evidence into one unit rather than splitting
across differently-worded copies), Fix 2 (equal bar for every link,
regardless of order — judge each link on an evidence_unit independently),
Fix 3 (broader candidate scan before finalizing links — check against the
full set of already-found signatures, not just the first one or two that
come to mind). Same 6 personas, real `discovery_answers`, `temperature: 0,
seed: 42`, main pass only, no rerun-stability pass.

**Direct before/after check on 4 specifically-flagged cases from this
session's earlier free measurement passes:**
- Tomasz's Architect score (previously doubled by bundling a genuine
  crisis-logistics instance with a hands-on-labor quote): resolved, but
  not attributably — Architect wasn't extracted at all this run, an
  extraction gap rather than a judged-and-rejected link.
- Leona's fabricated Architect entry (previously built from zero
  genuinely qualifying evidence): resolved, and attributably — Architect
  doesn't appear in her populated set at all.
- Katalin's doubled score (Q3 "tangled...workable" bundled with genuine
  Q12): unchanged — identical composition, identical score (freq=2,
  score=10).
- Ingrid's doubled score (watchmaker-training + workshop-opening split
  across Architect/Originator/Pioneer): resolved and attributably — the
  two clauses merged into one Builder evidence_unit rather than
  fragmenting across three signatures.

Rating-quality re-check (15 entries, Strong/Normal — v5 produced zero
Weak, noted factually per the stop condition, not chased as a target):
3/15 disagreements (20%, vs. today's 46% baseline), but 2 of 3 are the
exact named mechanisms recurring verbatim on the same quotes (Matteo's
Architect/Contextualiser Q5, Originator/Pioneer Q3) — the lower rate was
explicitly not read as success. Linking-quality re-check: 2/5 multi-link
groups (40%, statistically similar to the 4/10 baseline) still show the
targeted inflation, same two Matteo pairs, unresolved. Deduplication check
(new this stage): no near-duplicate paraphrase-window splits found
anywhere — Katalin's Bridge link now shares Architect's full Q12 quote
rather than a truncated substring; Ingrid's training+workshop clauses
merged into one unit. Fix 1 worked cleanly in both testable cases.

**Recommendation:** Fix 1 (dedup) works. Fix 3 (broader scan) shows real,
mixed value. Fix 2 (equal bar) is inconsistent — works on Katalin's
Bridge/Guardian additions, fails identically on Matteo's two flagship
pairs. Not trustworthy enough yet to resume Call 2 work on top of it.

## 2026-08-11 — #118, Stage 1f: Detection Engine v6 — deliberately minimal prompt (standalone)

Per `docs/briefs/118-stage1f-detection-engine-v6-minimal-prompt.md`. Tests
whether prompt complexity itself, not any specific missing instruction, is
contributing to the reused-evidence/redundant-link problem.
`lib/detection-engine-v6.ts` strips v3 down to the domain/signature list,
output schema, three-tier relevance calibration, and one plain honesty
instruction — cutting Hard Constraints, Question Weighting, the
Conflicts-list, and the multi-step breakdown. None of Stage 1e's three
fixes added, deliberately kept as a separate experiment.

Real results, same 6 personas: full-25 coverage clean. Weak-calibration:
0/90 evidence entries Weak across all 6 personas — worse than v3's
already-thin 0.7%, a real regression. Populated-signature count up ~40%
on average vs. v3 (13.8 vs 9.8), consistent with looser, broader inclusion
once Hard Constraints/Conflicts-list were removed. Multi-link/reuse: 14
groups across 90 entries (vs. v3's 10/71) — more reuse, not less, and
larger groups (up to 4-way on one quote, vs. v3's max 3-way).
Rating-quality re-check: 4/16 disagreements (25%), including two new
self-report/preference-as-evidence failures (Pattern Seeker credited on
plain "I enjoy X" statements) plausibly tied to removing "ignore
aspiration/self-report." Linking-quality re-check: missed-link rate ~67%,
essentially unchanged from v3; 10/14 (71%) multi-link groups showed
inflation, worse than v3's 40% — Architect specifically recurred as the
attractor signature in inflated links a 3rd and 4th time this session
(Leona's "assumption that changes everything," Ingrid's watchmaker-training
3-way).

Cost: prompt tokens -15.4% as expected from a shorter prompt, but
completion tokens +17.8%, total tokens -4.1% (roughly flat), latency
+32.4% (worse, likely largely a single outlier run, reported plainly per
the stop condition against reading "shorter" as automatically better).

**Recommendation:** simplifying the prompt did not help and made the
linking-inflation problem measurably worse. Stage 1e's targeted fixes are
needed regardless of prompt density; complexity itself was not the driver
of the Architect over-extension pattern.

## 2026-08-11 — #118, Stage 1g: Detection Engine v7 — forced two-phase link review, single call (standalone)

Per `docs/briefs/118-stage1g-detection-engine-v7-forced-two-phase-review.md`.
Extends v5 (keeps Fix 1 dedup + Fix 3 broader scan), replaces Fix 2's
self-applied instruction with a forced two-phase output structure:
`draft_signature_links` (v5-equivalent extraction, no filtering) then a
separate, explicit `reviewed_signature_links` phase reviewing the draft —
testing whether visibly separating "extract" from "review" inside one
completion closes the gap Fix 2 alone didn't.

Matteo's two flagged cases, quote-level: both byte-identical between
draft and reviewed (Architect/Contextualiser Q5 both Strong/Strong
unchanged; Originator/Pioneer Q3 both Strong/Strong unchanged) — zero
effect. Across all 6 personas: 84 draft links → 77 reviewed (7 removed,
8.3%), zero rating changes anywhere, 3 of 6 personas (Tomasz, Leona,
Ingrid) showed zero review activity at all. Where review did act
(Katalin, 4 removed), it was indiscriminate — stripped a well-supported
link (Bridge on Q12, Guardian on Q11, previously found sound in v5)
exactly as readily as a weak one (Pioneer on Q3), rather than
distinguishing.

Fix 1 and Fix 3 continued to show value (Ingrid's merge, Katalin's new
sound multi-links). Cost: total tokens +49.3%, latency +144.6% vs. v5's
baseline — roughly 2.5x for a mechanism that changed nothing on the
motivating cases.

**Recommendation:** per the stop condition, this is evidence the problem
needs an actual separate second call, not further single-call
restructuring — a model committing to structured JSON doesn't reliably
hold a critical stance toward its own just-written output even when that
output is explicitly labeled for review.

## 2026-08-11 — #118, Stage 1h: Detection Engine v8 — genuinely separate review call (standalone)

Per `docs/briefs/118-stage1h-detection-engine-v8-separate-review-call.md`.
Two real, separate completions: Call A (v5's structure minus Fix 2) then
Call B, a cold second call receiving only Call A's finished `signatures[]`
output — no discovery_answers, no shared context, explicit instruction not
to assume the original links were reasoned well.

Result: 0 of 59 links changed, removed, or added across all 6 personas —
complete rubber-stamp, both on Matteo's two flagged cases (identical
Strong/Strong on both pairs) and everywhere else checked (Katalin's
Alchemist/Meaning Maker and Bridge/Guardian on Q11, Ingrid's
Pioneer/Builder on Q2, Leona's Alchemist/Truth Seeker on Q4 — all
untouched). Worse than v7's inconsistent-but-nonzero activity. Cost:
combined Call A+B averaged +59.6% tokens, +100.7% latency (roughly double)
vs. v5's single-call baseline.

**Recommendation, applying the explicit pre-agreed cost bar:** does not
clear "meaningfully better than v5" — strictly worse on the metric that
matters, at double the cost. This rules out shared-context/
self-defensiveness as the mechanism (a cold, adversarially-instructed
reviewer still accepted everything). Per the stop condition, no third
call proposed; the diagnosis offered instead (not a next-step to build)
was that per-link "judge independently" framing may itself prevent the
comparison that would expose a redundant pair, since each individual
link's lens reads as reasonable in isolation.

## 2026-08-11 — #118, Stage 1i: Detection Engine v9 — direct pairwise comparison of co-linked evidence (standalone)

Per `docs/briefs/118-stage1i-detection-engine-v9-pairwise-comparison.md`.
Single-call test (per the stop condition, not a fourth call-splitting
variant) — extends v5, replaces Fix 2 with an instruction to compare each
pair of co-links on the same evidence directly against each other, not
independently, and collapse pairs that are "the same observation wearing
different labels."

Matteo's two flagged cases: both survive unchanged (Architect/
Contextualiser Q5 both present, Strong/Normal; Originator/Pioneer Q3 both
present, Strong/Normal) — 4th consecutive failure on the same two cases
across v5/v7/v8/v9. Across all 6 personas, only 6 multi-link groups formed
this run (down from 10-14 in prior runs, likely ordinary extraction
variance, not the mechanism suppressing linking). Of those 6, independently
judged: 3 genuinely redundant or near-redundant (Matteo Q5, Matteo Q3, and
a newly-surfaced case, Leona's Synthesizer/Bridge on Q12 — "combines data
with physical observation" vs. "connects data-analysis with engineering
insights," the engineer being the physical-observation source in that
quote, i.e. the same act described twice), 1 genuinely distinct (Matteo's
Architect/Bridge on Q12), 1 defensible 3-way (Matteo's Q4), 1 borderline
(Leona's Alchemist/Truth Seeker on Q4). v9 removed zero links from any
group, anywhere — including the 3 independently-judged-redundant ones.
Cost landed within 1-2% of v5's baseline, as expected for a single-call,
modest-instruction-addition test — ruling out cost as the reason it didn't
help.

**Recommendation, per the stop condition:** this still fails to
discriminate redundant pairs from distinct ones. Per Miroslav's direction,
this is now his call, not a further prompt-wording attempt on the same
underlying idea — four structurally different mechanisms (self-applied
rule, forced two-phase single call, genuinely separate cold call, explicit
pairwise comparison) have all failed identically on the same two
motivating cases.

### Next (Stages 1e–1i)
- Fix 1 (deduplication) and Fix 3 (broader candidate scan) are validated
  as real, working improvements across v5/v7/v8/v9's tests — see the
  correction entry below for a narrowing of that validation.
- Fix 2 (equal bar / link review, under any of four tested mechanisms)
  remains unresolved. Per Miroslav's direction, no fifth prompt-wording
  or call-architecture variant to be attempted without a new hypothesis —
  see the correction entry below for what was actually being tested.
- `lib/detection-engine-v5/v6/v7/v8/v9.ts` all stay unwired, standalone,
  alongside `lib/detection-engine-v2/v3/v4.ts`.
- None of these briefs are deleted — none of this work was live-verified
  (no user-facing path exists to verify against), so the AGENTS.md
  docs/briefs cleanup trigger does not apply.

## 2026-08-11 — #118, Correction to Stages 1e–1i: one of the two "flagship" motivating cases was not a defect

Reviewed directly with Miroslav after Stage 1i. This is a correction, not
a new stage — no new code, no new test run.

**Matteo's Architect/Contextualiser Q5 pair was misdiagnosed as redundant
linking across all four stages (1e, 1g, 1h, 1i).** Re-examined directly:
Architect ("thrives in environments where multiple complex elements need
to be structured and aligned") and Contextualiser ("sees the broader
context and systems involved") genuinely capture two distinct real
aspects of the same quote ("several different things have to come
together — money, design, people, regulations, practical constraints")
— one reading the active structuring/integration, the other reading the
systemic breadth. This was treated as one of two flagship motivating
cases for the entire equal-bar-for-every-link investigation; it should
not have been.

**Originator/Pioneer Q3 is a real issue, but not the one being tested.**
Re-diagnosed: this is not a redundant-second-link problem — it's Pioneer
being stretched onto evidence ("starting our own company... no
institution behind us") that doesn't show genuine novelty-to-the-field,
closer in kind to the existing Builder/Architect/Originator conflict this
session already named and tried to handle via the Conflicts-list
mechanism (never fully working, per Stage 1b's own finding) than to a
per-link "does this evidence support two things" review. Fixing it needs
a different lever than any of the four tested this session.

**Net effect on Stages 1e/1g/1h/1i's conclusions:** the outcome — "failed
to fix the two flagship cases" — is factually correct in every stage's
real captured output (confirmed, unchanged, quote-level, every time). But
the benchmark itself was half-wrong: only one of the two cases was a
genuine instance of the problem the fixes were meant to solve, and that
one case was misclassified as the same kind of problem as the other.
**Fix 2 (equal bar / link review) has not actually been tested against a
correctly-diagnosed redundant-link case and should not be treated as a
closed, fully-explored dead end** — it was explored against a flawed
benchmark. Deduplication (Fix 1) and broader-candidate-scan (Fix 3) remain
validated on their own terms; this correction doesn't touch their
evidence directly, but see below for a separate, undiscussed risk that
does.

**Separately, an undiscussed risk raised by Miroslav, logged as open, not
resolved:** both "validated" fixes (dedup, broader-scan) work by pooling
more evidence under fewer, richer, multi-linked entries. That pooling
increases pressure on the model to invent a distinguishing detail — a
lens describing something not actually in the quote — whenever multiple
signatures share one piece of evidence and each needs its own angle. This
was observed once, in passing, during this session's very first
rating-quality check (Katalin's Alchemist lens claiming "transforming
stalled negotiations" — a detail absent from the actual quote, which
never mentions negotiations) and was never named as a risk tied to
dedup/broader-scan specifically until tonight. Neither fix's verification
checks (Stages 1e/1g/1h/1i) tested for this. Logged as an open, unresolved
concern for both fixes — not a settled win, regardless of whether these
engine versions are ever revisited.

### Next
- Fix 2 (link review) is open, not closed — a correctly-diagnosed
  redundant-link benchmark is needed before any further attempt or before
  concluding per-link review can't work.
- Fabrication-under-pooling risk (dedup + broader-scan) is open and
  unchecked — needs its own verification pass (does dedup/broader-scan
  measurably increase invented-detail rate vs. v3 baseline) before either
  fix is called settled.
- This correction stands regardless of whether
  `lib/detection-engine-v5/v6/v7/v8/v9.ts` are ever revisited or wired in.
