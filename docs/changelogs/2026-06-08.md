# 2026-06-08

## Changed

**Part 1 of the docs reorganisation** (per `docs/briefs/docs-reorg.md`). Docs-prose only — no code modified.

### Terminology / labels
- Retired all "Layer N" references from docs. Service-model tiers now use canonical user-state labels: **Anonymous**, **Registered**, **Paid (one-time)**, **Subscriber** (established in `product-decisions.md`).
- Translation Formula "Layer 1/2/3" prefixes dropped; the three elements are now **Pattern**, **Evidence**, **Meaning** throughout.
- "Primary Constellation" → **Primary Signatures** (display label); "Domain Profile" → **Identity Profile**. Data contract field names (`primary_constellation`, `domain_profile`) unchanged.
- Identity Profile radar domain order corrected to canonical: Visioning, Thinking, **Connecting, Driving, Sensing**.

### Single-source cleanup
- `identity-signature-report.md` is now the authoritative report spec; structural spec removed from `zyrro_identity_report_blueprint_v_1.md` (method/philosophy only remains).
- `zyrro_detection_engine_spec_v_1.md` marked as canonical source for the 25 signature taxonomy; pointer added to `zyrro_named_identity_system_v_1.md` and `identity-signature-icons.md`.
- `lib/artifact-schemas.ts` referenced as canonical field set in `zyrro_path_plan_blueprint_v_1.md`.
- `AGENTS.md` confirmed as single home for git + session protocol; Git and Session Continuity sections removed from `coding-standards.md` and `product-decisions.md`.

### Word counts removed
All word-count / length numbers removed from: `identity-signature-report.md`, `zyrro_identity_report_blueprint_v_1.md`, `zyrro_narrative_transformation_rules_v_1.md`, `zyrro_path_plan_blueprint_v_1.md`. Section shape is described; the prompts own the numbers.

### CSS variable references
- `branding-guidelines.md`: Colors table, gradient spec, typography weights/sizes, spacing/layout, and all Component patterns literals replaced with `globals.css` var references (`--color-*`, `--gradient`, `--font-weight-*`, `--font-size-*`, `--radius-*`, `--shadow-*`, `--spacing-*`). Literals with no matching var left in place (see below).
- `coding-standards.md`: brand gradient literal and font-weights list removed; point to `branding-guidelines.md` and `globals.css`.
- `identity-signature-icons.md`: shield gradient literal replaced with `--gradient` reference.

### product-decisions.md sync
- User States updated to built reality: Paid (one-time) entitlement enforced (`lib/entitlements.ts`, `product: 'onetime_payment'`); `/path` and `/plan` are live. `/dashboard` still placeholder. Subscriber tier label established; no subscription gating built (Stripe checkout is one-time `mode: 'payment'` only).
- URLs section updated; `app/` noted as source of truth for routes.
- `app/api/mentor/route.ts` key-source description rewritten in user-state terms (guest → Registered upgrade).
- `zyrro_path_plan_blueprint_v_1.md` added to Framework Reference list.

### Retired
- `docs/standards/identity-questions.md` — `git rm`. `lib/identity-questions.ts` is the single source. All cross-references cleaned up.

### Named identity output format
- `zyrro_named_identity_system_v_1.md`: Output Format examples corrected to title case; note added that display uppercasing is presentational (`globals.css` `.named-identity` rule). Stray `n` typo under Pattern Seeker removed.

---

## Decisions

- **One-time tier label**: **Paid (one-time)** (from `zyrro_path_plan_blueprint_v_1.md`'s existing usage), applied wherever service-model "Layer 3" appeared.
- **Translation Formula**: "Layer N" prefix dropped entirely; Pattern / Evidence / Meaning stand alone — no "Step" rename.
- **`artifacts.status` provenance**: doc kept provenance-neutral; committed migrations do not yet create this column — to be confirmed separately.
- **Literals without vars**: the following one-off values have no matching `globals.css` var and were left as literals in `branding-guidelines.md`: `0.5px solid rgba(0,0,0,0.07)` (card and nav borders), `2px solid rgba(255,255,255,0.4)` (primary button border), `rgba(0,0,0,0.06)` / `1px solid rgba(0,0,0,0.10)` (secondary button bg/border), `rgba(255,255,255,0.85)` / `rgba(255,255,255,0.95)` (icon colours with opacity), badge `80x88px` / `28px` dimensions.

---

## Next

**Part 2 of the docs reorg is pending** (`docs/briefs/docs-reorg.md` — code task, separate session):

1. `/identity` page: Section-4 eyebrow still renders `PRIMARY CONSTELLATION` → needs to change to `PRIMARY SIGNATURES` (`app/identity/page.tsx`).
2. Shared 25-signature taxonomy const: a single source that both `lib/prompts/identity-analysis.ts` and the `SIGNATURE_ICONS` map in `app/identity/page.tsx` import, so in-code copies can't drift.
3. Optional `LAYER_2_*` symbol renames in `lib/prompts/identity-report.ts`, `app/api/mentor/route.ts`, and `app/mentor/page.tsx` (all resolved mappings documented in the brief).

**Intentional, temporary docs-vs-code mismatch until Part 2 runs:** docs now say "Primary Signatures" while the `/identity` Section-4 eyebrow still renders `PRIMARY CONSTELLATION`; docs use Registered/Paid tier language while code symbols are still `LAYER_2_*`. Expected and tracked in Part 2.
