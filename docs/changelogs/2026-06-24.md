## 2026-06-24 — Session 1

### Changed
- Added migration `20260624000000_drop_redundant_artifacts_rls_policies.sql` and applied it to the live DB, dropping the two duplicate RLS policies on `public.artifacts`:
  - `"Users can insert their own artifacts"` (duplicate of `"Users can insert own artifacts"`)
  - `"Users can view own artifacts"` (duplicate of `"Users can read their own artifacts"`)
- Verified before and after via `pg_policies`: four policies remain, one each for DELETE, INSERT, SELECT, UPDATE.

### Decisions
- Kept the `… own artifacts` naming style (matches DELETE/UPDATE policies); the `… their own …` / `… view …` variants were the ones dropped.
- Baseline migration `20260531000000_artifacts_baseline.sql` left untouched — it is a historical point-in-time capture of the live DB and must remain as-is.

### Next
- Relocate `set_updated_at` trigger (separate task, flagged as do-not-touch in this brief).
