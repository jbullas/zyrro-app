## 2026-06-24 — Session 1

### Changed
- Added migration `20260624000000_drop_redundant_artifacts_rls_policies.sql` and applied it to the live DB, dropping the two duplicate RLS policies on `public.artifacts`:
  - `"Users can insert their own artifacts"` (duplicate of `"Users can insert own artifacts"`)
  - `"Users can view own artifacts"` (duplicate of `"Users can read their own artifacts"`)
- Verified before and after via `pg_policies`: four policies remain, one each for DELETE, INSERT, SELECT, UPDATE.

### Decisions
- Kept the `… own artifacts` naming style (matches DELETE/UPDATE policies); the `… their own …` / `… view …` variants were the ones dropped.
- Baseline migration `20260531000000_artifacts_baseline.sql` left untouched — it is a historical point-in-time capture of the live DB and must remain as-is.

### Next
- Relocate `set_updated_at` trigger (separate task, flagged as do-not-touch in this brief).

---

## 2026-06-24 — Session 2

### Changed
- Created `supabase/migrations/20260530000000_shared_functions.sql` housing `public.set_updated_at()` as the single canonical definition.
- Removed the function definition and its TODO comment from `20260531000000_artifacts_baseline.sql`; updated that file's prerequisite comment to reference the new migration.
- `set_artifacts_updated_at` trigger left in place; no live-DB changes made.

### Decisions
- Pre-dated the shared-functions migration to `20260530…` so it sorts before the artifacts baseline (`20260531…`) in migration order — a today-dated migration would break fresh builds because the baseline's trigger references the function.
- Did not apply `20260530000000_shared_functions.sql` to live DB — it is a `CREATE OR REPLACE` over an identical existing function (a true no-op), so applying it is optional.
- Did not `DROP` the function anywhere — live triggers on `profiles` and `conversations` depend on it.

### Next
- Full-schema capture for `profiles` and `conversations` tables (dashboard-created, not yet in migrations).

---

## 2026-06-24 — Session 3

### Changed
- Added `20260530000001_profiles_baseline.sql` — captures `public.profiles` (columns, PK, plan CHECK, FK→auth.users ON DELETE CASCADE, RLS enabled, 3 policies: INSERT/SELECT/UPDATE, BEFORE UPDATE trigger calling `public.set_updated_at()`).
- Added `20260530000002_conversations_baseline.sql` — captures `public.conversations` (columns, PK, status CHECK, FK→profiles ON DELETE CASCADE, `idx_conversations_user_id`, RLS enabled, 4 policies: DELETE/INSERT/SELECT/UPDATE, BEFORE UPDATE trigger).
- Updated the prerequisite comment in `20260531000000_artifacts_baseline.sql` to reference the two new migration filenames by name.
- Investigated `public.memory` (read-only, no migration written — see Decisions).

### Decisions
- `profiles` first (`20260530000001`), `conversations` second (`20260530000002`): driven by the FK constraint `conversations.user_id → profiles(user_id) ON DELETE CASCADE` — the referenced table must exist first.
- Both pre-dated into the `20260530…` window so the fresh-build order is: shared_functions → profiles → conversations → artifacts.
- Idempotency pattern matched exactly to the artifacts baseline: `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, DO-block-wrapped policies, `CREATE OR REPLACE TRIGGER`.
- Neither baseline applied to the live DB — `IF NOT EXISTS` makes both a no-op since the tables already exist. Capture is a repo concern only.
- **`memory` disposition pending human decision.** Findings:
  - Shape: `user_id` (uuid PK, FK→profiles ON DELETE CASCADE), `summary` (text, nullable), `key_patterns` (jsonb, nullable), `created_at`, `updated_at`.
  - Indexes: PK + `idx_memory_user_id` on `user_id`.
  - RLS enabled (not forced); 4 policies (DELETE/INSERT/SELECT/UPDATE, all `auth.uid() = user_id`).
  - Trigger: `set_memory_updated_at` BEFORE UPDATE → `public.set_updated_at()`.
  - Row count: **0**.
  - App code references: **none** — no hits in `lib/`, `app/`, `types/`, or any `.sql` migration for `from('memory')`, `from("memory")`, or the string `memory`.
  - Conclusion: the table exists in live with full RLS and a trigger, but has never been written to and is invisible to the application. Options: capture as a baseline (symmetry), drop it (dead weight), or document and defer. Call is the human's.
- **Decision:** memory table left in place, uncaptured — orphaned schema from the unbuilt mentor-conversation-memory feature; revisit (capture vs drop vs reshape) when /mentor scope is defined.
- **Decision:** memory table kept — Zyrro will need a memory capability of some form; the owning surface (/mentor or other) is not yet decided. Table is a correct per-user store to adopt or reshape when that's defined.

### Next
- memory surface/scope to be defined (mentor or other) — determines whether the existing table is adopted as-is or reshaped.

---

## 2026-06-24 — Session 4

### Changed
- Removed both `refreshSession()` fallbacks from `app/identity/page.tsx` `init()`: the `getUser()` miss now goes straight to `'anonymous'`, and the `discovery_answers` count error now goes straight to `/login`. Both were redundant given `proxy.ts`.
- Merged `test/drop-login-bandaids` into `dev`; deleted the throwaway branch locally and on origin.

### Decisions
- proxy.ts confirmed sufficient via JWT-expiry test on preview: session survived two runs (5-min wait at 300 s expiry; 8-min wait with margin) without the band-aids — `/identity` loaded the report correctly each time, confirming `proxy.ts` refreshed the expired token server-side on its own. Band-aids retired.
- JWT expiry restored to original after testing.
