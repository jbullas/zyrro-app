## 2026-06-24 — Session 1

### Changed
- Added migration `20260624000000_drop_redundant_artifacts_rls_policies.sql` and applied it to the live DB, dropping the two duplicate RLS policies on `public.artifacts`:
  - `"Users can insert their own artifacts"` (duplicate of `"Users can insert own artifacts"`)
  - `"Users can view own artifacts"` (duplicate of `"Users can read their own artifacts"`)
- Verified before and after via `pg_policies`: four policies remain, one each for DELETE, INSERT, SELECT, UPDATE.

### Decisions
- Kept the `… own artifacts` naming style (matches DELETE/UPDATE policies); the `… their own …` / `… view …` variants were the ones dropped.
- Baseline migration `20260531000000_artifacts_baseline.sql` left untouched — it is a historical point-in-time capture of the live DB and must remain as-is.

### Next
- Relocate `set_updated_at` trigger (separate task, flagged as do-not-touch in this brief).

---

## 2026-06-24 — Session 2

### Changed
- Created `supabase/migrations/20260530000000_shared_functions.sql` housing `public.set_updated_at()` as the single canonical definition.
- Removed the function definition and its TODO comment from `20260531000000_artifacts_baseline.sql`; updated that file's prerequisite comment to reference the new migration.
- `set_artifacts_updated_at` trigger left in place; no live-DB changes made.

### Decisions
- Pre-dated the shared-functions migration to `20260530…` so it sorts before the artifacts baseline (`20260531…`) in migration order — a today-dated migration would break fresh builds because the baseline's trigger references the function.
- Did not apply `20260530000000_shared_functions.sql` to live DB — it is a `CREATE OR REPLACE` over an identical existing function (a true no-op), so applying it is optional.
- Did not `DROP` the function anywhere — live triggers on `profiles` and `conversations` depend on it.

### Next
- Full-schema capture for `profiles` and `conversations` tables (dashboad-created, not yet in migrations).
